# Changelog

所有項目依 `CHANGE_CONTROL.md` 記錄，並以使用者結果與可重現驗證為核心。

版本號與 git tag 一一對應（`v0.2.0` = `## [0.2.0]`），`/api/health` 會回報執行中服務的版本——
「現在跑的是哪一版、出事回滾到哪、這個修補在哪一版進來」這三題要能從執行中的服務答得出來。

## Unreleased

### Security

- Security｜帳密保管金鑰搬進 macOS Keychain，不再跟密文同住 data/｜金鑰檔 `.secret-vault-key` 就放在它保護的 SQLite 旁邊——整包拷走 data/ 等於同時拿到鎖和鑰匙。現在金鑰存 login Keychain(服務名 `agent-hub-secret-vault`)，既有安裝啟動時自動遷移：讀回驗證逐位元一致才刪舊金鑰檔，之後 Keychain 讀不到一律大聲報錯、絕不默默重生金鑰(那會讓所有已存帳密變成解不開的垃圾且症狀完全看不出根因)。跨機還原走 `npm run key:export` / `key:import`，金鑰與備份分開保管。非 macOS 維持金鑰檔｜`lib/keychain.ts`(新增)、`lib/secretVault.ts`、`scripts/vault-key.ts`(新增)｜驗證：8 個遷移分支測試(含「標記說在 Keychain 但讀不到→報錯不重生」「兩把不一致→用檔案那把且兩邊都不動」)＋真 Keychain 整合測試；真實金鑰遷移後全部既有帳密解密驗證通過
- Security｜備份 zip 裡的網站登入狀態(cookies)改加密後才進包；備份可多抄一份到外接碟｜過去 browser-sessions/ 原封不動進備份，備份檔被拷去別處等於登入狀態明文外流；且 14 天備份跟正本同一顆硬碟，硬碟一死全滅。現在登入狀態以同一把保管金鑰 AES-256-GCM 加密進 `browser-sessions.enc/`(還原時自動解密，舊格式備份仍可還原；解不開時直接把「先 key:import」講清楚不靜默跳過)，設定 `backupMirrorDir` 可每日多抄一份到第二個位置｜`lib/dataBackup.ts`、`lib/secretVault.ts`(encryptBytes/decryptBytes)｜驗證：備份演練測試斷言 cookie 值不以明文出現在 zip、還原後內容一致、金鑰不對時錯誤訊息含下一步指令
- Security｜資料庫裡殘留的明碼帳密啟動時補加密｜「下次儲存才加密」的惰性設計在真實資料上等了很久也沒等到那次儲存(實測有 6 筆 webmail/Google 帳密仍是明碼)。啟動時一次補課，讀取端走同一條解密路徑完全無感｜`lib/settingsStore.ts`(encryptLegacyPlaintextSecrets)、`instrumentation.ts`｜驗證：補加密後值不變、重跑為 0 筆；真實資料 6 筆全部補上且解密回讀一致
- Security｜修補 `ip-address`(SSRF 分類繞過,high)與 `brace-expansion`(DoS) 相依漏洞｜`npm audit` 歸零｜`package-lock.json`｜驗證：`npm audit` 0 筆、mailClient(受影響的 imapflow 鏈)測試全過

### Added

- Added｜常駐服務日誌自動輪替｜launchd 寫的 engine.log/engine.error.log 過去無上限長大。排程 tick 內建 copytruncate 輪替(20MB 上限、留 3 份 gzip 封存；不能 rename——launchd 握著檔案控制代碼，rename 後它會繼續寫進被改名的檔案)｜`lib/logRotation.ts`(新增)、`lib/scheduler.ts`｜驗證：4 個輪替測試(含「原檔必須留在原地」「單檔失敗不拖垮其他」)

### Fixed

- Fixed｜從零建圖的模型第一輪已產出通過其他所有驗收的完整流程圖，只因為缺「排程 cron」一項就整張被丟棄，使用者拿到「AI 建立流程時沒有順利完成」｜以真實使用者需求(每週更新簡報數字的完整流程)實測建圖能力時踩到(診斷編號 fb2b1d95)：第一輪模型呼叫花了 8 分鐘產出完整圖，需求完整性檢查發現唯一缺口是 schedule.cron，把整包餵回模型要求重出——而此時整體建圖 10 分鐘預算只剩 45 秒，修補回合被切斷，整張好圖付諸流水。根因是「缺排程」跟「缺步驟」被同一種方式對待：後者只有模型補得出來，前者其實是使用者自己講過的話(「每週」「每天早上九點」)，確定性解析比再燒一輪模型可靠且永遠不會被預算切斷。新增 `scheduleSuggest.ts`：唯一缺口是排程且原話講得出頻率時直接推出 cron(沒講時間假設 09:00、沒講星期幾假設週一，假設的部分在回覆裡明講可到排程頁改；只講「自動」講不出頻率仍照舊餵回模型——頻率不能猜)｜`lib/workflow/scheduleSuggest.ts`(新增)、`lib/workflow/builder.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1116 個測試全過，新增 12 個 cron 解析測試+2 個建圖迴圈測試：唯一缺排程時一輪交付不燒第二輪模型、無法確定性補時仍走原本的丟棄重畫路徑)、`npm run check:change-guard` 全過

### Fixed（高強度 code review 抓出的 10 個問題，全部來自同一天稍早的改動）

- Fixed｜排程推測在六種常見中文講法上會「靜默排錯時間」，而且假設清單是空的、使用者以為那是自己講的｜對當天新增的 `scheduleSuggest.ts` 做高強度審查(26 個 agent、對抗式驗證)後，確認六個各自獨立的解析缺口：①「每週一到週五」被單日規則先攔截成「只跑週一」，工作日分支永遠執行不到；②只認阿拉伯數字，「每天晚上八點」解析失敗退回早上9點，回覆還反過來說使用者「沒講時間」(台灣口語幾乎都用中文數字報時，命中率極高)；③「晚上12點」(口語=半夜0點)沒做 12→0 轉換，夜間批次被排到中午12點；④頻率優先序是寫死的「月>週>天」，於是「整理每週營收，每天9點更新」這種「資料期間+排程頻率」混講的句子必然選錯；⑤「每月」後面的日期標記是選填，「每月10點」的小時被當成「每月10號」且完全不標假設；⑥「第3點」「3點重點」這種序數/量詞被當成時刻。共同的危險是排程猜錯不會有任何錯誤訊息(需求驗收只檢查 cron 存不存在)，使用者要等到「該跑的那天沒跑」才發現。整支解析重寫並定下一條原則寫在檔頭：**寧可回 null 讓模型去想，也不要猜一個看起來合理的 cron**——頻率不能猜、有猜的部分一定要進 assumed、多個頻率詞分不出哪個在講排程時一律回 null。另補：星期區間泛化成任意 `X-Y`(每週二到週四也支援)、跨週末的區間(週五到週一)cron 表達不了就整句交給模型、`describeSuggestedSchedule` 補上區間的白話描述(否則平台自己推薦的排程只會顯示「自訂的固定時間」)｜`lib/workflow/scheduleSuggest.ts`、`lib/workflow/builder.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1137 個測試全過，排程解析測試從 12 個擴到 32 個，涵蓋上述六類缺口、中文數字時分、頻率衝突回 null、以及「產出的 cron 一定通得過 isValidCron」)、`npm run check:change-guard` 全過

- Fixed｜自訂來源宣告的長等待沒有夾在剩餘建圖預算內，一台當掉的地端模型會吃光預算，重演「幾乎完成的圖被整包丟棄」｜同一天稍早為了讓自訂來源模型有機會上場(見下方 Fixed)，把 builder 的 30/45 秒快速失敗換成「尊重來源宣告的 timeoutMs」，但沒補上上限：來源可宣告到 600 秒，主力模型一 hang 就等滿 10 分鐘，備援鏈還沒開始就被總預算切斷——正好是這一輪其他修改要消滅的失敗。改成主力最多用掉剩餘預算的一半，另一半留給備援鏈；內建 gateway 維持原本的快速失敗不變｜`lib/workflow/builder.ts`｜驗證：同上批次

- Fixed｜建圖預算調到 20 分鐘後，「內容太長、把需求拆小」這個唯一正確的診斷變成永遠不會出現的死碼｜`budgetLimited` 判斷式比較的是 `budgetMs` 與絕對上限，而建圖總預算已經等於絕對上限(20 分鐘)、`budgetMs` 扣掉已耗時後恆小於它 → 判斷恆為 true。結果是「第一輪就拿到近乎完整 20 分鐘、跑滿仍沒產完」(真的是內容太長)的呼叫也被告知「時間被前面的等待用掉了，直接再送一次即可」，使用者照做重送、下次一樣跑滿被砍，無限循環。改成比較「這次**實際**分到多少(maxMs)」與絕對上限｜`lib/claudeCodeClient.ts`｜驗證：同上批次

- Fixed｜「AI 判斷/產生」指定的模型來源被刪掉/改名後，資料會被靜默改送到共用免費 gateway、由另一顆模型做判斷，執行紀錄完全看不出來｜這個欄位當天才加(見下方 Added)，它的存在意義正是「把這一步釘在某個端點」(例如地端模型、資料不出機器)，但底層 `resolveModel` 對不認得的代號刻意「退回內建來源試試看」——那是為流程層模型選單設計的行為，套在這個欄位上剛好造成最不該發生的事：同名模型剛好也存在於共用 gateway 時完全不會報錯。改成先確認「解析到的來源真的宣告了這個模型」(寫成 `來源::模型` 時則確認那個來源還在)，不符就直接以中文說明停下，不再靜默改道｜`lib/workflow/nodes/general.ts`｜驗證：同上批次，另以真實節點執行確認「來源不見→立刻停下並說明」「合法代號不被誤擋」

- Fixed｜修正輪無條件把使用者釘選的 high 推理力度降成 medium，違反「不能靠寫死低推理力度換速度」的既有拍板｜降檔當天的理由是「high 注定被總預算切斷」，但同一批改動已把建圖預算拉到 20 分鐘，這個前提不再必然成立：第一輪 6 分鐘完成、還剩 14 分鐘時，需要深推理的難修正仍被降級，而且使用者無從得知。改成**只看剩餘預算**——夠再跑一輪 high(門檻取實測值 10 分鐘)就完全尊重使用者設定，真的不夠才降檔；主力與備援共用同一套判斷，不再為「備援角色」另設一條無條件降級規則｜`lib/workflow/builder.ts`｜驗證：同上批次

### Added

- Added｜模型的主力與救援順序改由使用者自己排，並可指定「流程跑起來時用哪顆」——建置與執行分離，執行時用得到地端模型｜真實使用情境：公司允許用雲端模型建流程，但建好的流程在執行時、中間做判斷的步驟必須跑在自己掌控的機器上，資料落地審查才過得了。以前流程只有一個模型欄位，同時決定「建流程用誰」和「跑起來用誰」，表達不出這個組合。這次拆成三層可選設定，每一層留空就往上繼承，全部留空 = 與改動前完全相同的行為：①設定頁可排「文字判斷」與「看圖」各自的主力/救援順序（顯示的初始值就是目前生效的順序，直接調整即可），並可勾選「不要自動換模型」；②流程層可單獨指定執行時使用的模型與是否允許自動換；③`AI 判斷`、`AI 看圖片`、`瀏覽器登入`（驗證碼）三種會在執行時呼叫模型的步驟，都可以再單獨指定。模型來源新增「這是我自己掌控的機器」標記（只能由使用者宣告——網址判斷不出來，而這個標記會出現在執行紀錄上），執行紀錄每次呼叫都會寫明用了哪顆、是地端還是雲端、以及自動換模型時換了誰與為什麼換；流程設定面板則在執行前就先顯示「這條流程實際會用哪顆」。挑選邏輯統一收斂到 `lib/modelPolicy.ts` 單一入口，依「實際可用的來源 × 實測過的能力 × 使用者排定的順序」推導，挑不到時回傳白話原因而非退回預設值；新流程的預設模型改為推導（有設定內建 Base URL/金鑰就沿用原預設，否則預設 Claude Code），全新安裝不再列出使用者尚未設定的模型代號｜`lib/modelPolicy.ts`(新增)、`lib/modelProviders.ts`、`lib/settingsStore.ts`、`lib/db.ts`、`lib/workflow/nodeHelpers.ts`、`lib/workflow/nodes/general.ts`、`lib/workflow/nodes/readImage.ts`、`lib/workflow/nodes/browserLogin.ts`、`lib/workflow/store.ts`、`app/api/model-providers/route.ts`、`app/api/workflows/[id]/route.ts`、`app/settings/ModelProvidersCard.tsx`、`app/workflows/[id]/WorkflowSettingsPanel.tsx`、`app/workflows/[id]/page.tsx`、`AGENTS.md`｜驗證：`npx tsc --noEmit`、`npm test`（1146 個測試全過，新增 11 個模型挑選測試：使用者排定的順序優先於平台預填、清單中暫時叫不動的自動略過、節點指定即獨佔且不排救援、來源被刪除時回空鏈並說明而非改用同名模型頂替、嚴格模式只保留主力、驗證碼不會挑到會拒絕解驗證碼的模型、無可用能力時回傳可行動的原因、地端/雲端標示正確）、`npm run check:change-guard` 全過

- Added｜「AI 判斷/產生」步驟新增選填的「這一步用的模型」欄位——節點級 AI 終於可以跟「整條流程的模型」分開｜實測結論是使用者的地端模型「小輸入小輸出的判斷可以、建圖/產碼那種大包不行」，但以前模型是流程層單一開關：想讓地端模型只做判斷，就得整條流程(含建圖、產碼)一起換過去，正好踩進它做不到的事。新欄位留空=完全維持原行為(用流程模型)；填了就依模型代號解析它所屬的來源(baseUrl/apiKey/該來源宣告的逾時)，「來源::模型」寫法也支援；不認得的代號依 modelProviders 的既有設計退回內建來源試(不另設守門，跟流程層模型選單同一行為)｜`lib/workflow/nodes/general.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1117 個測試全過，新增 1 個測試釘住「欄位存在且留空合法=不改變既有流程行為」)、`npm run check:change-guard` 全過；部署後建了一條「觸發→AI判斷(指定地端模型)」的迷你流程真跑驗證，執行紀錄確認回答確實由指定的地端模型產生、流程模型維持原設定不受影響，測試流程驗完即刪

### Fixed

- Fixed｜自訂模型來源(例如地端模型)在對話建圖時永遠沒得上場——45 秒就被靜默切到備援模型，使用者自己在來源設定裡宣告的等待時間(timeoutMs)完全被忽略｜真實踩到：使用者問「用我的地端模型建得出來嗎」，實測發現它 45 秒就被切去備援，測到的全是別的模型。30/45 秒短等待是為「共用免費 gateway 常整段沒回應」設計的(快速失敗、把預算留給備援)，但自訂來源是使用者明確選擇且自己宣告了 timeoutMs 的——地端模型吐一整包建圖 JSON 本來就要幾分鐘。改成：主力模型屬於自訂來源且有設 timeoutMs 時，用「builder 預設」與「來源宣告值」較大者；備援模型(都在共用 gateway 上)維持短等待不變｜`lib/workflow/builder.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1116 個測試全過)、`npm run check:change-guard` 全過；部署後以地端模型實測建圖(等待時間正確套用來源宣告值)

- Fixed｜「從零建圖」在需要修正輪時必然失敗——建圖總預算 10 分鐘在數學上只塞得下本機 Claude Code 的一輪呼叫｜同一天實測四次死在同一個上限(診斷編號 fb2b1d95/54a05cd3 及兩次重測)：從零建圖的完整提示(40k+ 字)走本機 Claude Code 一輪 7~9 分鐘(實測 medium 474 秒、high 400~530 秒——力度降檔救不了這個規模，瓶頸是提示與輸出的體積)，需求完整性驗收抓到缺口後的修正輪又要同等時間，10 分鐘內任何需要修正的從零建圖必然被切斷且整包丟棄。總預算拉高到 20 分鐘(兩輪+餘裕)：這個上限的本意是防失控不是節奏控制，gateway 模型的失敗本來就快(45 秒 timeout)不受影響，只有「模型真的還在想」會用到 20 分鐘，畫面有進度列與停止鈕隨時可中止；env 覆寫上限同步放寬到 30 分鐘｜`lib/workflow/buildControl.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1116 個測試全過)、`npm run check:change-guard` 全過；部署後以同一個真實需求端到端重測成功——四輪對話後 651 秒交付 phase:ready 的 12 步完整流程(拉高前必死的「第一輪+修正輪」這次都跑完了)，需求核對四項全 ✅、確定性補排程正確出手(每週→週一 09:00)、套用到畫布+排程建立+說明面板全部正常；測試用的流程與排程驗證完已清除

- Fixed｜relay 掛掉時本機 Claude Code 以「最後備援」身分上場，卻仍用高推理力度——上場那一刻預算必然已被 relay 的等待消耗過，40k 字建圖提示在高力度要 8 分鐘上下，實測差幾十秒被整體預算砍掉、整包丟棄(診斷編號 54a05cd3：主力 timeout＋備援模型 503 後只剩 527 秒)｜「使用者自選 Claude Code 當主力」和「relay 掛掉後被迫當備援」是兩種情境：前者第一輪拿得到完整 10 分鐘預算，維持使用者設定的力度(實測 449 秒成功交付)；後者一律降到 medium——「完成的 medium」嚴格優於「被砍的 high」。同場加映：產碼規則補上「換日期不能用寬鬆 M/D regex」鐵則(實測建圖產出的程式碼用 \\d{1,2}/\\d{1,2} 去撈「資料日期：2026/8/4」會咬到年份中段抓出「26/8」，替換後整格變亂碼)｜`lib/workflow/builder.ts`、`lib/workflow/codegen.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1116 個測試全過)、`npm run check:change-guard` 全過

- Fixed｜用一句話幫既有流程「加功能」成功後，完成度核對誤報「你提到的值完全沒有出現」——新增的值明明全部落在新節點的設定裡｜真實踩到：一句話新增三個步驟(含使用者點名的分頁名稱與範圍)，三個步驟都正確建立、值都正確落地，回覆卻附上「請再確認：你提到的◯◯完全沒有出現」。根因：完成度核對的比對範圍只算「被修改設定的既有節點」(`applied` 的 nodeId)，結構修改「新增」的節點完全沒算進去——而「加功能」的值幾乎都落在新節點裡。把結構修改成功新增的節點 id 一併算進比對範圍｜`app/api/workflows/[id]/build/route.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1116 個測試全過)、`npm run check:change-guard` 全過

- Fixed｜產碼規則叫 AI「更新簡報文字一律整段刪除重寫」，混合格式的段落(部分數字粗體/不同字級/不同顏色)照做會把使用者排好的格式全部洗掉｜真實驗證過的案例：簡報上的 KPI 說明段落裡「53」「43」是粗體、括號補充是小一號字級——整段刪除重寫後全部變成同一種格式。舊規則禁止「定位單一數字」的原因是「寫死 startIndex 在資料一變就錯位」，這個顧慮對「執行當下用 regex 對剛抓下來的文字找位置」不成立(當天已在兩條真實流程的正式執行中驗證：11 處同時替換、粗體/字級/顏色/字型全部保留，且重跑冪等)。規則改成依格式是否均一分兩種：均一的方塊照舊整段重寫；混合格式的段落用「執行期 regex 定位＋FIXED_RANGE 刪插＋原樣式套回＋由後往前替換＋值相同跳過」，並要求比對不到句型時 throw 附原文，不准硬猜位置｜`lib/workflow/codegen.ts`(GOOGLE_SLIDES_TEXT_UPDATE_RULES)｜驗證：`npx tsc --noEmit`、`npm test`(1116 個測試全過)、`npm run check:change-guard` 全過；規則描述的做法本身當天已在真實簡報的正式執行中驗證

- Fixed｜本機 Claude Code 高推理力度下，建圖的任何「第二輪」(修正/加功能後的驗證輪)都注定撞上 10 分鐘整體預算被砍，整包丟棄什麼都沒套用｜同一場實測的第二個發現(診斷編號 81985119)：對一條 24 步的真實流程用一句話要求加功能，第一輪模型呼叫(高力度、68k 字提示)就花 490 秒，第二輪剛起步就被預算切斷，HTTP 408、整包丟棄。高力度在這種規模的提示上單次 8 分鐘上下，而修正輪其實是「有精確回饋的定向小修」(補排程/修接線)，實測 medium 力度 30～140 秒就能完成。改成：第一輪維持使用者設定的力度(開放式設計需要它，不違反「不能寫死低推理力度換速度」的既有拍板)；修正輪、或剩餘預算不足 4 分鐘時降到 medium——「一次 medium 完成」嚴格優於「一次 high 被砍、交付零」。另修同場發現的誤導錯誤訊息：預算見底時 Claude Code 的失敗訊息原本說「內容太長請把需求拆小」，實際上是這次呼叫只分到幾十秒——照那個建議去拆需求只會白忙，改成老實講「時間被前面的等待用掉了，直接再送一次即可」｜`lib/workflow/builder.ts`、`lib/claudeCodeClient.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1116 個測試全過)、`npm run check:change-guard` 全過；力度降檔的實際效果以真實建圖請求重測確認(見上則)

- Fixed｜「用網頁信箱寄信」節點失敗時，存下來的除錯畫面看不到寫信表單的內容，查不出真正卡在哪個欄位｜使用者實測踩到一次「主旨記錄已填，但畫面截圖是空的」的失敗，回頭想查是哪個欄位填錯，卻發現存下來的除錯 HTML 裡完全找不到「標題」兩個字。原因：這套信箱的寫信表單常常是嵌在 iframe 裡的獨立文件，`saveDebug()` 只存了 `page.content()`(頂層文件)，沒存 iframe 內部真正的內容——調查到這裡就沒有更多資料可以往下查了。改成除錯存檔時，把頁面上每一層 iframe 的內容都各自存一份，之後同類失敗才有真正的第一手資料可以核對；這次事後重跑两次都正常送出，研判是這套信箱畫面「分好幾層慢慢載入完成」造成的間歇性時機問題(這個節點的既有註解就記錄過同一套系統的類似案例)，不是每次都會發生的固定錯誤｜`lib/workflow/nodes/webmailSend.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1088 個測試全過)、`npm run check:change-guard` 全過；額外用「這次寄出的信先都寄給我自己」對這條真實流程實際觸發兩次正式執行(不是安全排練)，兩次都完整跑到「寄信」這一步、確認寄出的信真的送達測試信箱、標題與內文都正確，同時也用這兩次真跑驗證了上面「Excel 加總」那則修復的數字是對的

- Fixed｜「Excel 篩選+highlight」節點沒有輸出「highlight 那一欄的加總」，下游只能拿 rowCount(篩到幾列)頂替，算出來的業務數字是錯的｜使用者實測踩到：兩條分開算的業務數字剛好都跟篩選區間的天數一樣——兩個不相干的業務量因為都套用了同一個錯誤公式(拿「篩到幾列」當「加總」)，湊巧都等於篩選區間的天數，直接讀真實的 Excel 檔案核對後確認兩邊的正確答案完全不同、也都不是那個天數。根因：`excel-process` 節點的 `outputs` 只有 `rowCount`(篩到幾列)，完全沒有「highlight 那一欄的加總」這個欄位可以引用，下游只能拿 rowCount 湊合(這個節點本來就已經在同一趟迴圈裡逐列讀取 highlight 那一欄的值來畫底色，加總只是多做一件事，不用重新讀檔)。新增輸出欄位 `highlightColumnSum`：只加總真的是數字的儲存格，文字/空白/區間外的列不算進去，dry-run(只讀驗證)也算得出同樣結果。這條流程裡兩個「設定變數」步驟的引用也一併從 `{{rowCount}}` 改成 `{{highlightColumnSum}}`｜`lib/workflow/nodes/excelProcess.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1088 個測試全過，新增 1 個測試用「3+5(數字)+備註(文字,跳過)+區間外的999(不算)」的資料驗證加總只算對的那幾格，且正式執行與 dry-run 結果一致)、`npm run check:change-guard` 全過；額外直接讀這條真實流程失敗當下下載的原始 Excel 檔案手動核對，確認算出來的正確答案才是對的

- Fixed｜對話裡某一輪回覆講「下方預覽新流程，確認後按『套用』」，畫面卻完全沒有預覽卡可以按｜使用者實測踩到：一則要求修改流程的訊息，回覆的文字明講有預覽，直接查伺服器資料證實那一輪根本沒有存下任何 `pendingGraph`。根因：這句提示只有 `phase:"ready"` 才由前端(`wfChatStore.ts`)自動掛上，但這句話一旦出現在對話歷史裡，就會被送回模型當成「上一輪 AI 說過的話」——模型在別的 phase(例如 `phase:"clarify"`，可以自由先描述一份方案再確認)有機會照樣抄一句類似的話，那一輪根本沒有東西可以預覽。新增 `stripReadyOnlyPromise()`，在 phase 不是 `"ready"` 的三個顯示路徑(演練/直接套用的 edits/一般回答)一律把這句話濾掉再顯示；順便修掉自己造成的另一個小問題：新增的「複製既有流程節點」功能(見上則 Added)自己的訊息裡也重複寫了一次同一句提示，導致畫面顯示兩次——拿掉，交給前端統一掛一次就好｜`lib/wfChatStore.ts`、`lib/workflow/importExistingWorkflowNodes.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1084 個測試全過，新增 5 個測試涵蓋濾除/半形全形標點/不誤刪正常內容/濾掉重複兩次)、`npm run check:change-guard` 全過；額外用 Playwright 直接開這條真實流程的對話，確認清掉 localStorage 後從伺服器重新載入，舊訊息裡的誤導文字也一併清乾淨了

- Fixed｜高強度 code review(9 個確認/高可能性發現)一次修完，主要是今晚 Mail2000/子流程相關改動自己引入的落差｜①`browserLogin.ts` 的登入成功判斷：卡在雙重驗證以外的未知未登入畫面(安全驗證/條款確認等)原本會直接誤判成功，改成只要沒偵測到已登入特徵就一律攔截，2FA 判斷只用來決定錯誤訊息講哪一種。②`engine.ts`／`subWorkflow.ts`：A→B→A 互相呼叫的子流程沒有被擋下來，會排進佇列卡到 15 分鐘逾時才失敗——改成每次呼叫前帶著完整呼叫鏈(`__subChain`)，目標已經在鏈上就在送出前立刻報錯。③`actionRecorder.ts`：「錄一段操作給我看」沒有跟進共用登入狀態(`shareLoginAcrossWorkflows`)的機制，還在讀每條流程各自一份的舊檔名，永遠讀不到已經手動登入過的共用狀態，逼使用者在錄製視窗裡重新登入——改成跟 `manualLogin.ts` 一樣查有沒有開共用登入，讀寫對應的共用檔案。④`builder.ts`：對話裡提到既有流程名稱時的「真實內容參考」功能，呼叫時忘了排除正在編輯的這條流程自己，使用者訊息剛好提到自己的名字會被誤判成可以呼叫的既有流程，執行時撞上「子流程不能呼叫自己」——把流程 id 真的傳進去排除自己。⑤`app/page.tsx`：首頁「有 N 件事要你處理」橫幅的自動展開判斷漏算了待處理的 AI 修復提案，只有這一種待辦事項時橫幅會保持收合，使用者要自己點開才看得到。⑥`lib/webmailKeepAlive.ts`：續命這段(開瀏覽器+造訪+等待)要好幾秒，這段期間如果手動登入視窗或正式執行剛好也在寫同一份共用登入狀態，續命寫回的可能是讀檔當下的舊快照，蓋掉剛更新的新狀態——改成寫回前重新比對檔案修改時間，變了就放棄這次寫入。⑦把三處(`engine.ts`／`manualLogin.ts`／`webmailKeepAlive.ts`)幾乎一模一樣的登入狀態檔原子讀寫邏輯抽成共用模組 `lib/workflow/browserSessionFile.ts`，之後只要改一處。⑧`subWorkflow.ts` 拿掉重構後沒用到的 `getMaxConcurrent` 死 import｜`lib/workflow/nodes/browserLogin.ts`、`lib/workflow/engine.ts`、`lib/workflow/nodes/subWorkflow.ts`、`lib/workflow/actionRecorder.ts`、`app/api/workflows/[id]/record/route.ts`、`lib/workflow/builder.ts`、`app/api/workflows/[id]/build/route.ts`、`app/page.tsx`、`lib/webmailKeepAlive.ts`、`lib/workflow/browserSessionFile.ts`(新增)｜驗證：`npx tsc --noEmit`、`npm test`(1072 個測試全過，新增/更新約 20 個測試，其中子流程死結修復是真的用真實引擎跑一次互相呼叫、確認幾百毫秒內就報錯而不是卡到逾時才失敗)、`npm run check:change-guard` 全過；**刻意不修**：`subWorkflow.ts` 拿掉併行門檻後，理論上母流程若自己也開著瀏覽器、同時呼叫子流程，兩者可能同時各開一個瀏覽器行程——但這條流程目前的實際用法(母流程只負責呼叫子流程+合併結果+寄信，本身不開瀏覽器)不會踩到，保留現況；真的需要「任何情況下都保證只有一個瀏覽器」時再回頭處理

- Fixed｜流程頁的「📖 說明」按鈕在一般螢幕寬度只看得到圖示、看不到文字，導致這個本來就存在、能完整白話解釋整條流程每一步在做什麼的功能形同隱形｜使用者反映首頁搜尋加了步驟關鍵字後仍然「不夠清楚」，追問才發現他真正要的其實是「進去工作流裡面看完整到底做了什麼」——而這個功能其實已經存在(`lib/workflow/explain.ts` 的 `explainWorkflow`)，只是工具列上另外三顆按鈕(🔗資料流/📋紀錄/⚡自動開始)的文字要 1400px 以上的視窗才會顯示，「說明」按鈕的中文字被一起套用同一個門檻，實際使用情境下幾乎永遠是純圖示，沒有 tooltip 以外的任何提示，才會一直沒被發現。把「說明」按鈕的文字顯示門檻單獨降到一般手機/平板以上寬度(`sm:`)就會顯示，其餘三顆維持原本 1400px 門檻不動(那是先前修過的工具列擠爆 bug，不能一起放寬)｜`app/workflows/[id]/page.tsx`｜驗證：`npx tsc --noEmit`、`npm test`(1090 個測試全過)、`npm run check:change-guard` 全過；額外用 Playwright 把視窗縮到 1280px 寬(一般筆電常見尺寸)截圖確認「說明」按鈕現在看得到文字、工具列沒有被撐爆，並實際點開這個按鈕，對使用者一條真實的 30 步複雜流程(簡報週報更新)完整讀出說明面板的內容

- Fixed｜「📖 說明」面板裡有 7 種節點型別完全沒有專屬說明，只顯示同一個節點型別共用的靜態介紹，看不出這一步實際動到什麼｜使用者用過上一則修好的「說明」面板後回饋：「像是替換投影片那種，我就不知道是換了哪些啊，我想要看到」。查證：`explain.ts` 用一個大 switch 逐節點型別客製說明文字，但 `google-slides-replace-image`(換簡報圖片)、`google-slides-refresh`(重新整理簡報圖表)、`google-slides-create`(建立新簡報)、`excel-range-image`(Excel 範圍截圖)、`telegram-notify`、`line-notify`、`webmail-send`(用網頁信箱寄信) 這 7 種型別完全沒有對應的 case，全部落到最後的 `default`，只顯示 `NodeDefinition.description`(同一個節點型別在所有流程裡都一模一樣的靜態介紹文字)，看不到這個節點實際設定了「換哪一頁」「傳什麼訊息」「寄給誰」。逐一補上 7 個 case，各自顯示該節點真正用到的設定值(換圖片：目標簡報＋用標題找哪一頁＋新圖片來源；通知類：實際的訊息內容；寄信：收件人＋主旨)；順便把既有的 `slack-notify` case 也一併修好——它雖然有自己的 case，卻只講「到設定頁串接」這種通用話，同樣沒把訊息內容顯示出來，是同一類問題｜`lib/workflow/explain.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1093 個測試全過，新增 4 個測試涵蓋換簡報圖片講出哪一頁、Telegram/LINE 顯示實際訊息、網頁信箱寄信顯示收件人與主旨)、`npm run check:change-guard` 全過；額外對使用者這條真實流程重新截圖，確認「換掉開戶數頁的表格圖」這一步現在顯示「找到簡報裡標題含『開戶數』的那一頁…」而不是原本看不出差異的通用介紹文字

- Fixed｜首頁搜尋框找不到「我記得做過某個功能，但不記得是哪條流程」——上一版的修法只比對步驟的「名稱」，使用者記不清確切命名時還是找不到｜使用者用過上一版加的步驟搜尋後仍然回報：「我上次有多加一個功能是更新簡報的一頁圖，但是我現在不知道是更新在哪個工作流，我進去看說明也找不到」。查證：真實環境裡那一步的命名是業務化的「換掉開戶數頁的表格圖」，使用者用「更新簡報圖片」這種泛稱去搜，兩邊完全對不上字——搜尋語料只有步驟名稱一項，記不清命名就等於搜不到，而使用者本來就不可能記得自己幾個月前取的每一個步驟名稱。改成搜尋語料跟流程頁「📖 說明」面板共用同一份 `explainWorkflow()`：除了步驟名稱，也把白話說明句(含節點類型講的「換簡報上的圖片」這種泛稱動詞)與設定裡的實際值(目標分頁、找哪一頁、訊息內容)一起收進來——不管使用者記得的是「做了什麼」還是「內容是什麼」，甚至只記得泛稱說法，都比對得到；且跟「說明」面板顯示的是同一份事實，不會有兩邊對不起來的風險｜`app/api/workflows/route.ts`、`app/page.tsx`｜驗證：`npx tsc --noEmit`、`npm test`(1093 個測試全過)、`npm run check:change-guard` 全過；額外對正式環境實際搜尋「圖片」(不在該步驟自訂名稱裡、只出現在節點類型的泛稱說法「換掉簡報上的圖片」裡)，確認現在找得到那條流程並正確標出是「換掉開戶數頁的表格圖」這一步

- Fixed｜自訂步驟(custom-code)的說明只講得出「替換投影片圖表」這種類別名稱，講不出「換的是哪一頁」——業務內容一多，光看類別完全分不出兩個同類步驟的差別｜使用者實測踩到：兩條結構相似的流程各自有一個「更新簡報」類的自訂步驟，光靠說明面板顯示的類別名稱完全分不出彼此差在哪，原話：「他就要說清楚是換哪一頁投影片啊，不然數量一多起來根本不知道幹了什麼了」。查證：這一步實際「用什麼文字判斷是哪一頁」寫死在已經產生的程式碼裡(例如同時比對頁面文字含「行銷業績總覽」和「整體營運全貌」)，但連給產碼器看的 intent 本身都只寫得出「找目標頁與最大 image 元素」這種泛稱——這個具體到能分辨彼此的關鍵字，intent、label 兩層都看不到，只存在程式碼內部。新增 `extractCodeTargets()`：從程式碼裡「明確拿來當比對條件」的寫法(`.includes()`、`===`、宣告成全大寫常數)抓出含中文的字面值，過濾掉字串接變數組出來的 log/錯誤訊息半句話(這種寫法只抓得到「簡報共」這種無意義片段，比沒有說明更誤導人)；custom-code 的說明文字與設定欄位一併把抓到的關鍵字列出來｜`lib/workflow/explain.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1096 個測試全過，新增 3 個測試涵蓋正確抓出比對關鍵字、純技術英文命名一律排除、說明面板正確顯示抓到的關鍵字)、`npm run check:change-guard` 全過；額外對使用者這條真實流程重新讀取說明面板，確認「替換投影片圖表」這一步現在額外顯示「程式碼裡會核對／找出：『行銷業績總覽』『整體營運全貌』『每週開戶數折線圖_◯◯』『週開戶趨勢』」，跟同一條流程裡另一個「更新 ETC 簡報文字方塊」步驟顯示的關鍵字完全不同，兩者不再無法分辨

- Fixed｜排程執行失敗時，錯誤訊息把 Google 錯誤頁面的完整原始 HTML(兩三千字，含 CSS/meta 標籤)整段塞進去，使用者完全看不懂發生什麼事｜使用者原話：「他給了一大串的報錯根本看不懂」。查證：兩條週報流程各有兩個自訂步驟(規劃/執行「搬移歸檔」)會呼叫使用者自己部署的 Apps Script，錯誤處理寫的是 `throw new Error(\`Apps Script HTTP ${res.status}：${await res.text()}\`)`——`res.ok` 為 false 時**不分青紅皂白把整個回應內容原封不動塞進錯誤訊息**，這支 Apps Script 目前不可用時 Google 回的是一整頁 HTML，直接照貼給使用者看。同時**直接重現了實際故障**：用跟工作流程式碼一模一樣的請求方式呼叫這支 Apps Script 網址，目前真的收到 Google「找不到網頁」的通用錯誤頁而非正常回應——這代表 Apps Script 部署本身需要使用者到自己的 Google 帳號檢查(可能被刪除或存取權限被改過)，不是程式碼能修好的問題；另一條「全員週會簡報kpi表更新」流程在對應步驟也是一模一樣的故障，且在 2026-07-31 都還正常，判斷是 Google 那邊的東西在這之後起了變化。修法分兩層：①直接修好這兩條真實流程裡 4 個受影響步驟(各流程各 2 個)的已存程式碼，比照平台內建 Google Sheet 節點既有的清楚錯誤訊息模式——404 給固定的中文提示(部署可能被刪除，指路重新部署)、5xx 給「Google 暫時出錯稍後再試」、其餘狀態才附回應內容且裁到 300 字以內，不再整段照貼；②在 custom-code 產碼規則(`GOOGLE_SHEET_SCRIPT_CELL_RULES`)加一條規則，避免未來重新產生這類程式碼時再犯同樣的錯｜`lib/workflow/codegen.ts`；4 個工作流節點的已存程式碼(`規劃週欄搬移`／`執行週欄搬移寫入`／`判斷是否需要搬移歸檔並規劃位置`／`執行週期搬移歸檔並寫入新頭`，經 `PATCH /api/workflows/[id]` 的 `nodeConfig` 安全路徑直接修正，只換錯誤處理那一小段、不動其餘邏輯，套用前逐一以 `new AsyncFunction` 驗證語法合法)｜驗證：`npx tsc --noEmit`、`npm test`(1096 個測試全過)、`npm run check:change-guard` 全過；額外對正式環境的 4 個節點重新讀取設定，確認新的錯誤處理已生效且不是舊的整段塞 HTML 寫法；**Apps Script 部署本身是否需要重新部署，仍需使用者自行確認**，AI 沒有使用者的 Google 帳號存取權限，這部分無法代為修復

- Fixed｜上一則修復的診斷後來證實是錯的：一開始用 `curl` 重現故障、判斷是「Apps Script 部署被刪除或設定被改」，但後來改用跟正式程式碼完全一樣的呼叫方式(Node.js `fetch`，`redirect:"follow"`)重測，同一支網址反而正常，連續多輪、每輪 6～8 次幾乎全過，只有極少數幾次仍會收到 Google 的錯誤頁——**真正的情況是這支 Apps Script 偶爾會暫時性失敗，不是部署壞掉**；`curl` 處理這種轉址的方式跟 Node 的 `fetch`不一樣，才會一直重現出「壞掉」的錯覺，把使用者導向不必要的「去 Google 檢查部署」這條路。查證方式：對兩條流程各自的 Apps Script 網址(含使用者另外從「管理部署作業」畫面找到的舊部署)用 Node.js 直接打了共 24 次請求(6 種組合 × 每種多輪)，同一支網址前後兩次測試結果不一致(這輪全過、上輪有失敗)，是判斷「間歇性、不是永久壞掉」的直接證據。修法：不改網址(兩支原本設定的網址其實都還能用，不需要重新部署)，改成幫這 4 個步驟的 Apps Script 呼叫加自動重試(最多 3 次、間隔遞增)——網路/格式異常這種偶發狀況會自動重試撐過去；腳本本身回的業務錯誤(例如真的找不到分頁)判斷是確定性問題，不重試、直接往外丟，不浪費時間｜4 個工作流節點的已存程式碼(同上則清單)，一樣經 `nodeConfig` 安全路徑更新、套用前以 `new AsyncFunction` 驗證語法｜驗證：對正式環境這 4 個節點重新讀取設定，確認新版程式碼含重試邏輯；**教訓**：診斷外部服務故障時，重現用的呼叫方式必須跟正式程式碼完全一致(同一個 HTTP client 實作)，不能圖方便用 `curl` 代替，兩者對重新導向/邊界情況的處理不一定相同，會產生誤導的偽陽性結論

- Fixed｜上一則加的自動重試把「執行週欄搬移寫入」這個節點拖過 90 秒逾時，反而讓原本會成功的執行失敗｜替使用者新增功能後照慣例對整條流程實際跑一次驗證，撞到這個自己造成的回歸：這個節點裡有兩段刻意設計成「失敗只記警告、不讓整個節點失敗」的輔助動作(同步日期到另一個分頁、複製儲存格格式)，本來會在幾百毫秒內快速失敗並被自己的 try/catch 接住；套用重試後，這兩段輔助呼叫變成每次都先重試 3 次(最多多等 9 秒)才輪到它們自己的 try/catch，兩段加起來最壞多等將近 20 秒，遇到當下 Apps Script 本來就比較不穩的時段，直接把整個節點推過引擎的 90 秒硬性逾時，從「輔助動作失敗但主要任務成功」變成「整個節點被引擎強制中止」。修法：`call()`/`callScript()` 加一個可選的 `maxAttempts` 參數，這兩段本來就有自己優雅降級的輔助呼叫傳 `1`(不重試，維持原本快速失敗的行為)，真正需要重試撐過暫時性問題的主要寫入/核對呼叫維持預設 3 次｜4 個工作流節點裡的「執行週欄搬移寫入」(`applyWeekRotation`，兩條流程各一個)｜驗證：對正式環境重新完整執行這條流程，確認不再卡在這個節點的 90 秒逾時

- Fixed｜連續三次對正式流程完整執行都在不同節點失敗，查出來是平台內建的「更新 Google 試算表指定位置」節點對 Apps Script 網址回應 404 一律直接放棄、完全不重試，而這個 404 其實跟前面兩則修復查到的一樣是暫時性問題｜驗證上面兩則修復時，把整條真實流程完整跑了 3 次：第一次卡在「執行週欄搬移寫入」(上一則已修)，後兩次改卡在「填回主管報告上月存款餘額」、「填回主管報告年累計」——都是同一支已經證實正常的 Apps Script 網址、只是隨機卡在不同的寫入步驟。查證：`lib/workflow/nodes/googleSheet.ts` 的 `callSheetScript()` 收到 HTTP 404 一律丟 `PermanentError`，這個型別在引擎裡(`runNodeWithRetry`)會直接跳過重試機制(即使節點型別本身宣告 `retryable: true` 也沒用)，等於把「這個節點可以重試」的設計實質關掉了。既然同一支網址在同一次完整執行裡前面的寫入步驟明明成功、只是後面隨機某一步 404，代表部署本身沒有問題，是偶發的暫時性失敗——把 404 從 `PermanentError` 改成 `RetryableError`，讓引擎既有的重試機制(retryable 節點 3 次、3s/9s 退避)接手；如果 3 次後真的還是 404(部署真的被刪除)，一樣會用同一句「寫入網址回 404——部署可能被刪除或網址貼錯」的清楚中文訊息失敗，不影響原本的錯誤說明品質。這個節點型別是平台內建、被所有流程共用，不是只影響這兩條週報流程｜`lib/workflow/nodes/googleSheet.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1097 個測試全過，新增 1 個測試鎖住 404 要丟 `RetryableError` 不能丟 `PermanentError`)、`npm run check:change-guard` 全過；額外對正式環境重新完整執行這條流程驗證(見下一則的完整結果)

- Fixed｜使用者連續多次實際執行都失敗，量到根本原因是 Apps Script 這個外部服務會「整段幾十秒不穩、然後自己恢復」，而所有重試層加起來都撐不過一次抖動｜使用者原話：「我實際執行又不能了」。這次不再猜——對同一支網址、同一個請求連打四次量延遲，實測到：31.3 秒後回 404 錯誤頁 / 2.4 秒成功 / 9.4 秒成功 / 1.5 秒成功。也就是這個服務不是壞掉，是**抖動**：會有幾十秒到一分多鐘完全不通，然後自己好起來。對照失敗那次的執行紀錄：「填回週增量」這一步從 03:59:57 撐到 04:01:17(80 秒)、引擎層的 3 次重試(3s/9s 退避)全部落在同一個壞窗裡，三次都失敗，整條流程就掛了。之前幾則修復都只是把單一層的重試補上，但每一層都太沒耐心，湊起來還是撐不過一次抖動。修法：在「重跑不會有副作用」的呼叫外面再包一層更有耐心的重試(3 次、5s/12s 退避)，跟引擎層的重試相乘，總耐受窗從不到 1 分鐘拉到好幾分鐘；只重試 `RetryableError`(連線中斷/逾時/404/5xx 這些已證實會自己恢復的)，`PermanentError`(網址格式錯、腳本版本太舊、回應看不懂)重試再多也不會變好，直接往外丟不浪費時間。**安全邊界**：只有 `updateTable`(寫絕對值+讀回核對)、`readCells`、`capabilities` 這三個冪等動作套用；`append`(往下加一列)重跑會多寫一列，維持單次呼叫、由節點層既有的 idempotencyKey 機制把關。同時把「更新試算表指定位置」節點的逾時從 60 秒放寬到 4 分鐘——不然重試還沒跑完就先被引擎判逾時砍掉(而且逾時是 PermanentError、不會再重跑)，等於白做；正常情況這一步只要一兩秒，放寬不會讓它變慢。另外把「執行前檢查」自己疊的第二層重試拿掉(改為共用同一份內部重試)，避免疊成 5 分多鐘才告訴使用者一個真的已失效的部署｜`lib/workflow/nodes/googleSheet.ts`、`lib/workflow/preflight.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1101 個測試全過，新增 2 個測試：一個鎖住「暫時 404 要自己重試撐過去並成功」、一個鎖住「append 絕對只能打一次，不能被重試」)、`npm run check:change-guard` 全過

- Fixed｜使用者實際手動執行時，流程在「執行前檢查」這一關就整個失敗，連登入、抓信都還沒開始——比稍早修好的正式寫入步驟本身還更容易被同一種暫時性問題打倒｜使用者原話：「為什麼我實際跑還是失敗？」。查證：這次失敗發生在前一次驗證成功的 12 分鐘後，當下重新測試同一支 Apps Script 網址已經恢復正常，判斷又是同一種偶發性問題；查「執行前檢查」(`preflightExternalIntegrations`，在任何節點開始前就先跑一次無副作用探測，用意是避免使用者等了整條流程跑到最後才發現外部服務掛了)自己補的重試只有 3 次、間隔 1 秒/2 秒，總共撐不到 3 秒，比正式寫入節點透過引擎重試機制撐的 3 秒+9 秒(將近 12 秒)弱得多——這次不穩定的時間比 3 秒長，「執行前檢查」先倒了，即使正式寫入步驟現在已經扛得住這種短暫不穩，也沒機會證明。修法：把「執行前檢查」的重試間隔改成跟引擎節點重試一樣的 3 秒/9 秒退避，兩邊耐受度一致，不會再出現「保護機制比被保護的東西還脆弱」的情況｜`lib/workflow/preflight.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1099 個測試全過，既有的重試測試改用新的退避時間，執行時間對應變長但邏輯不變)、`npm run check:change-guard` 全過

- Fixed｜「找信件」節點偶爾會撈錯信——搜尋框確實填對了查詢字串，畫面卻還是完全沒篩選過的收件匣(跟搜尋前一模一樣的信件總數、頁數)，導致抓到的是「收件匣最上面剛好符合標題關鍵字」的那封，不是真正指定日期那封｜這個 bug 是在幫某條真實流程加跨月資料補齊功能時才第一次真的暴露出來(見下一則)：找「今天最新一封」時就算踩到同一個問題也不會發現(沒篩選的收件匣最上面剛好就是最新那封，恰好蒙對)，這次要找過去某一天的舊信，才第一次抓到錯的信、下載到內容完全不對的附件。逐字比對過搜尋框裡實際填的查詢字串跟人工操作時輸入的完全一致(連括號、空格都相同)，排除了文字/編碼問題；直接截圖確認搜尋後的畫面收件匣信件總數跟搜尋前一樣，證實問題是「按下 Enter 這次沒有真的觸發搜尋」這種偶發互動失敗，不是查詢邏輯寫錯。修法：用「搜尋前後，收件匣可見信件總數有沒有真的變動」當作搜尋是否真的生效的訊號(比固定等待時間可靠——沒真的觸發的搜尋畫面不管等多久都不會變)，沒變動就整套(清空搜尋框→重新輸入→按 Enter)重來，最多重試 3 次；也把固定 1500ms 等待改成優先等網路閒置(networkidle)，兼顧速度與可靠度｜`lib/workflow/nodes/findEmail.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1099 個測試全過)、`npm run check:change-guard` 全過；額外對正式環境重新完整執行需要跨月找信的那條真實流程，確認這次正確抓到了指定日期的那封信、下載到內容正確的附件(見下一則的完整結果)

- Fixed｜某條真實流程在週期跨月時(例如 7/29~8/4，橫跨 7 月與 8 月)會漏算跨到上個月的那幾天，且完全沒有任何錯誤訊息——使用者原話：「像這次跨月份的就需要抓8/1和8/5的信件附件，這樣才會都有算到」｜查證：直接讀了真實下載的日報附件，確認「每日明細」分頁**只有當月的列，換月就重新起算、不會保留上個月的資料**——8/5 那天附件的每日明細只有 8/1~8/4 四天，完全沒有 7 月任何一列。原本算「週增量」的邏輯只是「篩選 periodStart~periodEnd 範圍內的列」，篩不到的列就是沒加到、不會有任何錯誤或警告，是最容易被忽略的靜默漏算。修法分三層：①新增一個步驟判斷這次週期是否跨月，跨月時額外找「這個月 1 號」寄出的信件(它的附件通常還是上個月的完整月表)，多下載一份跨月備援附件；②「計算週增量」改成同時讀主附件與跨月備援附件(有才讀)，兩份檔案的日期列合併使用；③新增「週期內任何一天在附件裡找不到對應日期列就直接報錯」的完整性檢查——這個檢查本身也直接補上了「就算不跨月，同一份附件本身漏了某一天」這種原本也偵測不到的情況。合併加總的邏輯先用真實檔案+我自己造的測試資料驗證過數字正確，才套用到正式流程｜4 個新節點(計算跨月備援搜信日期／判斷是否需要跨月備援附件／找上月份日報信件／下載上月份日報 Excel／標記跨月備援附件路徑，經 `addNodes`/`addEdges` 安全路徑加入)、計算週增量與累計數字的自訂步驟(經 `nodeConfig` 安全路徑更新程式碼)｜驗證：對正式環境完整執行這條真實流程，確認①正確判斷 7/29~8/4 跨月、找到 8/1 那封信、下載到的附件確認含 2026-07-29～07-31 三天的完整資料列(直接讀檔驗證)；②「計算週增量」成功完成、沒有拋出遺漏天數的錯誤；③整條流程 27 個步驟全部成功，算出的各通路週增量都是合理的非零值(可回推核對：其中一個通路光後四天就有 17，加上跨月三天後總數達到 29，合理)

- Fixed｜「📖 說明」面板裡，同一條流程裡好幾個完全不同的自訂步驟(custom-code)顯示出一模一樣的說明文字，看不出彼此差在哪｜延續上一則修復按鈕可見性之後，實際用使用者一條真實的 30 步流程檢查說明面板內容，發現「判斷這次要更新哪一週」「開啟PPTX簡報並更新圖表」「執行週期搬移歸檔並寫入新頭」等十幾個做完全不同事情的自訂步驟，說明文字全部都是同一句「整理這次需要的日期與期間」——這正是使用者原話「看說明都還是不能確認是哪個」的真因。根因：`explain.ts` 對每個自訂步驟都呼叫 `nodeSummary()` 把 intent 粗略分成 4 類關鍵字(加總/平均/日期/分類)，intent 裡只要提到「日期」兩個字就全部塌進同一句，不管這個步驟實際的主要工作是什麼；而 intent 本身雖然具體，卻是給產碼器看的技術規格(欄位代號、儲存格範圍、演算法細節)，就算過白話轉換也會變成一句夾雜錯誤資訊的怪句子(實測：把格式碼片段「YYYY」「MM」誤判成上游資料欄位)，不能直接顯示。改成優先使用節點自己的 `label`——建圖當下 AI 已經幫每個自訂步驟取過具體、白話、逐步不同的業務名稱(「判斷這次要更新哪一週」本身就是 label，不是我新寫的)，這比用意圖文字重新用關鍵字猜分類可靠得多；只有還沒被取過名字(仍是節點型別的通用預設標籤「自訂步驟(AI 寫)」)才退回原本的關鍵字分類當保底｜`lib/workflow/explain.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1092 個測試全過，新增 2 個測試涵蓋「不同步驟即使 intent 都提到日期，說明文字也要各自不同」「還沒取過名字的步驟正確退回關鍵字分類」，另更新 1 個既有測試反映新行為)、`npm run check:change-guard` 全過；額外對使用者這條真實流程重新截圖說明面板，確認原本全部相同的十幾句說明現在各自顯示對應步驟真正的業務目的

### Added

- Added｜某條真實流程新增功能：把日報 Excel 裡一份通路彙總分頁的 A3:G16 範圍截圖，貼到簡報「開戶數」那一頁，並把簡報右下角的「資料日期」文字方塊同步更新成正確的資料日｜使用者原話：「在下載下來的...找到這個sheet，然後把A3:G16截圖下來，貼到簡報中...簡報中右下角的資料日期也要更新」。這條流程本來沒有這個功能；結構相似的另一條姊妹流程已經有結構完全一樣的「截圖→換圖」步驟且已在正式環境驗證過，直接沿用同一組設定(同一支「換簡報圖片」Apps Script 網址可以共用，不用另外部署)，只新增一個自訂步驟負責更新資料日期文字。新增的日期更新步驟第一版用「頁面文字含『開戶數』」找頁，實測真實簡報裡這個詞同時出現在 3 個頁面(標題頁一頁、內文提到兩頁)，會誤判成分不清楚要改哪一頁而正確地停下來——改用跟「換圖」那支 Apps Script 完全一樣的兩層判斷(先找『某個文字方塊內容剛好等於這個詞』的頁面，只有標題頁會命中；找不到才退回『頁面上任何地方含這個詞』)，複用同一支已經實測驗證過的邏輯，不用自己重新摸索｜新增節點：`把通路結算表畫成圖片`(excel-range-image)、`換掉開戶數頁的表格圖`(google-slides-replace-image)、`更新開戶數頁資料日期`(custom-code，經 `addNodes`/`addEdges` 安全路徑加入)｜驗證：對正式環境完整執行這條真實流程，確認「換掉開戶數頁的表格圖」與「更新開戶數頁資料日期」都成功執行(整條流程 27 步全部成功)，且正確地在真實運行中一度因為「開戶數」出現在 3 個頁面而正確攔下、改用兩層判斷後正確唯一定位到目標頁

- Added｜首頁搜尋框現在也搜得到「流程裡面某一步做了什麼」，不再只能搜名稱/短說明｜使用者原話：「我要找到我記得在簡報的工作流上次有新增一個動作，但是我忘記是在哪一個了，可是我看說明都還是不能確認是哪個」——短說明本來就是整條流程的摘要，一條流程動輒 20~30 個步驟，不可能靠一句摘要涵蓋每一步做過什麼。`/api/workflows` 清單多回傳每條流程的 `stepLabels`(所有非觸發步驟的名稱，不含程式碼/帳密等內部細節)，首頁搜尋在名稱/短說明都沒命中時，退而比對這份步驟名稱清單；命中時清單上不顯示原本的短說明，改顯示「🔎 符合步驟：「實際那一步的名稱」」，讓使用者一眼看出是哪一步、為什麼這條流程會出現在搜尋結果裡｜`app/api/workflows/route.ts`、`app/page.tsx`｜驗證：`npx tsc --noEmit`、`npm test`(1088 個測試全過)、`npm run check:change-guard` 全過；額外對正式環境實際搜尋一個只存在於某條流程內部步驟名稱、完全不在該流程名稱或短說明裡的關鍵字，確認真的能找到那條流程並正確顯示是哪一步符合

- Added｜流程短說明(首頁清單直接顯示的那一句)現在可以在建立後修改，不再只能靠 AI 建圖當下自動寫死｜使用者原話：「現在那些工作流沒有很完整的敘述，這樣有些功能我會不確定有沒有做」。查證：`description` 欄位只有 AI 建圖當下會自動產生，之後不管是流程設定 PATCH API 或畫面上都沒有任何管道能修改它；好幾條流程是用「複製」既有流程建立的，`description` 也整句原封不動複製過去，完全沒講是哪個對象/哪個資料來源，掃過清單完全分不出哪條是哪條(13 條正式流程裡有 8 條共用同一句一模一樣的說明、3 條完全空白)。`longDescription`(給 📖 說明面板用的長說明)本來就能改，`description`(首頁清單用的短說明)不行——加上這個欄位的 PATCH 支援，並重新讀每條流程實際的節點內容，把 13 條正式流程的短說明都改成明確講出資料來源、處理內容、輸出結果的版本(過程中額外確認到：有一組結構相同、只換資料來源的流程，雖然共用步驟的節點名稱沒有跟著每一份複製更新，但實際處理的資料欄位各不相同、彼此商業邏輯是對的，只是命名沒跟著改，不是誤用彼此的資料)｜`app/api/workflows/[id]/route.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1088 個測試全過)、`npm run check:change-guard` 全過；額外對正式環境的 13 條流程實際呼叫 API 逐一更新，並用 Playwright 截圖確認首頁清單真的顯示出新的說明文字

- Added｜「產出檔案」頁新增「選取多筆」批次刪除，不用再一筆一筆刪、一次一次跳確認視窗｜使用者原話：「產出檔案那邊讓我可以選起來刪不然刪好久」——原本每個檔案獨立一顆「刪除」鈕，每按一次都跳一次瀏覽器原生 `confirm()`，累積幾十個測試/舊產出檔案要刪幾十次。新增 `DELETE /api/files`(帶 `{ids: number[]}`，一次最多 500 個，其中幾筆刪除當下已經不存在也不擋其他筆，回傳實際刪了幾個、跳過幾個)；畫面加「☑ 選取多筆」切換成選取模式，每筆前面出現勾選框、點整列也能選取，配合「全選/取消全選」與「刪除選取的 N 個」，只跳一次確認視窗｜`app/api/files/route.ts`、`app/files/page.tsx`｜驗證：`npx tsc --noEmit`、`npm test`(1088 個測試全過)、`npm run check:change-guard` 全過；額外用 Playwright 對正式環境實際操作一次「全選→確認數字對→取消全選→勾選 8 筆→刪除」，並確認畫面上少了 8 筆、對應的檔案真的從磁碟跟資料庫都清掉了(順便拿今天測試累積的真實測試檔案清乾淨，不留給使用者自己抓)

- Added｜用白話叫平台「跑一次某條既有流程」時，AI 現在真的看得到那條流程實際的內容與輸出欄位｜使用者想合併兩條既有流程的結果來寄信，追問「我跟他說要去跑某一個流程，他有環境看到那個流程的內容嗎？」——查證後確實沒有：`builder.ts` 建圖時完全沒有查閱其他既有流程，只會把使用者打的名字原封不動塞進「執行子流程」(`run-workflow`)節點的設定，那條流程實際的步驟、最後會產出哪些欄位，AI 建圖當下一律用猜的，猜錯了要等使用者測試失敗後再靠「讓 AI 修」校正。新增 `existingWorkflowRefsSection`：對使用者這則訊息的文字做子字串比對，抓出訊息裡完整提到的既有流程名稱(不做模糊比對，抓不到就照舊行為，不會誤把不相干的流程當參考)，把它的步驟清單、以及(如果真的執行成功過)最近一次成功執行**真正輸出**的欄位一起餵給模型——沒執行過的老實說「不知道」，不會假裝有答案讓模型憑空編欄位名稱｜`lib/workflow/existingWorkflowRefs.ts`、`lib/workflow/builder.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1059 個測試全過，其中 4 個新測試涵蓋「沒提到既有流程回空」「沒跑過要老實說不知道」「真的跑過要帶出真實欄位不是編的」「排除自己」)、`npm run check:change-guard` 全過(過程中隱私掃描抓到我自己在程式碼註解裡誤寫了使用者真實的流程名稱當範例，已經改成通用範例修正)

- Added｜節點的快速編輯區把 CSS 選擇器這類網頁技術碼收進預設收合的「進階設定」，不再跟一般欄位混在一起｜使用者原話：「他還是有很多一般使用者看不懂的設定方式」，實測打開「找信件」節點，「搜尋框選擇器」`input[type="search"]...`、「信件標題欄選擇器」`td.ML_Subject` 直接跟「標題關鍵字」這種一看就懂的欄位並排顯示，非技術使用者完全無法分辨哪些是自己該填的、哪些是內部技術細節、壞了要靠「讓 AI 修」處理。`ParamField` 新增 `advanced` 旗標，標在「找信件」(搜尋框/信件標題欄選擇器)、「登入網站」(帳號/密碼/驗證碼圖片/驗證碼輸入/登入按鈕/登入成功後應消失的選擇器)、「下載附件」(下載連結/附件區塊選擇器)這幾個節點目前唯一含原生 CSS 選擇器的欄位上；`NodePanel.tsx` 的「✏️ 直接改設定」把標了 `advanced` 的欄位收進一個預設收合的 `<details>`，一般欄位維持原樣直接顯示｜`lib/workflow/types.ts`、`lib/workflow/nodes/findEmail.ts`、`lib/workflow/nodes/browserLogin.ts`、`lib/workflow/nodes/downloadAttachment.ts`、`app/workflows/[id]/NodePanel.tsx`、`app/workflows/[id]/AddNodePanel.tsx`｜驗證：`npx tsc --noEmit`、`npm test`(1087 個測試全過，新增 1 個測試鎖住「找信件」的兩個選擇器欄位標了 advanced、一般欄位沒有被誤標)、`npm run check:change-guard` 全過

- Added｜執行前可以勾選「這次寄出的信先都寄給我自己」，真的跑到底、真的算出內容，但不會驚動正式收件人｜使用者原話：「我也不知道如何測試寄一份真的跑完流程並計算出來的信給我本人」——查證「寄信 mail2000」節點當下的收件人欄位完全是空的，畫面上也沒有任何警示，唯一能看到「真的算出來的信長什麼樣」的辦法是手動把收件人改成自己、跑完再改回來，改壞了就會誤寄給正式收件人。新增 `runs.test_send_override`：`ExecutionPlanPanel`(執行前計畫)在有寄信步驟時多一個勾選+信箱輸入框(記在瀏覽器本機，下次不用重打)，勾了就把信箱透過 `/api/workflows/[id]/run` 傳給 `startWorkflowRun`，整段只存在這次執行的佇列項目與 `runs` 資料表(供程式重啟續跑時還原)，**完全不動存檔的節點設定**，執行完也不用還原任何東西。`webmail-send` 節點的 `execute()` 看到 `ctx.testSendOverride` 就把收件人／副本／密件副本换成它(副本密件副本清空——測試不該連帶驚動被 cc 的人)，主旨加註記原收件人是誰，讓使用者收到測試信時一眼看得出這封信原本要寄給誰｜`lib/db.ts`、`lib/workflow/types.ts`、`lib/workflow/engine.ts`、`lib/workflow/nodes/webmailSend.ts`、`app/api/workflows/[id]/run/route.ts`、`app/workflows/[id]/ExecutionPlanPanel.tsx`、`app/workflows/[id]/page.tsx`｜驗證：`npx tsc --noEmit`、`npm test`(1087 個測試全過，新增 2 個測試直接呼叫真正的 `execute()`(`dryRun:true`，不用開瀏覽器)，證明收件人真的被換掉、副本真的清空、沒勾的時候完全不受影響)、`npm run check:change-guard` 全過；範圍刻意只做 email(webmail-send)，Telegram/LINE 通知還沒接這個開關，畫面上只在真的有寄信步驟時才顯示，避免勾了卻什麼都沒發生

- Added｜對話裡明確要求「把既有流程的步驟複製過來」時，直接原封不動搬節點過來，不再需要使用者手動匯出貼 JSON｜使用者想把兩條既有流程合併成一條，希望「先跑A、完成後接著跑B」，但不想用「執行子流程」呼叫(擔心依賴既有流程、以後兩邊互相牽動)，而是要「複製貼進來，變成同一條新流程裡的真實節點，我自己在畫布上串接或拆開」。既有的 `existingWorkflowRefsSection` 只給 AI 一份摘要(步驟型別+標籤、輸出欄位樣本)，不含選擇器/程式碼這些真正能跑的內容，AI 只能請使用者去來源流程畫布右上角匯出 JSON 貼過來，再由 AI 讀著 JSON 用文字重新生成一份「看起來一樣」的節點——重新生成就有抄錯選擇器/漏改程式碼的風險，使用者也無從察覺。新增 `matchExistingWorkflows`(從 `existingWorkflowRefsSection` 抽出的確定性比對，兩處共用)+ `importExistingWorkflowNodes.ts`：使用者訊息裡完整出現既有流程名稱、且帶有「複製/匯入/搬/貼/拿過來」這類意圖詞時，`buildWorkflow` 完全不呼叫模型，直接從磁碟原始資料複製節點物件(只改 id 避免撞名、只改座標避免疊在一起)，回傳 `phase:"ready"` 走使用者已經熟悉的「套用到畫布」/「捨棄」確認流程。不複製來源流程自己的觸發節點(一條流程只能有一個)；每組匯入節點裡「組內沒有上游」的節點會先接到目標流程現有的觸發節點，讓圖是可以直接套用的合法狀態(graphLint 的可達性檢查會擋下斷開的孤兒節點)，使用者套用後可以自己刪掉這條起始線改接到別的地方｜`lib/workflow/existingWorkflowRefs.ts`、`lib/workflow/importExistingWorkflowNodes.ts`(新增)、`lib/workflow/builder.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1079 個測試全過，新增 7 個測試，其中 3 個直接把複製完的圖餵給 `lintGraph`，確認過得了「套用到畫布」實際會做的檢查；1 個用真實 `buildWorkflow` + 會丟例外的假模型 client 證明真的是 0 次呼叫模型)、`npm run check:change-guard` 全過(過程中隱私掃描抓到測試裡用了使用者真實的流程名稱當範例，已經改成通用範例修正)

### Fixed

- Fixed｜「執行子流程」在依序模式(maxConcurrent=1)下會死結，逼使用者把全站設定改成併行才能用｜使用者想合併兩條都需要登入 Mail2000 的流程結果來寄信，查現有的「執行子流程」(`run-workflow`)積木發現它有一個前提：必須把「排程 & 執行」頁的「同時觸發時怎麼跑」改成併行才能用，錯誤訊息也明講「依序模式下母流程會佔住唯一名額，子流程永遠排不進去」。使用者追問「為什麼要並行？可以輪流啊」——查證後這個追問是對的：母流程呼叫子流程時是**整個停下來等**(runWorkflowAndWait)，母子兩者本來就是輪流、不是同時做事，但舊版 `processQueue()` 用同一個 `activeCount` 算「同時執行中」名額，母流程自己傻等就已經佔滿依序模式唯一的名額，這是排程機制的簡化寫法造成的死結，不是真的需要並行。而「改成併行」的副作用是**全站**其他互不相干、真的需要嚴格輪流的排程(例如好幾條在同一個季度時間點觸發的 Mail2000 流程)也會一起被放行併行執行，重新帶回剛修好的共用登入互踢風險。修法：新增 `nestedSubWorkflowCall` 標記，讓「執行子流程」呼叫出來的子流程不佔用併行名額(只受「同一條流程不能同時兩個 run」與排隊總量上限約束)，依序模式下也能正常輪流跑完，不用再改動全站設定；拿掉了 `run-workflow` 節點裡「請改成併行再重試」的錯誤訊息(不再需要)｜`lib/workflow/engine.ts`、`lib/workflow/nodes/subWorkflow.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1055 個測試全過，新增的兩個測試用真實引擎不 mock 跑一次，其中一個先用真實情境重現死結——刻意暫時還原成舊寫法重跑，實測 20 秒內沒有跑完被判定 hang 住，證實測試真的會抓到這個退化，再復原成正確版本重跑 2.6 秒內完成)、`npm run check:change-guard` 全過

### Changed

- Changed｜「排程 & 執行」頁把執行中跟已暫停的排程分開兩區顯示｜使用者原話：「要把有在執行的和暫停的分開擺放」——原本所有排程混在同一份清單裡依「下次執行時間」排序，暫停的排程只靠卡片上一顆小小的「已暫停」badge 分辨，夾在還在跑的中間，掃過去容易誤以為某條也還在自動執行。改成「▶ 執行中（N）」「⏸ 已暫停（N）」兩個獨立分組，拖曳排序邏輯不變(仍操作同一份排程陣列，只是依 enabled 分組渲染)。順手抓到並修掉一個因為這次重新分組才變明顯的舊 bug：資料庫存的 `enabled` 其實是 0/1 的數字不是真正的 boolean，`s.enabled && X` 這種寫法在 enabled=0 時，JS 的 `&&` 短路結果是數字 `0` 不是 `false`，React 會把它當成合法內容直接印出來，已暫停的排程卡片上因此一直混著兩個裸的「0」字元(截圖比對前後畫面才發現)——先轉成真正的 boolean 再判斷｜`app/schedules/page.tsx`｜驗證：`npx tsc --noEmit`、`npm test`(1088 個測試全過)、`npm run check:change-guard` 全過；Playwright 對正式環境實際截圖比對修前(卡片上有裸的「0」)與修後(乾淨)兩張圖，確認畫面真的修好，不是只看程式碼推論

- Changed｜全站 UI/UX 大盤點與整改（3 支子代理跑完設定頁/文案、非流程頁、流程編輯頁三份完整盤點後動手）｜使用者原話：「除了我跟你做出來所以了解的以外，其他人看到整個平台就是很雜亂難理解，資訊太多了，沒有給在正確的地方，有些 ui/ux 也不夠直覺」「我想要使用者容易知道怎麼做，使用者體驗要非常好」。盤點量出：同一件事最多 8 種叫法、Google 授權兩條完全不同的教學路徑、設定頁首屏 20 個空欄位卻沒講「都不用填」、歡迎導覽 0 次提到設定頁但 15+ 條錯誤訊息把人導去那裡。**不刪任何功能**，只動位置、說明與重複路徑：
  - **修掉 5 個上一輪重排造成的事實錯誤**：節點缺帳密的提示還說「設定頁最下面」，但通知串接已經搬到最上面；換圖節點的錯誤訊息指向已經不存在的舊入口；OAuth 錯誤把程式碼變數名（`googleOAuthClientId`）當欄位名丟給使用者看，畫面上實際叫「用戶端 ID」。另外修一個真的會讓面板互相打架的 bug：流程頁工具列 4 顆按鈕清空右側面板狀態的方式互不一致，關掉一個面板會冒出使用者沒點過的舊面板——改成單一 `activePanel` 狀態，這一整類 bug 從設計上消失，不用逐一補漏
  - **合併重複路徑**：Google 授權原本有兩條教學（設定頁一鍵授權 4 步 vs 流程頁 OAuth Playground 手動換 token 8 步），兩者其實是同一組三個 secret，改成只教一鍵那條，手動貼值的表單保留當作「已經有值」的備援，不再教怎麼去 Playground 換。Apps Script 部署教學原本三處各寫一份（其中節點面板那份漏了「別直接開 script.google.com」的警告——這是真的會踩到的雷），抽成一個共用元件，往後只改一個地方。排程頁的「所有排程」跟「一鍵執行」原本各自列一次同一批正式流程，合成一份清單，沒排程的流程顯示「未設定排程」+「加排程」
  - **設定頁加一句「這一頁全部都是選填」**：AI 模型已經預設接免費服務，其餘每一項都是「用到哪個服務才設定哪個」，原本完全沒有地方講這件事
  - **草稿可見性**：「設為正式」以前只藏在流程頁 ⋯ 選單，草稿頁本來就會看，現在卡片上直接有按鈕；首頁有草稿時多一條平靜的提醒（跟真正的告警分開，語氣不同）
  - **首頁 4 張告警卡（上線檢查/簽核/AI修復提案/排程失敗）收成一條「有 N 件事要你處理」橫幅**，展開才看細節，內容與互動邏輯不變；真的緊急（健康檢查沒過）時第一次載入就自動展開
  - **統一全站用詞**：8 種「只讀測試」講法收成「演練」、4+ 種節點單位講法（積木/節點/一格）收成「步驟」、6 種「填帳密」對話卡標題收成 2 種、AddNodePanel 跟畫布卡片副標對同一個節點各用不同名字的 12 處全部同步（新增機械化測試鎖住，往後漂移會直接讓測試失敗）
  - **可靠性頁改名「平台健康度」**、三個問題區塊從倒序 ③②① 改回正序 ①②③、版面跟其他頁一樣置中、載入失敗補上重試按鈕；側欄名稱全面跟頁面標題同步（原本「Workflows」點進去卻是「我的流程」）
  - **手動登入改用平台自己的小對話框**（Google／Microsoft 按鈕＋自訂網址欄），不再是瀏覽器原生 `window.prompt()`
  - **執行前計畫畫面補上「只演練，不真的寫入/發送」勾選**——以前這個選項只有部分執行（框選幾步）才有，完整執行永遠是真的執行到底
  - **產出檔案頁補上是哪條流程產的**（可篩選），以前只有檔名，多條流程都輸出報表時只能用檔名自己猜
  - **對話送出鍵改成 Enter**（Shift+Enter 換行，⌘+Enter 仍可用）；4 顆純圖示工具列按鈕補上文字；首頁統計卡、5 個空狀態頁面補上可點的動作按鈕（原本只有 1 個空狀態有按鈕）；執行紀錄頁的空狀態拆成「真的沒紀錄」跟「篩選後沒結果」兩種文案
  - **~10 條使用者一定會看到的錯誤訊息改成白話在前、技術細節在後**：自訂程式碼失敗、Google 試算表 Apps Script 執行錯誤、LINE 群組 ID 查詢、內網網址被擋等，原本都是技術原文開頭（JS 錯誤、DOM 傾印、環境變數指令），現在先講「發生什麼事、下一步做什麼」，原文收進句尾當附註

- Changed｜介面依「使用者怎麼找得到」重整：⋯ 選單從 11 項瘦到 6 項、設定頁分成四段、首頁直接標出哪條流程沒設定完｜使用者原話：「東西有點太多了，我自己都不知道功用是什麼」「我想要使用者容易知道怎麼做，使用者體驗要非常好」。**一項功能都沒有刪**（刪不刪是產品決定），只動位置與說明：
  - **新增「⚙ 這條流程的設定」單一入口**：把原本平鋪在 ⋯ 裡的四項（換簡報圖片／產出檔案放哪／手動登入／錄一段操作）＋模型選擇收成一頁，**每一項都直接顯示目前狀態**（已設定好／沿用全域資料夾／用哪個 AI），不用點進去才知道。原本那個選單同時混了狀態變更、畫布操作、一次性設定、檔案操作四種性質，使用者得先認出「哪幾項是設定」——那正是他找不到換簡報圖片的結構性原因
  - **首頁列表標出「⚠️ 差 N 步沒設定」**：清單 API 直接算 `setupNeedsFor`。17 條流程原本要一條一條點進去才知道哪條會失敗
  - **設定頁重排成 ①連線與帳號 ②AI 模型與行為 ③產出的檔案 ④進階（摺疊）**：產出資料夾從最底部移到第三段（它常用），保留期限與稽核紀錄收進摺疊的「進階」（保留、不刪）。另外把**模型設定三層的關係寫出來**（模型 API → 模型來源 → 單條流程覆寫，且「內建」那組就是第一層），這三者原本看不出關係；「已儲存帳密管理」也補上一句它跟「共用帳密」是同一份資料的兩個視角
  - **「我的步驟」補上刪除**：DELETE API 早就有、UI 沒有，錄壞的步驟只能一直躺在清單裡。確認訊息明說「已經加進流程裡的那些不受影響」，否則使用者不敢按
  - **徽章文案改成使用者的話**，並讓沒作用的徽章消失：「已驗證版本／內容指紋」→「已用真實檔案驗過」；n8n 映射徽章**確認完就不再常駐**（原本永遠顯示 ✓ 卻點不出任何待辦）
  - **n8n 轉換收進「⬇ 匯入…」**：它是一次性的搬家工具，原本跟「建立新流程」平起平坐

### Fixed

- Fixed｜換簡報圖片實測三個真 bug，現在端到端通了｜使用者設定完後實測，連續踩到三個只有真的打 Google 才會發現的問題：①**方法名錯**——腳本寫 `images[0].replaceImage(blob)`，但那是 Slides **REST API** 的請求名稱，Apps Script 的 Image 服務叫 `replace()`。**單元測試的假環境當時也照著同一個誤解寫 `replaceImage`，所以 12 個測試全綠、真的跑才爆**——假環境要照真實 API 的形狀寫，否則只是在驗證自己的誤解。②**手寫的二進位是壞的**——範本裡內嵌了一段我自己拼的 base64 佔位 PNG，CRC 全錯，Google 回「您嘗試使用的圖片無效或已損毀」。改成由 agent-hub 用 sharp 產一張真的送過去，範本裡不再有任何手寫二進位。③**每測一次就在使用者的雲端硬碟多一份測試簡報**，而平台的權限刪不掉(那份是腳本以使用者本人身分建的，`drive.file` 涵蓋不到)——改成記住上次那份、下次沿用同一份，最多只會有一份｜驗證：**真的打 Google 完成整條路**，並用 Slides API 抓回那一頁的縮圖確認灰色佔位圖真的變成表格；連跑兩次確認沿用同一份簡報沒有再多建


- Fixed｜自動部署出來的腳本回 403「存取遭拒」，而訊息卻叫使用者去改部署設定（改了也沒用）｜實測：自動部署成功、`/exec` 網址拿到了，但打過去一律 403。查了那個部署的實際設定——`access: ANYONE_ANONYMOUS`、`executeAs: USER_DEPLOYING`，**跟手動部署選的完全一樣，所以不是設定錯**。真正的原因是**這個腳本從來沒有被使用者本人授權過**：手動部署時中間那個「Google 尚未驗證這個應用程式 →允許」畫面就是在做授權，而用 API 建的腳本完全沒經過那一步，沒授權的網頁應用程式對外一律 403。修法：①範本加一個 `doGet`，使用者用自己的瀏覽器打開 `/exec` 一次就會跳授權、按允許後看到「已就緒」字樣（順便當成肉眼可見的連通檢查）②自我測試遇到 403／存取遭拒時改回這個專屬訊息並附上可直接點的網址，不要再跟「部署權限沒選任何人」混成同一句話——**上一版就是混在一起，會把人導去改一個本來就正確的設定**｜驗證：查詢真實部署的 entryPointConfig 確認設定無誤（排除掉「設定錯」這個假設），重新部署後 403 訊息已正確變成「需要授權」並帶連結


- Fixed｜「你還沒打開 Apps Script API」——但使用者明明打開了｜實測截圖：他照訊息去 script.google.com 把開關打開，再按一次還是同一句話，等於卡死。根因是**「Apps Script API 沒開」其實有兩個完全不同的東西**，而我的判斷式把它們混在一起：①帳號層的個人開關（script.google.com/home/usersettings，訊息是 `User has not enabled the Apps Script API`）；②**Cloud 專案層的 API 啟用**（訊息是 `Apps Script API has not been used in project 12345 before or it is disabled`）。真正擋住他的是第二種，而我的 regex `Apps Script API.*(not enabled|disabled)` 會誤命中它、然後指去第一種的畫面。**指錯地方比不指還糟**——照著做卻沒有用，使用者會以為是產品壞了。修法：兩種分開判斷；第二種一律把 **Google 自己在錯誤訊息裡附的啟用網址**原樣交給使用者（那串網址帶著他的專案編號，我們自己組不出來），並在畫面上做成一顆可以直接點的按鈕；另外一律附上 Google 的原始訊息（摺疊）——判斷式再分類錯時，那是唯一能對照的東西｜`friendlyApiError` 改回傳 `{message, actionUrl, actionLabel, raw}`｜驗證：用真實帳號重現，正確辨識成 Cloud 專案那種並取出含專案編號的網址；2 個測試分別盯住兩種不可混淆

- Fixed｜設定卡把「自動」當主打、「手動」收進摺疊區——**對新手是反的**｜使用者原話：「你要有辦法讓第一次或是剛開始用的人都看得懂要怎麼做！」。自動那條需要 Google Cloud 專案 + OAuth 用戶端 + 登記重新導向網址 + 重新授權 + 開兩個 API 開關，五個前置條件全是新手沒看過的東西；**手動那條一個都不用**，只要有 Google 帳號。現在依「這台機器有沒有設定好 Google 授權」決定誰排前面：沒有 → 手動當主要路徑（標題直接寫「不用申請任何東西」）；有 → 自動當主要路徑。手動步驟也照新手重寫：每一步講清楚會看到什麼畫面、要點哪幾個字，並**預先安撫 Google 那個「尚未驗證這個應用程式」的嚇人畫面**（新手幾乎都停在那裡）｜驗證：瀏覽器實測兩種狀態的畫面

### Added

- Added｜自我測試通過後，網址自動填進流程的「換掉簡報上的圖片」那一步｜原本畫面上寫「我會自動填進去」但程式碼根本沒做——那是這個 repo 最不能接受的那種假承諾。現在真的做了，而且**只有在自我測試通過之後才填**（沒驗證就先填等於把一個不確定會動的設定塞進正式流程）。寫入走 `nodeConfig` 只改那一個欄位，不整包送 nodes（AGENTS.md 鐵則 1）


- Fixed｜按「前往 Google 授權」直接被 Google 擋下來（redirect_uri_mismatch），而畫面上完全沒提到要先做什麼｜使用者實測截圖：`Access blocked: This app's request is invalid / Error 400: redirect_uri_mismatch`。根因是**兩條授權路徑用不同的重新導向網址**：既有的 Google 憑證是照「OAuth Playground」那條設定流程建的，登記的是 `https://developers.google.com/oauthplayground`；新加的一鍵授權是由 Agent Hub 自己接收結果（`http://127.0.0.1:3000/api/oauth/google/callback`），那一行憑證裡沒有，所以必被擋。**設定頁的 Google 卡片其實早就把這行網址連同複製按鈕做好了，是我新做的卡片沒有把它帶過來**——功能存在但不在使用者當下的動線上，等於不存在（同一類問題的第四次）。現在第 1 步直接顯示那一行 + 複製按鈕 + 「到憑證頁 → 點你的 OAuth 用戶端 → 已授權的重新導向 URI → 新增 → 儲存」的逐步指路，並說明為什麼會多這一步（原本那行不用刪，兩行可以並存）；按鈕下方另外寫明「出現 redirect_uri_mismatch = 上面那行還沒加或還沒存」｜`SlidesImageScriptCard` 改讀 `/api/oauth/google/status` 的 `redirectUri`

### Added

- Added｜流程頁一進去就直接告訴你「這條流程還有幾步沒設定好」，並附上一顆直接把設定畫面打開的按鈕｜使用者原話：「我看不懂要去哪裡操作，這也會是大家會遇到的問題」。原本的做法是「套用流程圖的當下，在對話裡推一張設定卡」——那對**新建**的流程有效，對**已經存在**的流程等於永遠不會出現(沒有人會再套用一次)，而且對話訊息本來就會被後續對話往上推走。結果功能就藏在「⋯ 選單」的第七個項目裡，使用者根本不知道要去哪。**這個 repo 為了同一件事踩過三次**(排程暫停只有一個表情符號、模型來源卡在頁面 87% 深度、這次)，教訓每次都一樣：功能找不到就等於不存在。改成一支純函式 `setupNeedsFor(nodes)` 當「這條流程還差什麼」的單一答案，流程頁一載入就顯示橫幅；日後新增任何「需要使用者親手設定一次」的節點型別，加進那份清單就會自動出現，不用再各自發明入口｜`lib/workflow/setupNeeds.ts`｜驗證：6 個測試(沒填要列出、填好要消失、只有空白也算沒填、兩種都缺要都列、每一項都要有「為什麼」跟按鈕文字)

- Added｜換簡報圖片的設定卡改寫成「編號三步、每步都有一顆真的能點的東西」｜同一個回饋的另一半：光用文字描述位置(「到 Apps Script 設定頁把某個開關打開」)對沒碰過的人等於沒說。現在第 1 步授權、第 2 步開 API 開關、第 3 步自動部署各自獨立，第 1 步沒做完第 3 步就是暗的並寫明「先完成第 1 步，這顆才會亮起來」；第 2 步直接給可點開的連結並先講清楚「你會看到一個叫 Google Apps Script API 的開關，切成開啟就好，不用改別的」。**`.btn` 這個類別本身沒有邊框與底色**，單獨用會讓連結看起來像純文字——那正是「看不懂要去哪裡點」的成因，所有可點的東西一律要看起來可點


- Added｜「幫我自動建好並部署」：平台直接用 Apps Script API 幫使用者建專案、貼程式碼、部署成網頁應用程式，不用再手動複製貼上＋走一次部署精靈｜使用者原話：「上次不是做什麼設定說以後就不用自己手動換 app scripts 嗎？不能讓他直接幫我嗎」——**他記得沒錯，而且我該講清楚**：當初索取 `script.projects`／`script.deployments` 兩個權限時，註解裡就寫了目的是「讓平台之後能自己更新/重新部署那份指令碼」，但**實作從來沒補上**，整個 repo 只有那兩行要權限的程式碼，沒有任何一支真的去打 Apps Script API。等於權限要了卻沒用。這次把它補完。**已經部署過就更新同一個部署**(不是每次建新的)——每建一次新部署就多一個 /exec 網址，使用者流程裡填的舊網址會安靜地停在舊版程式碼上，那種問題他不可能自己發現。兩件平台做不到、只有使用者本人能做的事(Apps Script API 總開關、同意新權限範圍)各有專屬的錯誤訊息直接指路｜`lib/appsScriptDeploy.ts`、`app/api/slides-image-script/deploy`、卡片改成「自動為主、手動收進摺疊區當後備」｜驗證：5 個測試(清單檔一定要有 webapp 區段否則拿不到 /exec、四步呼叫順序、已部署時走更新不建新的、拿不到網址要指路到手動)；實測對真實帳號查權限狀態，正確列出缺少的正是 script.projects/script.deployments 兩項(presentations 已有的不會誤報)


- Added｜換簡報圖片可以「按一顆按鈕真的做一次給你看」，示範用的是用完即棄的測試簡報｜使用者原話：「因為簡報上的那張圖本來就是新的了，所以我要怎麼確定你是會做的？」——這個問題**沒辦法用程式碼審查或單元測試回答**，因為那些都沒有真的碰過 Google；但拿正式簡報示範又有風險。所以腳本多一個 `selfTest` 動作：自己開一份新簡報 → 放一張灰色佔位圖 → 用**完全相同的換圖程式碼**把它換成表格圖；agent-hub 再用 Slides API 把那一頁抓成縮圖顯示在卡片上。看得到圖 = 這條路真的通了，而正式簡報一個字都沒動｜`selfTest`、`app/api/slides-image-script/self-test`、`lib/slidesSelfTestImage.ts`｜驗證：假 Apps Script 環境測「自我測試會另外開一份新簡報、原本那份完全沒被碰過」＋測試圖本身有測(必須是合法 PNG、尺寸合理、不是一張全白圖——否則整個自我測試會退化成「系統說成功了」)

### Fixed

- Fixed｜貼進簡報的表格圖會被橫向拉伸｜實測使用者的簡報：圖框是 2.541:1，平台產生的表格圖是 2.483:1，直接把圖設成框的寬高會讓整張表橫向拉伸約 2%(字會變胖)。反過來「以寬度為準保持比例」則會讓圖變高，表格列數一多就會往下撞到「資料日期」那一行。改成**保持比例、縮到完全放得進原框、並在框內置中**：不變形、也永遠不會超出原本的版面。節點會從 PNG 檔頭讀出真實像素尺寸一起送給腳本(讀不到就退回舊行為，寧可維持原樣也不要用猜的比例把表格拉歪)｜`fitInBox`、`pngPixelSize`｜驗證：4 個測試(圖比框瘦/比框寬各一，都斷言貼上去的長寬比跟原圖一致、且不超出原框)


- Added｜「Excel 範圍截圖」節點：把試算表的某個範圍畫成圖片，取代人工「開檔案、框選、截圖、貼進簡報」｜忠實還原 Excel 畫面：合併儲存格(跨出範圍會裁切)、佈景主題色與 tint、**數字格式**(存 0.2697、畫成 26.98%；存 218530.7、畫成 218,531)、欄寬列高、框線樣式與顏色、點→像素的字級換算。錯誤格(#DIV/0!)畫成空白**但一定把是哪幾格記進執行紀錄**——主管簡報上出現 #DIV/0! 很難看，但靜默吞掉錯誤正是這個 repo 反覆踩過的「表面成功」。刻意做成平台節點而不是 custom-code：custom-code 的程式碼會被重新產生，而這種「錯了會直接被主管看到」的視覺產物不該放在會被重產的地方｜`lib/xlsxRangeImage.ts`、`lib/xlsxCellStyle.ts`(跟 `xlsxRender` 共用樣式解讀，避免「AI 看到的樣子」跟「貼進簡報的樣子」漂移)、`lib/workflow/nodes/excelRangeImage.ts`｜驗證：10 個單元測試(數字格式/合併裁切/範圍解析)＋用真實的 14 列 × 7 欄報表在引擎裡實跑產圖，逐格核對

- Added｜「換掉簡報上的圖片」節點 + 一次性部署卡片｜**為什麼不用 Google 簡報 API**：它的換圖只吃公開網址，餵不進圖片內容本身。純 API 的做法一定要「上傳雲端硬碟 → 設成任何人可讀 → 換圖 → 刪掉」，中間那幾秒公司資料是公開的，而且企業版 Workspace 常常直接禁止對外分享(那條路會失敗)。Apps Script 的 `replaceImage(blob)` 可以直接吃圖片內容，全程不需要公開連結。刻意**不**加進既有的試算表腳本範本——Apps Script 依程式碼推導授權範圍，加進去會害每個只想寫試算表的使用者都被迫授權「存取你所有的簡報」。找頁面是**先找標題方塊剛好等於、找不到才退回頁面含有**(實測：一份 50 頁週報裡同一個詞出現在 4 頁，只用「包含」永遠模棱兩可)，且該頁必須剛好一張圖，否則拒絕不猜。腳本網址是 anyone-can-access，所以強制帶平台產生的驗證碼｜`lib/googleSlidesImageScriptTemplate.ts`、`lib/workflow/nodes/googleSlidesReplaceImage.ts`、`app/api/slides-image-script`、流程頁「⋯ → 讓流程能換簡報上的圖片」｜驗證：8 個測試**直接把產生出來的腳本原文載進假的 Apps Script 環境執行**(測的是真的要部署出去的那段程式碼，不是複製品)＋6 個節點測試(參數驗證、只讀試跑一次請求都不送、部署權限沒開的 HTML 回應要翻成看得懂的下一步)

### Fixed

- Fixed｜使用者明確要求的「寄歡迎信」被當成 AI 自作主張，寄信步驟直接被刪掉｜真實回歸(gemma4 全庫實測第 11 案)：需求「填完自動寄歡迎信給對方」，模型正確建出 trigger→送歡迎信→寫進試算表，平台卻把 send-email 刪掉並回一句「🧹 這幾件事你沒有要求…你這次沒有要求寄信或通知」。根因是判斷「使用者有沒有要求寄信」的 regex 只認「寄」**緊接**信／email／郵件／出，「寄歡迎信」中間隔了兩個字就配不到 → `wantsEmail=false` → 需求驗收把它列進「未獲授權的外送」→ autoTrim 自動移除。**這是確定性規則，換任何模型都一樣會被刪**。修法：把寄信說法抽成共用的 `EMAIL_ACTION`(允許「寄」跟「信」之間插修飾語)，**正反兩面共用同一份**——只放寬「想要」不放寬「禁止」的話，「不要寄歡迎信」會被讀成「他要寄歡迎信」，比原本的漏判更危險；同時把 `requirementCheck` 自己留的那份 wantsEmail／wantsNotification 複本刪掉改用共用判斷(同一個判斷放兩份，遲早只修好其中一份——這次就是)｜`lib/workflow/dataChangePolicy.ts`、`lib/workflow/requirementCheck.ts`｜驗證：3+1 個單元測試(寄X信要算要求寄信／否定句同步放寬／不含寄信動作不誤判／該案例的 send-email 不被列為未獲授權)；重跑該回歸案例 26 秒通過

- Fixed｜回歸庫把「同樣正確的另一種做法」判成建錯圖｜第 20 案「PDF 抽表」只認 `pdf-read`，但 `read-file` 的實作真的會呼叫 `extractTextFromFile` 把 PDF 抽成文字(不是只有描述這樣寫)，用它讀 PDF 一樣抽得到內容。這個期待是在 read-file 支援 PDF 之前寫的，已經過時。`expectAny` 改成 `anyOf`(好幾組候選、每組至少中一個)，硬指定其中一種等於把另一種正確答案判成錯｜`scripts/regression.mjs`｜驗證：重跑該案例 65 秒通過

### Added


- Added｜每個模型來源可以自己設定「最多等幾秒」｜實測使用者的地端 gemma4 時發現：預設 90 秒是為雲端 gateway 調的，但地端模型「比較慢但免費無限」——用同一個逾時會把**正在正常產出的長回應切斷**，看起來像「這個模型不會建流程」，其實只是沒等它講完。真實數據：本機 gemma4 建簡單流程 17 秒、建複雜流程(webhook+分類+簽核)會超過 90 秒。表單有「用 300 秒（本機模型建議）」一鍵設定｜`ModelProvider.timeoutMs`、`getClient` 依來源取用｜驗證：把 gemma4 的逾時放寬到 300 秒後，原本失敗的 4 個回歸案例**全部通過**
- Added｜回歸庫可以指定模型，用來評估「某個模型建圖能力如何」｜`AGENT_HUB_REGRESSION_MODEL=xxx node scripts/regression.mjs`。不指定就用平台預設，行為跟以前完全一樣｜這是把「這個模型能不能建流程」從軼事變成數字的工具

### Fixed

- Fixed｜「使用者每次執行時自己貼的文字」被誤判成「沒有真實資料來源」，導致任何模型都建不出流程｜真實踩到：需求「把我給的一段文字裡的所有數字加總」建圖失敗兩輪後放棄。根因是 `requirementCheck` 的 realBusinessData 規則——需求裡有「數字」就觸發，而它認得的「真實來源」只有檔案／試算表／網址／信件這幾種形狀，一個叫「要處理的文字」的執行時輸入欄不算。**這是確定性檢查，所以跟模型聰不聰明無關，換任何模型都一樣建不出來**。修法：不衍生的 text/textarea 觸發參數也算真實來源(使用者自己貼的內容本來就是真實資料)；期間選擇器那種 select 仍然不算——那正是這條規則原本要擋的情境(要做 KPI 簡報卻沒說資料哪來)｜驗證：2 個回歸測試(放行使用者貼的文字、仍擋住沒有來源的 KPI 簡報)，修完後同一句需求 17 秒建出正確的 4 步流程

### Added

- Added｜每個模型都有自己的「🖼️ 測看圖」按鈕，而且看圖能力改成**逐個模型、以實測為準**｜使用者回饋：「我想測試我的 gemma4 能不能看圖片沒地方能測啊」——我把測看圖只做在**新增表單**裡，已經存起來的來源完全沒有入口。而且原本「看不看得懂圖片」是**來源層級**的一個布林值，但同一台機器底下常常同時有看得懂圖跟純文字的模型，來源層級的值一定有一半是錯的。現在每個模型一行、兩顆按鈕(測連線／測看圖)，狀態分成「實測看得懂 / 實測不能讀圖 / 標記為看得懂（未實測）/ 未測」——**沒測過**跟**測過不行**是兩件事，不能都顯示成一樣。平台之後要挑「看得懂圖的模型」時，實測結果優先於任何預設清單｜`modelSupportsVision` 改成實測優先、`getVisionVerified`｜驗證：瀏覽器實測 15 個模型每個都有兩顆按鈕、狀態文字正確

### Fixed

- Fixed｜刪除模型來源的稽核紀錄不足以重建，誤刪就救不回來｜真實事故：我把使用者自己加的模型來源當成測試資料刪掉了，而稽核紀錄只有 `{models:1}`——連模型代號叫什麼都無從得知，備份也早於他新增的時間。稽核軌跡要能回答「刪掉的是什麼」才有意義。現在新增與刪除都會記下 label / baseUrl / 模型清單 / 看圖標記 / 有沒有金鑰(**金鑰本身永遠不記**，稽核不能變成第二個洩漏管道)｜`/api/model-providers` 的 DELETE 先讀後刪
- Fixed｜測試撞到使用者的真實設定｜`modelProviders.test.ts` 原本用 `gemma4` 當測試模型代號，而使用者真的接了一個同名的來源——`resolveModel` 解析到他的那個，測試就紅了，而且紅的原因看起來像程式壞掉。測試跑在**真實的 data/ 上**，測試資料的命名一律改用 `zz-test-*` 這種不可能撞名的字串

### Added

- Added｜加模型來源時可以「先測再存」，而且看圖能力用**實測**而不是打勾｜使用者要求：「填入要可以驗證，也要能驗證是否能看圖」。原本要先存下來才能測(第一次接自己的模型時位址打錯是常態，等於先存一個錯的再回頭改)，而「看不看得懂圖片」更是一個**核取方塊**，我還在旁邊寫「不確定就先不要勾」——那是把一個可以驗證的事實丟回去讓使用者猜。現在表單上兩顆按鈕：①測連線 ②測看不看得懂圖片，通過才存；看圖測試通過會自動勾起來。**看圖測試不是問「你看得到嗎」**——這個專案自己記錄過 `Deepseek-v4-pro` 不會說看不到，而是自信地看圖亂講。所以改成產生一張只有 4 個隨機字元的圖(每次不同、排除 0/O/1/I/L)要它念出來，答對才算數，而且三種失敗要分開講：老實說看不到(純文字模型，正常)、空回應(推理型模型 token 都拿去思考)、**答錯但講得很篤定(最危險，會在驗證碼與截圖判讀上編造答案)**｜`lib/modelVisionProbe.ts`、`/api/model-providers/test`｜驗證：5 個單元測試(含「自信地答錯」必須被標成危險)＋真實打到 gateway 驗證錯誤路徑

### Fixed

- Fixed｜錯誤訊息把「服務太慢」講成「連不到」，會害人去檢查一個沒問題的網址｜實測時 gateway 回 504 過載，而我的訊息卻說「連不到這個服務，請確認網址和連接埠沒打錯」——**位址根本沒問題**。現在分成三種各自給不同下一步：對方回報過載(過一下再試，位址沒問題)／連得上但沒在時間內回應／真的連不到。另外看圖的逾時從 25 秒放寬到 60 秒——傳一張圖上去本來就比問一句話慢，用同一個逾時會把「正常但比較慢」誤判成失敗｜驗證：實測 504 與逾時兩種情況的訊息都改對了

- Fixed｜「模型來源」加了卻找不到——排在整頁 87% 的位置｜使用者回饋：「我沒看到還能填別的模型誒」。實測量出來：那張卡片在 y=5167／整頁 5925，也就是要捲過所有帳密設定才看得到；而使用者要找「能不能用別的模型」時，眼睛看的是頁面最上面那個「模型 API」區塊——那裡顯示的還是九個寫死的模型。**功能找不到就等於不存在**(這個 session 第二次踩到同一類問題，前一次是排程的暫停按鈕)。現在搬到「模型 API」正下方(y=623，整頁 11%)，標題也直接寫出「內建 + N 個自己接的」｜驗證：實測位置從 87% → 11%
- Fixed｜自己接的模型不會出現在任何下拉選單裡｜加了來源之後，設定頁的「測試連線」下拉與**流程頁真正選模型的那個選單**都還是只列內建那幾個——等於加了也用不到(除非知道要按「自訂」手動打字)。兩個選單都改成從 `/api/model-providers` 拿清單，自訂模型獨立成一組「你自己接的」並標出來源名稱｜驗證：加一個假來源後，實測 `gemma4（家裡那台 (n8n)）` 同時出現在設定頁與流程頁的選單裡

### Added

- Added｜模型不再綁死一組 Base URL：可以接自己的地端／別台機器的模型｜使用者原話：「我現在多了一個地端的 gemma4 模型可以用，我從別台電腦用 n8n 串出來的…你現在寫死成那樣那我有 gemma4 的就會變成不能用」。**診斷後真正的瓶頸不是模型名稱寫死**(自訂名稱本來就填得進去)，是整個平台只有一組 `baseUrl`/`apiKey`——他的 gemma4 在另一台機器上，換過去就等於放棄現有 gateway 的所有模型。現在可以有多個「模型來源」，每個有自己的網址與金鑰；模型代號**直接寫 `gemma4` 就好**，平台會找到對的端點(只有同名撞到兩個來源才需要 `來源::模型`)——使用者不該為了平台的內部結構學新語法。沒聽過的代號也不再被拒絕，會退回內建端點試試看。「這個模型看得懂圖片嗎」以前是硬編碼清單(外面的模型永遠進不去)，現在自訂來源自己勾；UI 的 ✓「可用」改成**以這台機器的實測結果為準**，不是寫死的 KNOWN_WORKING_MODELS(那份換個服務商就不準)｜`lib/modelProviders.ts`、`/api/model-providers`、`ModelProvidersCard.tsx`、`getClient(model)` 與 engine 的端點解析｜驗證：6 個單元測試 + 真實加一個來源實測 `gemma4` 解析到對的端點
- Added｜每條流程各自指定產出資料夾，也可以完全不另外存｜使用者原話：「產出的檔案要能分開啊，我每個要放的資料夾不一定一樣，也有可能不用放資料夾直接放在『產出檔案』裡面就好，要能在 workflow 中設定，並且要直觀」。流程頁「⋯ → 📁 這條流程的檔案要放哪」給**三個講人話的選項**：①不另外存(只留在平台的「產出檔案」頁) ②跟設定裡的一樣 ③指定一個資料夾(可當場建新的)。第①項必須是明確選得到的選項而不是靠「留空」暗示——留空在這裡本來就已經是第②項的意思。優先順序：步驟自己填的 > 這條流程的 > 全域設定 > 桌面(舊行為)｜`Workflow.outputFolder`、`resolveExtraSaveDir` 三層解析、`OutputFolderPanel.tsx`｜驗證：瀏覽器實測三個選項與 21 個可選資料夾都正確渲染

### Fixed

- Fixed｜視覺模型的挑選差點把 Claude Code 放進驗證碼候選｜改成用能力挑選模型之後，`pickVisionModel` 會把 Claude Code 算成「看得懂圖片」——但 AGENTS.md 明令它**絕不能**當驗證碼候選：Claude 會基於安全政策主動拒絕，而那個拒絕是 `is_error:false` 的「成功」回應，重試或換一次都不會變好，只會白燒一輪重試+退避。**既有測試當場抓到**；已加 `excludeClaudeCode` 選項並在驗證碼路徑指定
- Fixed｜視覺模型的實測可靠度順序差點被洗掉｜`VISION_MODELS` 的**順序本身就是資訊**(依實測可靠度排：minimax-m3 最穩、Kimi-k2.6 偶爾答非所問)，攤平成「所有來源的模型清單」時會被排序洗掉，實際挑到的變成 Kimi。同樣是既有測試抓到的；挑選規則改成「內建視覺模型照實測可靠度 → 使用者自己接的(實測過的優先)」
- Fixed｜接自己的模型時打錯位址會讓「測試連線」轉圈圈好幾分鐘｜原本用一般呼叫的 90 秒逾時×多次重試，而「第一次接自己的模型時把 IP 或埠打錯」正是最常見的情況。改成 20 秒、最多 2 次，並把連線類錯誤翻成人話(講出是哪一個來源、哪個網址、要檢查什麼)，不要丟一句英文的 Connection error 給他猜。實測：打錯位址 **1 秒**就回明確訊息

### Added

- Added｜可靠性總覽：把「這個平台到底靠不靠譜」變成數字｜使用者最在意的三個問題(從零建得起來嗎、出問題 AI 修不修得了、排程能不能 100/100)，平台原本**一題都答不出來**，只能憑印象。新增 `/reliability` 一頁回答三題，並且**樣本太少就說樣本太少**——不會把 1/2 顯示成 50% 讓人以為那是個比率；每個數字下面都寫清楚它的定義｜`/api/reliability`、`app/reliability/page.tsx`｜驗證：真實資料實測(排程觸發 2 次、17 條流程 115 步驟其中 28% 是 AI 寫的程式碼、11/17 完全只用現成積木)，截圖確認長字串不會把版面擠歪
- Added｜修復迴圈留下紀錄，自我修復率才算得出來｜修復跑完就結束、什麼都沒留下，所以「AI 修不修得了」以前答不出來。新增 `repair_attempts` 表，記在唯一的共同底層(`aiRepairGraph` 的薄包裝，用 finally 涵蓋「修復自己拋錯」那條路)，成敗由既有資料推導：修完之後那條流程的下一次執行結果。**刻意不去改 autofix/autorun 那兩支散落十幾處 return 的路由**——那是已經在正式使用、修過很多真實 bug 的東西，為了埋量測動它的控制流風險太高｜`lib/workflow/repairMetrics.ts`｜驗證：三條修復路徑(autofix/autorun/背景提案)都標了來源，測試產生的紀錄已清掉
- Added｜排程看門狗：不准安靜地不執行｜真實踩過——一條排程開著、畫面看起來正常、每分鐘都被自動觸發檢查跳過，長期完全不執行卻沒有任何人發現，唯一的痕跡是一行終端機警告。無人值守的產品裡「安靜地不執行」比明確失敗危險得多。現在①該跑卻被擋住 → 桌面通知(每天最多一次，節流用 audit_log 當跨行程共同記憶)②排程器自己卡住(下次執行時間過期超過 25 小時) → 通知。排程頁與可靠性頁也會把這種排程標紅｜`lib/scheduleWatchdog.ts`｜驗證：2 個節流測試(第一次要通知、24 小時內不重複、30 小時前的舊紀錄不算)；刻意不在測試裡呼叫 warnXxx(那會真的在使用者螢幕上冒通知)
- Added｜對話框收螢幕錄影：說不清楚的步驟，錄一段給 AI 看｜使用者原話「有些步驟很難說清楚可以用錄影的方式呈現，但是又怕他理解不了」。所以錄影**只當附件、不自動產生任何東西**——AI 拿它理解意圖，最後仍然是畫出流程圖由使用者按「套用到畫布」，理解錯的代價只是補一句話。**在瀏覽器裡抽關鍵影格，影片完全不上傳**：一段 30 秒螢幕錄影動輒 20-60MB 會撞上傳上限，抽完只剩幾張 JPEG；而且不需要 ffmpeg(這是開源專案，少一個系統相依就少一個在別人機器上壞掉的理由)。畫面幾乎沒變的取樣點會跳過並誠實說明「不是漏抽」｜`lib/videoFrames.ts`｜驗證：型別/lint 通過，拖放提示文案改成明講可以拖影片
- Added｜瀏覽器錄製：示範一次，變成可重複套用的步驟｜這是「說不清楚」裡**最能確定的那一半**——手指知道嘴巴說不出來的東西(那個鈕藏在下拉選單裡)。Playwright 錄的是真實 DOM 選擇器不是螢幕像素，所以沒有什麼需要「理解」(真實病灶：AI 猜選擇器猜成 div，頁面上其實是 SVG 的 g)。**精確 vs 可變的界線不靠模型猜**：結構(點哪個按鈕、填哪一格)永遠照錄的做，值(他在欄位輸入的內容)預設變成設定欄位，由使用者用白話逐一勾選，再過既有的 `applyParameterization` 確定性驗證。所以示範一次「寄信」存下來的是「怎麼寄信」，不是「寄那封一模一樣的信」｜`lib/workflow/actionRecorder.ts`、`/api/workflows/[id]/record`、`/api/user-steps/from-recording`、`RecordActionCard.tsx`｜驗證：7 個單元測試 + **真實跑過兩輪錄製**
- Added｜產出檔案可以指定要放哪個資料夾｜使用者原話：「可以在桌面自己新增資料夾然後指定產出的檔案要放去哪個資料夾，而不是都直接丟在桌面上」。原本 excel-process 是**寫死** `~/Desktop`、write-file 則要使用者自己貼絕對路徑——前者不給選，後者要小白知道什麼是絕對路徑。現在是「先選桌面/文件/下載 → 再點一個現有資料夾」，而且可以當場建一個新的不用切去 Finder。**沒設定就完全不改變現有行為**(照舊放桌面)；只准選家目錄底下的資料夾(realpath 比對，擋 symlink 繞過)；設定過的資料夾被刪掉會明講而不是靜默失效｜`lib/outputFolder.ts`、`/api/output-folder`、`OutputFolderCard.tsx`｜驗證：瀏覽器實測(三個位置按鈕、21 個可選資料夾、建立輸入框都在)

### Fixed

- Fixed｜錄製收尾的雜項會弄壞整條流程（真實跑過才發現，不是理論問題）｜Playwright codegen 收尾時會多產出 `await page.close()` 與 `await context.storageState({path})`。前者關掉的是**整張圖共用的那個分頁**，後面每一步都會壞；後者在節點作用域裡沒有 `context` 物件，一執行就 ReferenceError，而且還會把本機絕對路徑寫進步驟裡。單元測試用的樣本沒有這幾行，所以只有真的錄一次才會暴露｜已加回歸測試釘住
- Fixed｜錄製結束後留下孤兒行程與關不掉的瀏覽器視窗｜原本透過 `npx` 啟動，kill 到的是 npx 這層中介而不是真正在跑的錄製器(實測 finish 之後 `pgrep playwright codegen` 還有一個活著)。改成直接跑本機的 playwright CLI + 自成行程群組，結束時收掉整個群組
- Fixed｜密碼欄的掃描配不到真實選擇器，明碼密碼會留在檔案裡｜原本用 `[^'"`]` 字元類別配對引號，但真實選擇器長得像 `'input[name="PASSWD"]'`(外層單引號、裡面有雙引號)，整個配不到。結果是「不在已保存帳密裡的密碼」原封不動留在步驟程式碼裡，而那段程式碼會進 workflow JSON、可能被匯出、也會進每日備份。改用反向參照配對引號｜**這個漏洞是我自己寫的測試抓到的**

### Added

- Added｜排程可以「全部暫停」，而恢復只會打開這次被關掉的那幾筆｜使用者要求「排程那邊要有可以手動暫停一些排程的功能」。個別暫停其實一直都有，但那顆按鈕是**一個沒有文字的 🟢 emoji**，還夾在拖曳把手 ⠿ 跟流程名稱中間——使用者完全看不出它可以按，所以直接來要一個已經存在的功能。**找不到的功能等於不存在**，因此改成帶文字的「⏸ 暫停 / ▶ 恢復」，並補上「出門/放假/正在改東西」用的一鍵全部暫停。**關鍵設計：恢復只打開「這次被全部暫停關掉的」那幾筆**——若做成「打開全部」，會把使用者幾週前刻意關掉的排程一起放回背景執行，而背景執行會真的寄信、寫試算表、動外部系統；他以為只是還原剛才的動作，實際上發生了他沒同意的事｜`lib/scheduler.ts`(pauseAllSchedules/resumePausedBatch)、`/api/schedules/bulk`、`app/schedules/page.tsx`｜驗證：2 個單元測試釘住「不動到原本就暫停的那筆」與「遇到已刪除的排程要跳過並回報」；真實瀏覽器實測 12 筆全部暫停→標題列變 0 個會自動執行→恢復→回到 11 個，DB 核對 12 筆 enabled 與 next_run_at 全部還原正確（刻意不對 pauseAllSchedules 寫單元測試：它掃全表，會關掉使用者真正在用的排程）

- Added｜排程「開著但其實不會執行」現在會在畫面上講出來｜排程器每分鐘觸發前會過一道自動觸發檢查(草稿/缺帳密/匯入未信任)，沒過就跳過並只寫一行終端機警告。畫面上那一列跟正常的一模一樣(綠燈+下次執行時間)，所以使用者會一直等一個永遠不會發生的執行。實測發現**現在就有一條這種排程**(開著、流程還是草稿)。現在該列會顯示紅色「不會執行」標籤與具體原因＋怎麼修｜`/api/schedules` 的 blockedReason、`app/schedules/page.tsx`｜驗證：真實瀏覽器截圖確認警告出現在正確那一列、文案講出「流程還是草稿，要它真的跑先設為正式」

### Fixed

- Fixed｜登入節點在雙重驗證(Authenticator App)畫面前會誤判「登入成功」｜使用者的 webmail 帳號改成要求手機 Authenticator App 雙重驗證，導致 11 條依賴它登入的排程流程失效。根因：`browser-login` 節點原本只靠「帳號欄位消失」判斷登入成功，但雙重驗證畫面同樣沒有帳號欄位——帳密送出後就算卡在驗證碼畫面，也會被判成功、實際下游步驟讀到的是未登入內容。**這個節點不會、也不該自動輸入雙重驗證碼**(手機掃碼綁定的密鑰通常拿不到，讓伺服器自動算也會讓雙重驗證形同虛設)。改成表單消失後再確認畫面上真的有已登入特徵(登出鍵/收件匣等)，偵測到還卡在雙重驗證提示文字時丟出清楚指路「⋯ → 🔐 手動登入一次」的錯誤，不再默默回報假成功｜`lib/workflow/nodes/browserLogin.ts`｜驗證：`npx tsc --noEmit`、`npm test`(1047 個測試全過)、`npm run check:change-guard` 全過

### Added

- Added｜多條流程可以共用同一個網站的登入狀態，並且以後新增的登入節點預設就是這樣｜使用者的 webmail 帳號**不允許同一帳號同時多處登入，一登入新地方就把舊的踢掉**，而登入狀態本身還有 12 小時閒置逾時——11 條週期性流程(每週/每月/每季)若各自維護獨立登入狀態，只會輪流把彼此踢下線；若各自定期重新登入延長 session，一樣會互踢。正解是共用同一份登入狀態，並且**只用「造訪登入頁確認還活著」續命，絕不重新登入**。共用代號自動從「登入頁網址主機名稱＋帳密欄位名稱」推導(不是使用者手動打字互相對應，打錯字會悄悄退回互踢且不容易察覺)；`shareLoginAcrossWorkflows` 開關**預設是開的**(使用者原話：「要記得以後有這個登入 mail2000 的節點都要這樣做」)——用 `withSchemaDefaults` 讓「JSON 裡完全沒設定這個欄位」的既有 11 條流程與未來新建的節點都自動視為已開，不需要逐條手動勾選。新增背景工作 `lib/webmailKeepAlive.ts`：每 4 小時(12 小時逾時門檻的三倍安全邊際)開一個無頭瀏覽器，用共用登入狀態造訪登入頁一次、確認仍是已登入狀態、續存 cookie，全程不碰帳號密碼；`lib/workflow/manualLogin.ts` 的「🔐 手動登入一次」也支援寫進共用檔案，登入一次(含輸入手機驗證碼)即可讓所有共用同一帳號的流程一起生效｜`lib/workflow/sharedLoginSession.ts`、`lib/workflow/engine.ts`、`lib/webmailKeepAlive.ts`、`instrumentation.ts`｜驗證：`npx tsc --noEmit`、`npm test`(新增 sharedLoginSession/webmailKeepAlive 相關測試)、`npm run check:change-guard` 全過；未覆蓋風險：續命背景工作本身未經真實 12 小時長時間運行驗證，且沒有加進首頁健康檢查白名單(刻意決定——續命失敗不算核心基礎設施掛掉，失敗時使用者會從既有的排程執行失敗通知看到)

- Added｜「用網頁信箱寄信」步驟可以先測試寄送，強制只會寄到自己｜使用者要在流程裡加寄信步驟前先確認：「還要可以測試把信件傳給自己來檢視內容是否正確，千萬不能不小心真的傳到自己以外的地方」。查現有機制發現只有一半：只讀演練模式完全不會真的寄出(連寄給自己都不會，沒辦法在真實收件匣肉眼確認格式/附件)；另一個 SMTP 寄信節點有「留空＝寄給自己」的設計，但這個節點沒有跟進。新增專用的測試寄送：節點面板出現一個信箱輸入框＋按鈕，用這一步真正會產生的主旨/內容/附件寄一封信，但**不管節點設定的收件人/副本/密件副本是誰，一律只會寄到這裡填的信箱**——安全保證是結構性的，不是靠使用者記得改欄位：後端 API 自己組出唯一允許的 `nodeConfigOverrides`(`to`＝填的信箱、`cc`/`bcc`＝清空)，不接受前端傳任何其他覆寫內容｜`app/api/workflows/[id]/nodes/[nid]/test-send/route.ts`、`app/workflows/[id]/NodePanel.tsx`｜驗證：`npx tsc --noEmit`、`npm test`、`npm run check:change-guard` 全過；**用真實操作路徑跑過一次**——開一條測試流程(收件人故意設成假的外部信箱)，瀏覽器裡填自己的測試信箱按下送出，直接查資料庫裡這次執行存的 `node_config_overrides_json`，證實送出的覆寫是 `{"to":"這次填的信箱","cc":"","bcc":""}`，跟節點設定的假外部信箱無關；測完刪除測試流程

### Fixed

- Fixed｜「手動登入一次」實際存的是這條流程自己專屬的檔案，完全沒有寫進共用登入狀態｜使用者實測：照畫面指示做完手動登入(含輸入手機驗證碼)，但畫面上沒有任何成功/失敗的回饋，追問「我怎麼知道已經完成了？」。查真實存檔證據發現了比「沒有回饋」更嚴重的問題——`openManualLoginInner` 雖然正確算出了共用代號(`storageKey`)，但實際讀寫檔案的 `loadState(workflowId)`/`saveStateFile(workflowId, ...)` 兩處呼叫忘了改用這個新參數，仍然用的是舊的 `workflowId`。等於**這次登入完全沒有進到共用池**，只存進了觸發登入的那一條流程自己的檔案，其他 11 條共用同一帳號的流程完全吃不到。改用 `storageKey` 後，順手把「視窗關閉後有沒有真的登入成功」補上明確回饋：視窗一關(不管是使用者按關閉還是直接叉視窗)就自動用無頭瀏覽器重新造訪一次登入頁、跟 `browser-login` 節點判斷成功用同一套 `isAuthenticated` 標準，畫面上直接顯示「✅ 已確認登入狀態有效」或「⚠️ 沒有偵測到已登入」，不用再自己猜｜`lib/workflow/manualLogin.ts`、`app/api/workflows/[id]/manual-login/route.ts`、`app/workflows/[id]/page.tsx`｜驗證：`npx tsc --noEmit`、`npm test`(1053 個測試全過，新增 6 個測試釘住這個呼叫點)、`npm run check:change-guard` 全過；並且**用真實資料修復**：把使用者已經登入成功、但存錯位置的那份狀態手動搬到正確的共用檔案，再用平台實際的 `isAuthenticated` 邏輯重新造訪一次真實網址驗證，確認回傳 `true`——使用者不用重新走一次登入流程

## [0.2.0] - 2026-07-30

企業級稽核（外部 AI 稽核 36 項）的第一批處理：相依漏洞、存取控制、稽核軌跡、資料保留期限、
備份還原、版本發布，以及把隱私掃描這個安全網從「有人記得手動跑」變成「不跑就過不了」。

### Added

- Added｜本機存取權杖，讓「連得到 127.0.0.1」不再等於「能驅動整個引擎」｜87 支 API 原本沒有任何一支檢查呼叫者是誰，而 `proxy.ts` 的設計刻意放行「沒有 Origin 的請求」(curl/腳本/同機工具)——在企業機器上「本機行程都可信」這個假設不成立(MDM agent、防毒、公司內部工具、套件的 postinstall 都在跑)，同一台機器也可能有其他 OS 帳號。現在 proxy 對所有 /api 驗權杖(`data/local-token`，0600)，瀏覽器透過 httpOnly + SameSite=Strict cookie 自動帶上、使用者完全無感；webhook／Google 導回／健康檢查在白名單內(各自有自己的認證)。最危險的那幾支(帳密、流程增刪改、執行、匯入、開瀏覽器、抓網址)在 handler 內再驗一次——proxy 整層被繞過是真實發生過的漏洞類別，唯一的門不能只放在可能被跳過的地方。誠實邊界：**擋不住以同一個 OS 使用者身分執行的惡意程式**(它讀得到權杖檔，也本來就讀得到 .env 與 DB)，見 `SECURITY.md`｜`lib/localToken.ts`、`lib/requireLocal.ts`、`proxy.ts`｜驗證：權杖長度／檔案權限 0600／常數時間比對／白名單正反例共 4 個測試、瀏覽器實際操作一輪、腳本改用 header
- Added｜稽核軌跡（誰、什麼時候、對什麼做了什麼）｜原本只有「系統做了什麼」的執行紀錄，沒有「人做了什麼」。最關鍵的缺口是**人工核准閘**——它是產品賣點，但核准這個動作本身沒留下任何紀錄，核准無法歸屬在稽核上等於不存在。新增 `audit_log` 表與寫入點：核准／拒絕(所有管道共用同一個入口，不會有路徑漏記)、流程增刪改與匯入、步驟設定修改、手動觸發、帳密讀寫、查看明碼、全域設定、排程增刪改、我的步驟、保留期限清理。誠實限制：這個產品沒有使用者概念，所以 actor 記到「管道」等級(本機瀏覽器／腳本／Telegram／簽核連結)，不是哪個人；結構先建好，之後接上身分層直接換 actor 即可｜`lib/auditLog.ts`、`app/settings/AuditLogCard.tsx`、`/api/audit-log`｜驗證：寫入／查詢／細節不含帳密值／寫入失敗不影響主線／行為者判斷共 3 個測試 + 設定頁實際查看
- Added｜資料保留期限｜既有的 `pruneRuns` 是「每流程留最近 20 筆」的**筆數**上限(所以磁碟本來就不會無限長大)，但筆數回答不了「登入後的截圖最多留多久」——一條每月跑一次的流程，20 筆等於快兩年的截圖、名單、金額留在磁碟上。新增可設定的**時間**上限：除錯截圖與頁面內容預設 90 天(PII 最密集、過期價值趨近於零)，執行紀錄與產出檔預設不按時間刪(那是使用者的成果，要不要丟由他決定)。改設定不會立刻刪東西，畫面永遠先顯示「現在按清理會刪掉多少」｜`lib/retention.ts`、`app/settings/RetentionCard.tsx`、`/api/retention`、scheduler 每天一次｜驗證：設定值夾取／預設值不對稱／預覽跑兩次結果相同(證明真的沒動到東西)共 3 個測試
- Added｜備份還原路徑與自動化演練｜原本只有「建立備份」，還原全靠說明文字——未經演練的備份等於沒有備份。新增 `restoreDataBackup()` 與 `npm run restore:backup`：還原前確認服務沒在跑(不然會得到一半舊一半新)、**不刪任何現有資料**(既有檔案改名成 `.before-restore-<時間>` 留著)、**一定連舊的 `-wal`/`-shm` 一起移開**(手動還原最常踩的坑：舊日誌會套在還原後的資料庫上，看起來成功資料卻是舊的)、`.env` 只另存不覆蓋(現有的可能比備份新)、還原後跑 `integrity_check` 並讀出關鍵表列數驗證｜`lib/dataBackup.ts`、`scripts/restore-backup.mjs`｜驗證：完整演練測試(建立備份→清空資料庫→刪流程 JSON→留一份過期 WAL→還原→逐項比對內容一致)＋非備份 zip 要老實拒絕且不留半套檔案
- Added｜相依漏洞閘門與 Dependabot｜21 個 high 等級漏洞(含 Next.js 的 middleware/proxy bypass 公告——正好是擋「任何網頁隔空 RCE」的那一層)默默存在到有人專門稽核才被發現。列出來不等於會被處理，只有「不處理就卡住 CI」才會：新增 `npm run check:audit` 並加進 CI，另加 Dependabot 每週送修補 PR。閘門以**漏洞公告**為單位判斷而不是套件名(一個 brace-expansion 公告會列出 16 個套件，全是同一條傳遞相依鏈)，已接受的風險必須寫理由與**重新評估日期**，過期自動失敗｜`scripts/audit-gate.mjs`、`.github/dependabot.yml`、CI｜驗證：實際升級 next 16.2.10→16.2.12、postcss→8.5.25、sharp→0.35.3、adm-zip 0.5→0.6，**5 個公告中 4 個真的修掉**(含 Next.js 那條)，`npm audit` 21 high → 16 high 且全部來自剩下那一個公告；956 個測試、lint、build 全過
- Added｜剩下那一個漏洞：老實記成「已接受的風險」而不是硬修｜brace-expansion 的 DoS 只有 5.0.8 修好，但**兩條升級路實測都會弄壞工具鏈**：用 overrides 拉到 5.0.8 → minimatch 2/3 用 `require()` 當函式呼叫、v5 改具名匯出，eslint 直接 `TypeError: expand is not a function`；升 eslint 10 → eslint-config-next 16.2.12 內附的 eslint-plugin-react 在 eslint 10 下 crash。兩者都實際跑過才下結論(不是看版本號推測)。剩下的消費者(開發期的 eslint、寫 xlsx 的 archiver)glob pattern 都是程式碼寫死的，沒有攻擊者可控的觸發路徑；例外設 2026-10-31 到期，屆時強制重新評估
- Added｜服務會回報自己的版本｜`/api/health` 新增 `version`，版本號與 git tag 一一對應。「現在跑的是哪一版」要能從執行中的服務問出來，看原始碼回答的是另一個問題｜`lib/version.ts`｜驗證：實際打 /api/health 確認版本字串

### Changed

- Changed｜帳密／API Key 明碼端點由 GET 改 POST｜三個問題疊加：`proxy.ts` 的 Origin 檢查只套用在非 GET/HEAD 方法上(所以 GET 版本完全不過 Origin 檢查)、欄位名稱會進 access log 與瀏覽器歷史、以及「跨站讀不到回應」是靠瀏覽器的 CORS 行為——那是把自己的授權檢查外包給瀏覽器。改 POST 後自動獲得 Origin 檢查、key 走 body、加上本機權杖與稽核紀錄｜`/api/secrets/reveal`、`/api/settings/reveal`、`app/settings/RevealCopyButton.tsx`｜驗證：設定頁「顯示並複製」實際點過
- Changed｜隱私黑名單掃描不再靜默略過｜兩個缺口疊加讓這個安全網實質上沒在運作：黑名單被 gitignore(只存在作者那台機器)、CI 根本沒跑 change-guard。最惡劣的是**靜默略過看起來像通過**——別的 AI 工具在別的環境跑完看到 ✅，會合理相信隱私掃描掃過了。現在缺清單會大聲說「這一項沒有掃」且結尾訊息明確寫「略過，未檢查」；CI 從 GitHub Secret 還原清單再掃；另加 pre-commit hook 只跑隱私掃描(毫秒級，不會有人想用 --no-verify 繞過)｜`scripts/change-guard.mjs`、`scripts/install-hooks.mjs`、CI｜驗證：有清單／無清單兩種情況都實跑過，hook 實際安裝並在 commit 時執行
- Changed｜`package.json` 宣告 `engines: node >= 24`｜CI 鎖 Node 24 且註解自己記錄了 npm 10/11 lock 格式混用會假報 out-of-sync，但沒有 `engines`——用 Node 20/22 的人 clone 下來會踩同一個雷且沒有任何提示，這對「貼個 repo 網址給 AI 就自動裝好」的定位傷害特別大

### Added

- Added｜自訂步驟 dry-run 的隔離提升到獨立子程序與 Node OS permission｜只讀安全試跑的 custom-code 現在不在主程序執行：子程序沒有 API key、網路或任意瀏覽器物件，只能透過父程序核准的唯讀瀏覽器 RPC；Node 支援 permission mode 時寫檔、子程序與未授權檔案讀取由作業系統權限再擋一層，連繞過靜態字串掃描的別名寫檔也會失敗。較舊 Node 會明確留下 `vm-fallback` 證據，不能把 VM 說成完整敵手隔離。驗證：獨立子程序計算、DOM RPC、fetch/process 不可用、別名點擊攔截、別名寫檔 OS 權限攔截、TypeScript 與 lint；正式執行路徑不變。

- Added｜自訂步驟的真實唯讀執行隔離｜只讀安全試跑的 custom-code 不再只依賴正規表示式；改在受限 VM 中執行，移除 process／require／fetch 等全域能力，將瀏覽器包成只能讀取的介面，封鎖點擊、輸入、evaluate、檔案登記與未核准模組載入；正式執行路徑不變。驗證：sandbox DOM 讀取、Function constructor 逃逸、能力別名、檔案寫入、動態載入與 TypeScript 回歸；未覆蓋風險：Node VM 不是作業系統級隔離，正式執行仍是本機完整權限，若要把不可信程式碼當成真正敵手需再拆獨立程序／權限容器。

- Added｜不確定外部副作用的人類決策中心｜外部寄信、通知或寫入在逾時後可能已完成時，歷史面板會列出受保護的步驟，使用者可選「已完成，繼續後面」或「確定沒完成，允許重試」；平台不回傳帳密／原始輸入、不盲目重送，並從原處續跑。驗證：effectResolution、idempotency、API、歷史面板與完整品質閘門。

- Added｜失敗步驟安全重播｜執行紀錄新增「安全重播這一步」：凍結失敗當下保存的節點 input，只讀重播該步，不重跑上游、不把敏感 input 放進 API 回應或觸發參數；流程版本漂移時拒絕重播並說明原因。驗證：引擎／API／歷史面板與完整品質閘門。

- Added｜修好後可用目前版本重試原始輸入｜執行紀錄新增「用目前版本重試」：從原始輸入建立新的 run，不沿用舊版節點結果、一次性 secret 覆寫或暫時設定；原本安全試跑仍維持安全試跑，正式失敗才重新走正式副作用。與「從失敗那步續跑」分開呈現，避免小白選錯。驗證：currentRetry.test、真實 API route、HistoryPanel 瀏覽器快照、change guard、TypeScript、812/812 tests、lint 與 diff check 全部通過。
- Added｜一鍵安全診斷包｜失敗執行可下載只含流程指紋、節點狀態、錯誤分類、欄位形狀與脫敏線索的診斷 JSON；原始輸入、輸出、帳密與一次性覆寫確定性排除，方便小白交給 AI 或支援者而不必自行整理敏感資料。驗證：diagnosticBundle.test、真實 API／瀏覽器下載入口與完整品質閘門。

- Added｜可攜式驗證護照｜workflow 匯出一併帶出已保存的情境測試定義（輸入、預期欄位／分支形狀與安全控制），匯入後重新加密輸入並重新建立本機情境；不搬移執行紀錄、帳密或授權，圖指紋不一致的情境會跳過並回報數量；匯入後第一則白話訊息會明確說明哪些驗證帶回、哪些因版本不符被跳過。驗證：情境測試、TypeScript、完整測試、change guard 與真實匯出／匯入 API 回歸。

- Added｜常駐版與開發版同時開啟時仍可能互相覆蓋流程｜所有會修改流程的入口現在除了進程內快速鎖，也查 SQLite 的活修復 session；節點編輯、結構 PATCH、對話 edits、觸發設定、Webhook／LINE、提案套用、版本還原與另一輪 autorun／autofix 都會在另一個進程修復時回白話 409，不會覆蓋尚未驗證的 AI 修改。修復 session 仍保留 owner PID 與崩潰復原｜`repairSessions.ts`、多個 workflow API route｜活鎖查詢／解除、跨進程重複修復拒絕、死進程快照還原、lint、TypeScript 與受影響測試通過｜停止另一個進程的迴圈仍需由承載它的進程處理；本次先確保任何其他修改入口不能趁它執行時覆蓋流程

- Added｜批次處理遇到中途失敗不再整批重做｜repeat-steps 為每個項目保存本機加密檢查點；外層重試或從失敗處續跑時，已成功項目直接沿用，只有失敗／未完成項目重新執行，並在執行紀錄寫出「沿用檢查點、不重做」｜`repeatCheckpoint.ts`、`repeat_item_checkpoints`、`repeatSteps.ts`｜repeat-steps 真實 execute 回歸、3 個項目第二次只沿用 3 個成功檢查點、密文保存檢查與 TypeScript 通過｜目前檢查點綁定同一筆 run 與項目指紋；若使用者建立全新執行，仍會按全新輸入重新跑，避免誤用舊批次資料

- Added｜分支覆蓋仍需小白自己想測試值｜新增條件判斷／多路分流的安全情境推導、API 試跑與覆蓋面板一鍵建立；只允許直接手動輸入可確定推導，未知上游不猜｜`branchScenario.ts`、`/scenarios/branch`、HistoryPanel｜條件是／否與分流請假／其他真實 dry-run 成功並保存回歸情境；保留未測的報支出口｜未知檔案／信件／AI 值仍需實際資料保存情境
- Added｜出錯時備援仍需小白自己製造事故｜新增只限 dry-run 的故障注入情境：從最近一次確實走到該步驟的成功輸入開始，在 execute 前模擬一次失敗，確認 error 出口、正常路徑 skipped 與 Plan B 結果；保存 `forcedFailures` 控制並讓情境套件／AI 修復重播同一故障｜`engine.ts`、`scenarioTests.ts`、`/scenarios/error-branch`、HistoryPanel｜真實 API／引擎／瀏覽器按鈕驗證通過，正式執行不接受故障控制，臨時資料已清理｜只能驗證可抵達且已接 error 出口的步驟，外部服務真實故障仍需另行觀察
- Added｜下拉與是非輸入仍要小白逐個想情境｜從最近一次成功執行自動找出流程明確宣告的 select／boolean 選項，逐一以安全 dry-run 補測其他值，成功後才保存情境；文字、金額與檔案不猜，不把模型想像當成測試資料｜`inputVariants.ts`、`/scenarios/input-variants`、HistoryPanel｜真實 API 與瀏覽器按鈕完成 2 個變體並保存，臨時流程已清理｜未知資料仍需使用者用實際輸入保存情境
- Added｜流程改好後仍可能在幾天後悄悄退化｜新增可選的安全健康巡檢：每 15 分鐘至每天重播目前版本已保存情境，讀取／計算照常、外部寫入一律攔住；保存每批與每個情境的結果、版本指紋和白話差異，背景巡檢遇到退化會停止報綠並提醒，舊版情境要求重新保存｜`healthCheck.ts`、`workflow_health_checks`、`workflow_health_runs`、`/health-check`、scheduler、SchedulePanel｜單元測試、真實 API／瀏覽器巡檢通過，臨時流程與巡檢資料已清理｜只會重播已保存情境；沒有真實基準或需要真人簽核的情境不會被猜測或代簽

- Added｜簽核流程可一鍵補齊核准／拒絕情境：分支覆蓋面板看到尚未走過的簽核出口時，直接沿用最近一次成功輸入做安全試跑，只走指定出口並保存決策控制；後續情境回歸與 AI 修復會重播同一決策，不再把只驗過核准誤當成拒絕也可靠。安全試跑同時補上 `activePorts`，未選中的簽核分支會明確標成 skipped。驗證：wait-approval 單元、引擎分支、情境保存、真實 API 核准／拒絕重播與資料清理。
- Added｜情境回歸不再把「成功但走樣」當綠燈：若執行沒有丟錯但欄位、型別或分支和保存基準不同，面板會列出白話差異並定位第一個受影響步驟；「讓 AI 修這個情境」會把該情境的執行現場與差異餵給整圖修復器，且失敗 run 查詢嚴格限制在該情境，不會拿同流程其他情境的資料修錯。修復迴圈只有情境比對通過才可收工。驗證：情境診斷回歸、TypeScript 與完整測試。
- Added｜情境失敗後可直接讓 AI 修復：情境回歸列出失敗步驟時，新增「讓 AI 修這個情境」；修復器會使用該情境保存的輸入、整圖失敗現場與既有修復迴圈，修改後只以同一情境 dry-run 驗證，未通過的改動照原規則還原；舊版情境與不存在的情境在 API 層拒絕，不會拿錯版本或錯輸入修流程。驗證：型別／完整回歸與情境 API 版本閘門。
- Added｜情境失敗後可直接讓 AI 修復：情境回歸列出失敗步驟時，新增「讓 AI 修這個情境」；修復器會使用該情境保存的輸入、整圖失敗現場與既有修復迴圈，修改後只以同一情境 dry-run 驗證，未通過的改動照原規則還原；舊版情境與不存在的情境在 API 層拒絕，不會拿錯版本或錯輸入修流程。驗證：型別／完整回歸與情境 API 版本閘門。
- Added｜全套情境回歸：情境測試面板新增「全部安全重播」，可一次驗證目前版本所有已保存情境並顯示通過／未通過／舊版狀態；保存情境的原始成功執行會納入套件基準。新增 `scenario-suite` 自動觸發 readiness，排程、Webhook、資料夾監聽與訊息觸發在回歸未全綠時會停下並提供白話下一步；手動執行仍可用。驗證：情境套件狀態回歸、版本漂移、完整測試與真實批次 API/瀏覽器驗證。
- Added｜可重播情境測試包：成功執行可保存為具名情境，將輸入資料在本機加密保存，綁定目前流程版本、每一步輸出欄位型別與分支出口；之後可從執行紀錄一鍵安全 dry-run 重播，API 明確回報是否符合情境。流程改版後自動拒絕舊情境，避免單次綠燈冒充複雜流程已完整驗證。驗證：798 項既有測試加情境回歸、真實 API 保存/重播/匹配、真實瀏覽器入口與清理臨時流程。
- Added｜執行歷史新增「結果收據」：每次執行可查看各節點產生的欄位名稱、型別、筆數／文字量與檔案名稱摘要，並和上一次成功執行比較結構差異；只回傳確定性摘要，不顯示原始資料、個資或帳密。新增 receipt API、歷史面板入口與脫敏比較測試。
- Added｜版本歷史新增「查看差異」：以確定性摘要比較歷史版本與目前流程，列出新增／刪除／修改步驟、連線與執行參數變化；新增寄信、通知、產檔、外部寫入等副作用能力時以白話警示，不回傳完整程式碼或帳密。新增版本差異 API 與 VersionsPanel 互動，並以真實備份回歸驗證。
- Added｜執行前資料流面板：新增 data-flow API 與「🔗 資料流」入口，靜態列出每一步收到的上游欄位、這一步新增的欄位、執行期才會出現的欄位與模板引用；缺失欄位會白話警告，無法靜態分析的 custom-code 會標成「自訂步驟可能提供」，不執行程式碼、不讀帳密，並綁定目前圖版本指紋。驗證：dataFlow.test.ts 2 項、API 回應與真實瀏覽器面板驗證。
- Added｜啟用檢查一鍵導引：自動觸發 readiness 每個阻擋項新增標準化 actionCode，可直接前往設定、n8n 搬家核對、安全試跑或只讀保護；後端閘門不因 UI 導引而放寬。驗證：全套回歸與真實 n8n 匯入流程瀏覽器驗證。
- Added｜版本化自動化啟用護照：保存目前圖指紋、ready 結果、阻擋摘要、檢查時間與來源，重複檢查同版同結果去重；排程、Webhook、監聽與訊息觸發共用同一份 readiness。
- 執行前計畫現在鎖定圖版本指紋；使用者查看計畫後若流程被修改，正式執行會被拒絕並要求重新核對，不會把舊計畫套到新圖。
- 執行前計畫：正式執行前逐步列出讀取／計算、產生檔案、外部寫入、寄送通知與巢狀 repeat-steps 副作用；缺設定時直接導向安全設定卡，未補齊前不會啟動流程。
- HTTP 節點新增可理解的回應合約：可指定允許的狀態碼範圍，以及 JSON 欄位的 dot-path 與型別；狀態或欄位不符會在資料傳給下游前明確失敗，讓修復迴圈有具體證據可用。
- 持久化驗收標準：使用者可把已知正確答案保存到流程，之後安全驗收會重播「對答案」；標準綁定目前流程版本，圖被改動後自動標示需要重新確認，避免舊答案替新邏輯背書。
- Security｜驗收答案版本閘門：含驗收標準的流程若圖已變更，正式啟用與所有會碰外部資料的執行入口一律回報舊版本並停止，不會讓排程、Webhook、LINE 靜默沿用舊答案；安全 dry-run 保留可用，讓使用者能重新驗證並更新答案；未設定驗收標準的舊流程維持相容。
- Changed｜stale 驗收不再把小白留在錯誤訊息：流程頁正式執行與對話確認執行遇到 `ACCEPTANCE_SPEC_OUTDATED` 時，自動轉入安全只讀驗收並用白話說明下一步；不會繞過版本閘門或直接碰外部資料。
- Security｜n8n 轉換不再依 JSON 排列順序猜連線：沒有可辨識連線的匯入草稿保持斷線，遷移面板與正式執行閘門要求使用者先在畫布接好流程；避免「看似可跑、實際語意被改寫」。
- Changed｜n8n 遷移面板白話化：預設顯示「可以直接使用／需要你看一下／每一步目前要做什麼」，credential、fingerprint 與原始型別移到進階資訊，不讓工程術語成為小白核對複雜流程的門檻。
- Security｜n8n 分支語意不再被匯入成普通連線：IF 的 true/false 與 Switch 的每一路都保留 `fromPort`；n8n 沒有明確輸出名稱時使用醒目的待確認標籤並強制人工 review，不猜分類名稱或條件內容，避免匯入後看似完整卻走錯分支。
- Changed｜n8n 遷移確認改成兩段式：先「查看這一步」跳到實際節點，再由使用者明確按「我已確認」留下紀錄；不再讓單次按鈕操作看似完成檢查卻沒有讓人看過設定。
- Fixed｜n8n 工具列徽章現在也會反映未搬移連線／斷線圖，不再在遷移面板已顯示待確認時，工具列卻亮綠色「n8n 映射」。
- Added｜共用流程的只讀影響提前提醒：流程 GET 回應新增 `readOnlyImpact`，畫布會直接列出把這條共用流程納入「只讀」承諾的母流程；這只是白話的提前提醒，不取代執行前安全閘門，避免使用者等到執行時才知道相依流程會被擋。
- n8n 遷移逐步責任鏈：每個 review/unsupported 步驟都要個別確認並留下時間記錄，整體解鎖前不能只按一個模糊總開關；流程圖變更後全部確認自動失效。
- n8n 遷移護照與正式執行閘門：匯入只保存來源指紋、節點映射狀態與安全統計；畫布可逐步核對，含 review/unsupported 的版本必須明確確認，節點或連線變更後確認自動失效。

- Added｜新增只讀的 n8n workflow 分析入口 `/api/n8n/analyze`：辨識觸發、HTTP、程式碼、子流程、檔案、通知、Google 與 AI 節點，回傳 Agent Hub 映射狀態、需人工確認項與風險摘要；不保存、不執行、不回傳 URL、headers、程式碼或 credential 值。驗證：`n8nAnalyzer.test.ts` 覆蓋敏感資料不外洩、寫入/本機命令/子流程/未知節點標記與超大連線限制。未覆蓋風險：尚未提供 UI 與自動建立草稿；下一步需讓使用者逐項確認後才生成 Agent Hub draft。
- Added｜真實只讀驗收通過後建立「驗證證據護照」：保存流程圖版本指紋、每步輸入/輸出摘要、分支覆蓋摘要與產出檔 SHA-256，不保存原文、帳密或模型回覆；新增 `GET /api/workflows/:id/evidence` 回報最新護照與目前流程是否漂移，讓成功驗收能累積成可比對的本機資產，而不是一次性 AI 回答。驗證：`evidencePassport.test.ts` 覆蓋摘要化與圖版本漂移偵測。
- Added｜正式執行前接上證據漂移閘門：曾通過真實只讀驗收的流程若圖版本被改動，正式執行回 409 `VERIFIED_EVIDENCE_OUTDATED`，要求先重新安全試跑；沒有歷史護照的舊流程不回溯鎖死，安全試跑不受阻擋。驗證：新增漂移錯誤回歸測試與實際 run route 型別檢查。
- Added｜小白體驗補齊：流程頁工具列顯示「✓ 已驗證版本／⚠️ 需要重新驗收」；正式執行遇到版本漂移時自動切入安全只讀驗收，不要求使用者理解 409、指紋或版本術語。
- Added｜來源證據：固定本機來源檔案會在護照中保存內容 SHA-256、檔名與 Excel 分頁/範圍選擇；正式執行前若來源檔案消失或內容變更同樣會被擋下。API 只回傳檔名與摘要，不回傳本機絕對路徑；動態附件/下載暫存檔不被誤當成固定來源。回歸測試也實際改寫暫存來源檔，確認能辨識內容漂移。
- Added｜來源證據擴充：PDF、一般檔案、IMAP/網頁信件、瀏覽器下載附件、公開網頁與 Google 試算表讀取節點，會在真實執行後留下安全來源摘要（檔名/網域、內容指紋、分頁/範圍、頁數/筆數/文字量等），不保存原文或完整網址；流程頁新增「真實驗收證據」面板，讓非工程師能直接核對這次到底讀了什麼。驗證：新增多來源護照回歸測試，確認 PDF/信件/網址只存摘要且不含內容原文，完整測試 759 通過。
- Added｜產品護城河地圖 `PRODUCT_MOAT.md`：整理 24 項 Agent Hub 相對 n8n 與單次 Codex+API 的可防禦差異，並為每項對應實作與驗收證據，避免產品方向退化成只有聊天框或節點數量競賽。
- Added｜n8n 匯入後缺口引導：安全轉換建立草稿後，流程對話會明確列出被清空的 credential/Code、需重新確認的步驟與不支援節點，並指引使用者先看畫布、補設定、再按「測到會跑」；不是只丟一份圖讓小白自己猜哪裡壞。驗證：轉換結果與前端引導欄位都有型別檢查，n8n/來源證據回歸測試 12 通過。

### Fixed

- Fixed｜小白從零建流程會永遠卡在澄清：全新空白 workflow 描述「給我一份月份清單，每個月都要去找那個月的信、下載附件、擷取數字，最後彙整成一份檔案」，系統照設計先問一次資料來源；使用者明確回答「每次執行時讓我選檔，都用合理預設，直接建圖，不用再問」之後，平台仍然把模型的下一句反問原封不動丟回畫面，使用者除了重打同一句話之外完全沒有出路｜三個根因各自修掉：①`builder.ts` 的 clarify 分支只有兩道很窄的護欄(必須附了範例檔、或模型剛好回四句寫死的罐頭句之一)，其餘一律透傳——新增 `authorizesImmediateBuild()`(多組等義中文訊號：不用再問／別問了／直接建圖／都用預設／你決定就好)與共用的 `IMMEDIATE_BUILD_CONTRACT`，使用者授權後第一次呼叫模型就把契約寫進系統提示，模型仍反問時則餵回契約要求出圖，不再回給使用者；②`hasConcreteInitialRequest` 只看最後一則訊息，使用者一旦回答過澄清這個旗標就永遠是 false、罐頭反問護欄從第二輪起完全失效——改用累積到目前的整份需求判斷；③`requirementCheck.ts` 只看頂層節點，讀檔/擷取步驟被收進 `repeat-steps` 迴圈時 `manualFileUpload` 永遠 unmet，兩輪需求修正燒完後只能回「修正用盡」的 clarify(違反 AGENTS.md 容器型節點鐵則③)——新增 `flattenGraphNodes()` 讓型別與設定比對都涵蓋內嵌步驟。同時把 `isManualFileUploadRequested` 擴充到「執行時再提供／到時候再給檔案」這類沒有「上傳/選」動詞的等義說法，並讓「清單類名詞＋每個X都」也算 `loop` 需求(PROJECT_GOAL 已列為優先驗收情境，先前沒有任何確定性檢查在守)｜`npm test` 637 通過(新增 3 個回歸案例：等義授權說法辨識、從零建流程必須收斂成 ready 並驗證 repeat-steps／執行期檔案參數／擷取彙整輸出結構／無未解析變數與需求核對警告、迴圈內嵌步驟不得被判成沒做到)、`npx tsc --noEmit`、`npm run check:change-guard`、`git diff --check` 全過；另在**臨時建立的空白 workflow** 上用真實模型跑完整兩輪 `/build`(第一輪確定性 clarify 262ms；第二輪回 `phase:"ready"`，圖含 repeat-steps 迴圈、`filePath`／`monthListPath` 執行期參數、迴圈內 email-read→read-file→llm-decide→custom-code、外層彙整與 write-file，需求核對三項全 ✅、訊息無 ⚠️)，驗證後刪除臨時 workflow 並以 md5 比對確認 `data/workflows/` 既有 14 條流程完全沒有被讀寫｜未覆蓋風險：收斂仍需要模型至少願意出一次圖(連續 4 輪都反問時會走既有的「修正用盡」誠實回覆，不會憑空造圖)；`checkRequirements` 的 `unrequestedOutbound`／`noUnrequestedWrite` 兩項仍只掃頂層節點，藏在 `repeat-steps` 裡的未授權寄信/寫檔偵測不到(既有落差，本次未擴大安全邊界)；`authorizesImmediateBuild` 刻意寬鬆，誤判的後果是 AI 先出一版帶假設清單的草稿(等同既有連問三輪後的行為)而不是繼續反問

### Changed

- 建立跨 AI 工具共用的產品目標、依賴地圖、架構標準與變更控制制度；後續改動必須以複雜真實流程可跑通、可修復、且新手可理解為驗收標準。
- Changed｜Codex 步驟零的 8 個確認問題先前只有它自行推斷的答案、未經使用者核准就已建立治理文件｜使用者已逐項核准，`PROJECT_GOAL.md` 依核准結果調整：零技術可用性與高複雜跨系統可靠性同等重要不分先後、單人本機方向尚未鎖死不預先引入多租戶複雜度、正式訂出「複製流程」定義、驗收情境清單擴充並訂為持續累積、AI 修復說明必須簡短好懂｜使用者透過逐題確認，回答已寫回 `PROJECT_GOAL.md`｜`ARCHITECTURE.md`／`DEPENDENCY_MAP.md` 未含與此衝突之內容，故未需同步修改

### Security
- Security｜刪除流程時一併清除其情境測試與加密輸入，避免流程刪除後仍留下可被本機解密的孤兒測試資料；新增刪除生命週期回歸覆蓋。

- Security｜安全契約的副作用範圍不一致(使用者回報)：只讀契約只持久化 `file-write`/`file-modify`/`remote-write`，但執行前閘門的 delegated 那一側自己額外加上 `email`/`notify`。結果同一句「只讀取資料，不要修改」，`send-email` 藏在 `run-workflow` 或 `onFailureWorkflow` 會被擋、直接畫在本圖反而放行——同一份使用者承諾因為動作在本圖還是子流程而得到不同的安全結果｜把「承諾的範圍」寫進契約本身而不是散落在各掃描端：`dataChangePolicy.ts` 新增 `contractEffects`(要持久化的完整集合，只讀類 = 五類含 email/notify)與 `contractEffectsFor()`，`bannedEffects` 維持只含資料變更(本流程自己的外送仍由 `unrequestedOutbound` 那條規則管，兩條規則各管各的才不會重複報同一件事)；`findSafetyContractViolations` 的 direct 與 delegated 改用**同一份** `contract.bannedEffects`，刪掉 delegated 端偷加規則的那段。同時支援更精確的單項負面規則：「不要寄信」只持久化 `email`、「不要通知」只持久化 `notify`、「不要產出檔案」只持久化 `file-write`(不升級成外送禁令)，使用者明確要求的外送(「寄信給我」「telegram 通知我」)不會被記成禁止；`forbidsEmail`/`forbidsNotification` 的判斷從 `requirementCheck` 搬進 `dataChangePolicy` 共用。部分解除 UI：新增 `safetyContractUi.ts` 作為畫面與 API 共用的可放寬清單與白話詞彙表(API 支援五類、畫面只列三類的話使用者按不到的就等於不存在)，觸發面板的「🛡 只讀保護」逐項列出目前禁止的每一類、提供勾選式部分解除、按下去之前先顯示「放寬後仍會保護哪些」、預設不勾任何一項、勾滿全部要再按一次「確認完整解除」；API `allowEffects` 同步接受五類並保留 `updated_at`/`updated_note`；建立契約仍只能由使用者原話觸發，解除仍只能走這條路由，workflow PATCH 與 AI config 改動都碰不到｜`npx tsc --noEmit` 無錯、`npm test` 753 通過(+18)、`npm run check:change-guard` 通過、`git diff --check` 乾淨。重現輸出：契約內容 `["file-write","file-modify","remote-write","email","notify"]`，同一個 `send-email` 放在本圖／`run-workflow`／`onFailureWorkflow` 三個位置全部擋下且路徑正確。新增測試：本圖直接新增 send-email/telegram-notify 與迴圈內嵌通知都在建 run 前被擋、三個位置結果一致、單項契約互不波及、「不要產出檔案」不誤擋寄信與遠端讀取、放寬 remote-write 後仍擋 email/notify、放寬 email 後仍擋資料寫入；UI 層以純邏輯測試(預設不選、部分/完整解除的請求組裝、亂送 key 不能把 partial 灌成 full)加上機械比對 `TriggerSections.tsx` 真的逐項渲染勾選框、走 `allowEffects`、有二次確認、文案有講到外送｜未覆蓋風險：這台機器上沒有任何既有契約(功能尚未部署過，已查 `workflow_safety_contracts` 為 0 筆)，所以沒有寫「舊契約補上 email/notify」的遷移——若之後在別台機器上先建了三類契約再升級，那份舊契約不會自動擴大範圍；UI 仍只通過型別/lint 與機械比對，**沒有在瀏覽器實際點過**；未 build、未重啟、未部署

- Security｜P0(這一系列的最後一塊：把只讀承諾從「建圖當下」變成「每次執行前」)：`noUnrequestedWrite` 只在 build/checkRequirements 當下成立。母流程通過「只讀／不要修改」驗收後，被它呼叫的子流程或 `onFailureWorkflow` 可以在任何時候被改成 `google-sheet-append`／`send-email`／未確認 POST——母流程一個字都沒動，`startWorkflowRun()` 不會重掃委派鏈，正式執行照樣寫出去｜新增 DB 表 `workflow_safety_contracts` 與 `lib/workflow/safetyContract.ts`：契約只由**使用者自己講出「只讀／只分析／只計算／不要修改／不要寫入」時**建立(`/build` 從使用者該則訊息原文記錄，存原話＋時間當稽核依據，不採用模型的摘要或推論；「不要產出檔案」語意太窄刻意不建立契約)，存 DB 而非 workflow JSON(JSON 是 AI 建圖/修復/匯入都能改寫的地方，放那裡等於 AI 自己解除自己的限制)；解除是對稱且可稽核的另一半——`POST /api/workflows/[id]/safety-contract` 支援整份解除或只放寬其中幾項(縮小)，保留該列並寫入 `updated_at`/`updated_note`，AI 產出含寫入節點的圖**不會**自動解除(有測試釘住)；觸發面板新增「🛡 只讀保護」區塊顯示原話、建立時間、現在會被擋在哪裡與解除按鈕。`engine.ts` 的 `startWorkflowRun()` 在 `assertRunnableGraph` 之後、**建立 run 之前**呼叫 `assertSafetyContract(wf)`，用最新磁碟版重掃本圖(含 repeat-steps)+ 委派鏈(run-workflow、onFailureWorkflow、子流程自己的 onFailureWorkflow)；非 manual 觸發被擋時走 `notifyDesktop` + `console.warn`(此時還沒有 run 可以寫紀錄)。為了不再造第二份副作用判定，把「這次禁止哪些變更」抽成 `dataChangePolicy.ts`、把「這張圖自己的直接副作用」抽成 `dataChangeScan.ts`，`checkRequirements` 與執行前閘門共用同一份，委派鏈仍走既有的 `scanDelegatedWrites`/`storeSubflowResolver`/指紋確認。另補 `readOnlyParentsBlockedBy()` 反向索引警示(明確標註為加分項，不是防線——會漏掃、也擋不住多進程競態)；匯入路由一併 `clearSafetyContract`｜`npx tsc --noEmit` 無錯、`npm test` 735 通過(+14)、`npm run check:change-guard` 通過、`git diff --check` 乾淨。實際 runtime gate 重現：建圖時子流程純讀 → 放行；子流程被改成 `google-sheet-append` 後按執行 → `SafetyContractViolationError`，訊息帶 `runChild → demo-gate-child.writeSheet` 與原始要求原話，`runs` 資料表 0 列 → 0 列(擋在建立 run 之前)。新增測試：契約只從使用者原話建立、子流程改寫 sheet／改未確認 POST／失敗備援改寄信／交錯委派鏈改壞逐一在建 run 前拒絕、子流程本人已確認且指紋相符的查詢可跑而內容一變即拒、解除/縮小後預期寫入可執行、AI 改圖不自動解除、無契約既有流程不誤擋、非 manual 觸發同樣被擋、反向索引找得出受影響母流程、匯入不沿用契約；測試只驗閘門(放行案例用 `assertSafetyContract` 直接斷言，**不呼叫 `startWorkflowRun`**，避免真的把 run 排進佇列去連外)，測試流程/契約/runs/版本備份跑完自行清除｜未覆蓋風險：契約的建立詞庫是確定性中文規則，換個說法(例如純英文或很迂迴的講法)可能不會建立契約——漏建的後果是回到修這一輪之前的行為，不會誤鎖；反向索引警示尚未接進 workflow 儲存路由(目前只是可呼叫的函式)；`data/workflows/` 14 條既有流程 md5 逐檔比對零變更，全程未執行/修改/刪除任何既有 workflow，未 build、未重啟、未部署

- Security｜P0(delegated 分析的最後兩個缺口)：①`onFailureWorkflow` 沒有納入跨流程副作用分析——engine 在主流程失敗後會依 `wf.onFailureWorkflow` 直接 `startWorkflowRun(target.id, ...)`，但 requirementCheck/subflowEffects 只掃 `run-workflow`，所以使用者說「只讀取、不要修改」時，主圖再乾淨，只要失敗備援指向會寫入或外送的流程仍判安全；②父流程對子流程的 POST 一律當未確認，過度保守，一條被多處重用的純讀子流程會在每個呼叫端被重複擋下，逼使用者把共用流程拆散｜`lib/workflow/subflowEffects.ts`：掃描輸入從 `nodes` 改成 `{nodes, onFailureWorkflow}`，兩種委派共用同一套 `follow()`(解析 → 失敗就 fail closed → 成功就往下掃)，`onFailureWorkflow` 與 `run-workflow` 交錯遞迴且各自計數(`MAX_SUBFLOW_ANALYSIS_DEPTH=2` 對齊執行期 `level>=2`、新增 `MAX_FAILURE_HOPS=2` 對齊執行期 `__errorHop>2`)；解析規則與 engine 的 `findWorkflowByRef` 一致(id → 唯一名稱)，找不到／同名歧義／動態 target／循環／超過上限一律 fail closed，路徑形如 `onFailureWorkflow → fallback.writeSheet`(查不到時就是 `onFailureWorkflow(找不到流程「X」…)`，因為還沒有子流程 id 可接)；被委派流程的禁止清單另外併入 `email`/`notify`(把寄信藏進失敗備援一樣是未授權外送)。`SubflowLookup.found` 新增 `onFailureWorkflow` 與 `readOnlyApprovedNodeIds`，`subflowResolver.ts` 用**子流程自己的 workflow id** 查 `approvedReadOnlyNodeIds` 並逐一比對 method/url/headers/body 指紋——只接受該子流程本人、指紋相符的使用者確認，父流程的確認絕不替子流程背書，子流程被改動/匯入/複製/撤銷確認後指紋不符就重新 fail closed；「AI 的 `readOnly:true` 永遠只是建議」這條底線不變。engine 的 dry-run 不觸發失敗備援行為完全未動(這次只改靜態分析)｜`npx tsc --noEmit` 無錯、`npm test` 721 通過(+15)、`npm run check:change-guard` 通過、`git diff --check` 乾淨；重現輸出：主圖乾淨但 `onFailureWorkflow` 指向會寫入的流程 → `met:false` 並帶完整路徑，指向純讀流程 → `[]`；`run-workflow` 指向未確認 POST 的子流程 → `met:false`，該子流程擁有者確認過且指紋相符 → `[]`。新增測試：失敗備援指向 google-sheet-append/send-email/telegram-notify/未確認 POST/未知 custom-code 逐一被攔、純讀備援不誤擋、找不到/歧義/動態/無 resolver fail closed、備援自己的備援往下掃且超過 hop 上限 fail closed、中間層寫入路徑串接、`onFailureWorkflow ↔ run-workflow` 交錯循環被攔、子流程自己的備援也算在委派鏈上、沒有只讀類需求時不憑空多出限制；子流程 POST 未確認被攔/本人確認可通過/確認別的節點不算/改動請求內容後再次被攔/父流程確認不得背書；builder 層失敗備援繞過要被打回且具體 target 進第二輪 prompt｜未覆蓋風險：跨流程分析看的是**建圖當下**的內容，之後子流程或備援流程被改成會寫入不會回頭讓母流程的需求核對失效(執行期仍有 dry-run 與各節點自己的防護)；使用者確認的正確性由使用者負責，平台只保證它明確、可見、綁指紋、AI 改不到；`data/workflows/` 14 條既有流程 md5 逐檔比對零變更，全程未執行/修改/刪除任何既有 workflow，未 build、未重啟、未部署

- Security｜P0(接續上一筆)：只讀限制還有兩條繞過——①`run-workflow` 會去執行**另一條流程**，只看本流程的節點型別它靜態上「沒有副作用」，把寫入藏進子流程就完全放行；②節點 config 上的 `readOnly` 是 AI 建圖/修復/匯入都能直接寫進去的欄位，拿它當放行條件等於 AI 自己批准自己(誤判或幻覺就能把真的會寫入的 POST 標成唯讀並通過需求驗收與只讀試跑)｜新增 `lib/workflow/subflowEffects.ts` 做跨流程遞迴分析(含子流程裡的 repeat-steps 內嵌步驟、設定型副作用與下一層 run-workflow)，找不到 target／名稱不唯一／target 是執行期模板／子流程循環／超過分析深度(`MAX_SUBFLOW_ANALYSIS_DEPTH=2`，與執行期 `level>=2` 對齊)／沒有提供 resolver 一律 fail closed，findings 帶完整呼叫路徑(`runChild → 子流程id.writeSheet`)；resolver 由呼叫端注入(`lib/workflow/subflowResolver.ts`)，`requirementCheck` 本身不 import store/engine，解析規則與執行期 `subWorkflow.execute` 一致。信任邊界：`readOnly` 降級成純粹的「AI 建議」(`suggestsReadOnly`)，真正的放行改成使用者確認——新增 DB 表 `http_read_only_approvals` 與 `lib/workflow/httpReadOnlyApproval.ts`，批准綁 method/url/headers/body 指紋(headers 正規化欄位順序/大小寫，`readOnly` 本身不進指紋)，任一項被 AI、節點編輯、匯入或修復改動即自動失效；新增 `GET/POST/DELETE /api/workflows/[id]/http-readonly`(只用磁碟上最新版的節點設定算指紋，不接受呼叫端傳來的 config)與 NodePanel 上的確認卡(「AI 建議這是查詢，但需要你確認此端點不會寫資料」)；`engine.ts` 只讀試跑前查一次 `approvedReadOnlyNodeIds`，`dryRunSkipKind` 未傳 = 一律未確認，repeat-steps 內嵌步驟沒有真實 node id 因此永遠拿不到豁免；匯入路由存好新流程後呼叫 `clearHttpReadOnlyApprovals`。需求驗收把「圖不安全」與「等使用者確認」分開：兩者都回報在同一個 `noUnrequestedWrite`(呼叫端只需看這一個 key)，但「純粹等確認」時標 `needsUser`，`unmetFeedback` 不餵回模型、builder 不阻擋 ready、`checklistText` 顯示 🔒——避免模型為了消除 unmet 把使用者真正需要的查詢步驟刪掉；有真違規時 `needsUser` 為 false，照常要求模型修｜`npx tsc --noEmit` 無錯、`npm test` 706 通過(+29)、`npm run check:change-guard` 通過、`git diff --check` 乾淨；兩條重現案例修正後都回 `met:false`(子流程那條 `needsUser:false` 並帶呼叫路徑，readOnly 那條 `needsUser:true` 並指向使用者確認)。新增測試涵蓋：寫入型子流程、巢狀孫層子流程、循環(診斷要說「循環」而不是「太深」，所以解析順序改成先查循環再判深度)、找不到/重名/沒填/動態 target、沒有 resolver、超過深度、子流程裡的 POST 不受父流程背書、子流程迴圈內寫入與盲區、純讀子流程不誤擋；AI 宣告 readOnly 未經確認仍被攔、使用者確認後同一份精確請求可在 dry-run 執行、改 method/url/headers/body 後確認失效、header 只是順序/大小寫不同不失效、確認綁流程+節點(別條流程與別的節點不沾光)、匯入清空與使用者取消、真違規與待確認並存時不得整項放過模型、迴圈內嵌 http-request 永遠拿不到確認；builder 層兩條(子流程繞過要打回且路徑進第二輪 prompt、AI 建議唯讀的查詢步驟不得被修正迴圈刪掉且一次收斂)｜未覆蓋風險：跨流程分析看的是**建圖當下**的子流程內容，之後子流程被改成會寫入不會回頭讓母流程失效(執行期仍有 dry-run 與各節點自己的防護，但需求核對清單不會自動更新)；使用者確認的正確性由使用者負責，平台只能保證它明確、可見、綁指紋、AI 改不到，無法驗證遠端端點真的不寫入；`delegated` 分析只涵蓋 `run-workflow`，`onFailureWorkflow`(整條流程失敗時執行另一條)尚未納入同一套分析；`data/workflows/` 14 條既有流程 md5 逐檔比對零變更，全程未執行/修改/刪除任何既有 workflow，未 build、未重啟、未部署

- Security｜P0(接續上一筆)：使用者說「只讀取資料，不要修改、不要產出檔案」時，需求驗收完全放行會寫進別人 Google 試算表/簡報的節點——`checkRequirements(...).filter(x => x.key === "noUnrequestedWrite")` 對 `google-sheet-append` 與 `http-request{method:"POST"}` 都回 `[]`。根因有兩層：①`noUnrequestedWrite` 只查 `file-write`/`file-modify`，上一輪才新增的 `remote-write` 根本沒有被任何規則使用；②上一輪的 `sideEffects.test.ts` 還把「需求驗收型別必須維持重構前清單」寫死，等於把這個缺口永久釘成預期行為｜`lib/workflow/sideEffects.ts` 重寫副作用模型：把「節點實際能力(effects)」與「只讀試跑怎麼處理它(dryRun: skip/self-guard/none/by-config)」拆成兩個欄位——以前是因為「dry-run 對 google-slides-* 的處理方式跟 write-file 不同」就把它們分類成沒有副作用，這正是謊報能力的成因；逐一對照 `execute()` 實際行為重新分類全部 34 個型別，新增 `workspace-file`(下載附件/解壓/登入狀態這類只寫本次工作區的，不列入任何禁止寫入規則，否則「只讀取信件附件來分析」會被自己的規則擋死)、`approval-request`、`delegated` 三個分類；新增 `configuredSideEffects()` 作為「設定/程式碼決定副作用」的**共用**判定，`dryRun` 與 `requirementCheck` 都走它(custom-code 的寫入訊號 regex 從 dryRun 搬進來，不再只有一邊有)。`lib/workflow/requirementCheck.ts` 把單一的 `readOnlyNoWrite` 布林換成「禁止哪些副作用分類」的集合：「只讀/只分析/只計算/不要修改/不要寫入」禁止本機與遠端的所有變更、「不要產出檔案」只禁止新增本機檔(不誤擋明確要求的遠端更新)、明確要求的輸出(存成新檔／更新試算表)從禁止集合中移除；靜態判斷不出會不會寫的 custom-code(還沒產碼又沒有 intent)在只讀需求下 fail closed。`nodes/general.ts` 的 http-request 新增 `readOnly` 布林設定作為「POST 但只查詢」的明確宣告(預設關閉、只認真正的 `true`、看得見也存得進流程 JSON——刻意不用網址 regex 猜，`/v1/pages` 與 `/v1/databases/{id}/query` 只差路徑，猜錯一次就是真的送出資料)，builder prompt 同步教 AI 只在真正的查詢端點加這個宣告｜`npx tsc --noEmit` 無錯、`npm test` 677 通過(+12)、`npm run check:change-guard` 通過、`git diff --check` 乾淨；重現案例修正後 `google-sheet-append` 與 `http-request{POST}` 都回傳 `met:false` 並指名節點 path。移除「必須跟重構前清單完全一致」的凍結式斷言，改成行為測試：四個 Google 寫入節點逐一在只讀需求下 unmet、POST/PUT/PATCH/DELETE 預設被攔而 GET 與明確宣告唯讀不被攔、網址長得像查詢不算宣告、custom-code 寫入訊號被攔而純讀計算不誤擋、判斷不出時 fail closed、「不要產出檔案」不誤擋明確要求的 Sheet 更新、「不要修改原始資料存成新檔」允許新檔但仍禁遠端、迴圈內遠端寫入帶巢狀 path、builder 第二輪 prompt 含具體節點 path 且最終圖移除未授權寫入；dry-run 既有保證改用「涵蓋」而非「完全相等」斷言(原本一律略過的 8 個型別一個都不能少、self-guard 的節點不得改成整步略過)｜未覆蓋風險：**http-request 勾選 `readOnly` 後，該節點在只讀安全試跑會真的執行**——這是這次刻意新增的 opt-in 例外(需求要求設計可驗證的唯讀宣告)，預設關閉，未勾選時行為與修改前完全相同；宣告的正確性由使用者/AI 負責，平台只能保證它明確、可見、可稽核，無法驗證遠端端點真的不寫入；`run-workflow` 標成 `delegated`(副作用取決於被呼叫的流程)但尚未在只讀需求下 fail closed，需要跨流程分析才能做，本次未處理；`custom-code` 的寫入判定仍是字面規則，刻意拆解的代稱鏈防不住(既有限制，真正解法是執行期隔離)；本次為原始碼修改，**尚未重新 build／重啟常駐服務**

- Security｜P0(承上一筆的未修完部分)：把 `send-email` 埋進第四層以上的巢狀 `repeat-steps`，`checkRequirements` 與 `lintGraph` 雙雙回空、但 `repeatSteps.execute()` 其實會遞迴執行內嵌迴圈，信照樣寄得出去。根因是三個地方各走各的巢狀走訪：requirementCheck 的攤平寫死 `depth >= 3` 就停(註解卻宣稱「任意深度」)、graphLint 只驗第一層內嵌 steps、執行器完全沒有深度限制——埋在沒人看的深度就同時繞過兩道確定性檢查｜新增 `lib/workflow/repeatNesting.ts` 作為深度政策的**單一真相來源**(`MAX_REPEAT_STEPS_NESTING = 2`，即最多「迴圈裡再放一層迴圈」；`data/workflows` 現有流程實際最深只有 1 層)，並提供唯一一份走訪 `walkGraphSteps()`——它走到政策上限為止，且把「超限」與「steps 讀不出來」兩種盲區**明確回報**給呼叫端；`requirementCheck` 改用它，掃描有盲區時 `noUnrequestedOutbound`／`noUnrequestedWrite` 兩條安全規則自己也 fail closed(不是只加一項旁邊的警告——使用者若只看那個 key 判斷安不安全，看到的必須是「不確定＝不通過」)，另加 `inspectableGraph` 項說明真正該改的是結構；`graphLint` 的內嵌步驟驗證改成遞迴到上限、超限直接報錯並帶完整 path(`loop[步驟0][步驟0]`)，`assertRunnableGraph` 共用同一份 lint，所以 engine 執行入口在任何副作用前就會拒絕舊資料／匯入／繞過建圖流程而來的超深圖；`nodes/repeatSteps.ts` 另加執行期閘門(新增 `NodeContext.repeatDepth`，內嵌步驟 +1)，在迴圈開始前 `PermanentError`，是繞過 lint 時的最後一道。另新增 `lib/workflow/sideEffects.ts`：把節點副作用分類集中成一份**涵蓋 registry 每一個型別**的清單(純讀取寫 `[]`)，`requirementCheck` 與 `dryRun` 各自挑自己該管的分類，取代原本各寫一份、會漸漸漂移的手寫字串清單；`sideEffects.test.ts` 機械比對 registry 與分類表，新增節點沒分類就直接讓測試失敗｜`npx tsc --noEmit` 無錯、`npm test` 665 通過(較上一筆 +21：巢狀走訪路徑與盲區回報、四層 send-email／write-file 不再回空、合法最大深度仍被完整掃到且最深處副作用照樣攔下、合法深度乾淨流程不得誤擋、壞 JSON steps 也 fail closed、lint 遞迴驗到上限並帶完整 path、超限被 lint 與執行前閘門拒絕、執行器閘門在任何 fetch 之前就擋下且合法深度不誤擋、builder 第二輪 prompt 確實含完整 path 並收斂成合法圖、副作用分類涵蓋率與推導集合逐一比對)、`npm run check:change-guard` 通過；`data/workflows/` 14 條既有流程以 md5 逐檔比對確認零變更，全程未執行、未修改、未刪除任何既有 workflow｜未覆蓋風險：`MAX_REPEAT_STEPS_NESTING` 從無上限收為 2，若有人手動編過 3 層以上的流程會在下次執行時被拒絕(掃過既有 14 條，實際最深 1 層，無受影響者)；dry-run 的 `DRYRUN_WRITE_TYPES` 推導結果以測試釘死為與重構前完全相同(`excel-process` 仍刻意不在其中，它同時是主要的讀 Excel 途徑)，本次沒有放寬也沒有擴大任何安全邊界；`http-request`／`custom-code` 的副作用仍依 HTTP 方法與程式碼內容個別判斷，不在分類表裡固定歸類；本次為原始碼修改，**尚未重新 build／重啟常駐服務**，正在跑的 production 仍是修復前的建置

- Security｜P0：把寄信／通知／寫檔步驟收進 `repeat-steps` 的 `config.steps`，就能整個繞過使用者明說的「不要寄信」「不要通知」「只讀不要寫入」——`checkRequirements` 回傳空的 unmet 清單、自我修正迴圈不會要求移除、圖直接以 ready 交付，使用者明確禁止的外送動作被無聲放行(使用者提供可重現案例：`不要寄信也不要通知` + 迴圈內一個 `send-email`，回傳 `[]`)。根因是同一輪修改把多數正向需求檢查改走遞迴攤平的 `flattenGraphNodes`，但兩條**安全否決**規則 `unrequestedOutbound`／`noUnrequestedWrite` 仍用 `graph.nodes.filter` 只掃頂層｜`lib/workflow/requirementCheck.ts`：兩條安全否決規則改走同一份 `allNodes`(任意深度，不只一層)；`FlatGraphNode` 新增 `path`(頂層=節點 id，內嵌=`loop[步驟0]`／二層 `outer[步驟0][步驟0]`，格式與 `lintGraph` 回報內嵌步驟錯誤時一致)與 `nested` 旗標，內嵌步驟的 `id` 一律 `undefined`(不替它捏造 graph id)；`desktop-notify` 的「接了 error 分支就當合法失敗備案」豁免明確限定 `!nested`——內嵌步驟不在 `edges` 裡、接不到 error 分支，拿它沒有的 id 去比對只會配到別人的線；順帶修掉同一條規則裡的否定語氣誤判：`explicitlyWantsFileOutput` 是裸字比對，「不要**產出檔**案」被讀成「他要檔案」，`readOnlyNoWrite` 整條失效而且「產出檔案」還會被列成需求逼模型補一個 `write-file`，改成先判 `forbidsFileOutput`／`forbidsModification`(兩者分開，「不要修改原始資料，存成新檔」仍要讓存檔成立)｜`npx tsc --noEmit` 無錯、`npm test` 644 通過(新增 7 個回歸案例：巢狀 send-email＋不要寄信、四種巢狀通知型別＋不要通知、失敗備案豁免只對頂層成立、只讀／不要修改／不要產出檔案＋巢狀 write-file/excel-process、明確要求寄信/通知/寫檔時不得誤擋、二層巢狀 repeat-steps、以及 builder 層驗證具體巢狀座標真的被餵回模型且模型移除該步後才收斂)、`npm run check:change-guard` 通過、`git diff --check` 乾淨；`data/workflows/` 14 條既有流程以 md5 逐檔比對確認完全沒有被讀寫、無新增或刪除｜未覆蓋風險：安全規則涵蓋的仍是既有那份型別清單(`send-email`／四種通知／`write-file`／`excel-process`)，`google-sheet-append`／`google-sheet-update`／`http-request` 的未授權寫入本來就不在這兩條規則的範圍內(由 dry-run 的 `DRYRUN_WRITE_TYPES` 另外把關)，本次沒有擴大清單；攤平遞迴深度上限 3 層，超過的巢狀不會被掃到(`repeat-steps` 目前也沒有這種真實用法)；本次為原始碼修改，**尚未重新 build／重啟常駐服務**，正在跑的 production 仍是修復前的建置

- Security｜某次改動(commit `e4af5aa`)在使用者不知情下把真實內部業務報表結構(分頁名稱)、真實欄位字母對照與真實客戶/產品名稱寫死進了公開原始碼與測試檔，已經推上公開 GitHub repo(`origin/main`)——違反使用者本人訂下「真實工作字眼絕不能進公開 repo」的鐵則。由 xhigh 力度的多分身程式碼審查工作流找出並人工核對確認(`git log`/`git grep` 逐一驗證命中位置、確認外洩範圍集中在同一個 commit、確認 community/blueprints 目錄乾淨)｜移除 `lib/workflow/structuredExcelCompiler.ts`／`lib/workflow/simpleChatCodeRecovery.ts` 兩個檔案(連同呼叫它們的 `lib/workflow/codegen.ts`／`app/api/workflows/[id]/build/route.ts` 裡的 import 與呼叫點)——這兩個檔案的整個存在理由就是手動比對這一位使用者的真實報表結構，改寫成通用版本仍會維持結構可辨識，唯一乾淨的作法是整個移除、退回原本就有的通用 AI codegen 備援路徑；殘留在測試與註解裡的真實範例字串全部換成中性佔位字；順手掃到這個 session 稍早自己寫的資料夾功能程式碼／測試也把使用者一個真實群組名稱當範例用了進去(還沒推上去，但既然要推送一併清乾淨)，同樣換成中性佔位字。新增 `scripts/change-guard.mjs` 的隱私掃描區塊：讀取 `.gitignore` 掉的 `data/privacy-blocklist.txt`(清單本身不進版控，內容不寫進任何會被 commit 的檔案，避免清單或說明文字自己變成另一個洩漏管道)、用 `LC_ALL=en_US.UTF-8`(避免中文假陰性，這是使用者自己過去踩過的坑) + `git grep --untracked` 掃整個追蹤中的檔案樹，命中就讓 change guard 失敗並列出檔案行號；`AGENTS.md` 補上這個機制的說明，讓任何工具讀到治理文件的第一步就知道有這道防線｜移除後跑 `tsc --noEmit`／`npm test`(634 通過)／`npm run lint`／`npm run check:change-guard` 全過；用 `git grep`+`LC_ALL=en_US.UTF-8` 重新掃過一次整個追蹤樹確認乾淨；對新的隱私掃描機制做了正向測試(黑名單存在時，真的把命中的字眼寫進一個暫存檔案，確認 change guard 正確攔下並列出檔名行號，過程中這份 CHANGELOG 說明文字自己也曾經直接引用了外洩字眼原文，被這道新檢查正確攔下、才發現連事故說明本身都要避免逐字引用)與復原後的乾淨結果，兩者都用指令輸出驗證過，不是只讀程式碼判斷｜已經推上 `origin/main` 的舊 commit(`e4af5aa`)本身在 git 歷史裡仍查得到這些字眼，這次只確保**目前最新版本**乾淨，沒有重寫 git 歷史／force-push(使用者當下選擇先不做這個更高風險的操作)；`data/privacy-blocklist.txt` 是這台機器上手動維護的清單，不會隨這次修復自動涵蓋未來踩到的新字眼，仍需要人工發現後手動加入

### Added

- Added｜新節點若忘了同步補齊畫布圖示／分類色，會悄悄退化成灰色方塊，使用者以為系統壞了｜新增 `lib/workflow/nodeRegistryConsistency.test.ts`，機械比對 `registry.ts` 與 `nodeVisuals.tsx` 的節點型別是否一致（含殘留型別偵測）｜人工暫時移除一個型別的 ICONS 項目、確認測試失敗且訊息點名該型別，再復原｜只涵蓋 ICONS／TYPE_META 兩處，`nodeSummary.ts` 的卡片摘要仍未機械化檢查
- Added｜建圖/修圖/產程式碼呼叫本機 Claude Code 時，`builder.ts` 建圖固定寫死 `effort:"low"`、`codegen.ts` 產生自訂程式碼視情境猜 `low`/`medium`，理由是「有確定性檢查攔著、犧牲推理力度換速度不會出事」——但確定性檢查只攔得住寫進規則裡的情況，攔不住的還是要看模型自己想得夠不夠深，寫死低推理力度等於一開始就限制它。使用者明確要求「不能為了速度讓 AI 建不了工作流」，且當下 gateway 本來就常態不穩定拖慢整體速度，多花的推理時間相對可忽略｜新增 `settingsStore.ts` 的 `getBuilderEffort`/`setBuilderEffort`(預設 `high`)，套用到 `builder.ts`(建圖)、`codegen.ts`(產程式碼)、`graphRepair.ts`(讓 AI 修)、`nodeEditor.ts`(點節點修改)四個呼叫 Claude Code 的地方；設定頁新增「🧭 AI 推理力度」區塊(低/中/高三檔)，透過 `/api/settings` 的 `builderEffort` 欄位讀寫｜用 Playwright 對真實設定頁點擊切換三檔並確認伺服器端數值即時更新；用 curl 直接呼叫 `/api/settings` 確認 GET 預設值、POST 更新、無效值拒絕(400)都正確｜使用者切成「低」之後實際建圖品質有沒有感受得到差異，本次未做 A/B 比較

### Fixed

- Fixed｜`normalizeFolderPath()` 對超過 40 字的單一路徑片段是整段濾掉，不是截斷——建巢狀資料夾時若子資料夾名稱過長，會悄悄退化成只剩已存在的上層路徑，`POST /api/folders` 回 `{ok:true}` 卻什麼都沒建立、使用者打的名稱整段消失也沒有任何錯誤提示；改工作流所屬資料夾(`PATCH /api/workflows/[id]`)若新名稱單段過長，更會整段變空字串、意外把工作流移出資料夾｜由 xhigh 力度的多分身程式碼審查工作流找出並驗證(CONFIRMED)；`lib/settingsStore.ts` 的 `normalizeFolderPath()` 改成對每段做 `.slice(0,40)` 截斷，不再用 `.filter()` 整段丟棄｜更新 `lib/settingsStore.test.ts` 對應測試反映新的截斷行為，並新增「單段過長時不會誤判成空輸入」的測試；部署後用真實 API 重現兩種情境：巢狀資料夾建立(44字子資料夾名稱，確認回傳路徑真的帶著截斷後 40 字的子資料夾，不是退化成上層路徑)、工作流改名(43字單段，確認 `group` 欄位是截斷後的 40 字而非清空)，兩者皆通過後清除測試資料｜150 字整條路徑上限的既有 fallback 邏輯不受影響(該分支只在極端多段情境觸發，未在本輪變更)
- Fixed｜整圖套用 API(`PUT /api/workflows/[id]/build`)收到選填欄位為 JSON `null` 時行為失控：`schedule:null` 直接在讀 `schedule.cron` 時 TypeError 變 HTTP 500，`triggerParams:null` 被誤判成「執行參數格式不正確」擋掉整次套用——模型輸出 JSON 與一般 API 客戶端都常把選填欄位寫成 null 而非省略，這不是理論情境(本輪以「小白從零建流程」實測旅程直接踩到)｜`app/api/workflows/[id]/build/route.ts` 在欄位驗證前把 `triggerParams`/`schedule`/`autoWebhook`/`onFailureWorkflow` 四個選填欄位的 `null` 一律正規化為 `undefined`(視為「沒有提供」)｜修復前用 curl 重現 500 與 400；部署後用同一 payload 回歸實測改回 HTTP 200 且套用成功，測試流程已清除｜route handler 無法直接單元測試(需起 server)，此修法以真實 API 回歸取代單元測試；其他 route 若有同型「!== undefined 但沒防 null」的選填欄位判斷，本輪未逐一掃描
- 將一般流程畫面的內部模型選項、程式規格與外部服務技術詞移出主要操作路徑，改由白話對話與明確進階入口承接。
- Google Slides 使用官方 OAuth 與 Slides API 刷新連結圖表；設定後先以只讀方式驗證可讀到正確簡報與圖表，再允許正式更新。
- Fixed｜安全試跑(dry-run)時「等人簽核」節點完全沒有防護，會真的建立簽核紀錄、發送真實 Telegram／Email／桌面通知、並卡住整條 run 等一個不存在的真人回應｜`lib/workflow/nodes/waitApproval.ts` 在通道設定驗證之後、建立簽核紀錄之前加 `ctx.dryRun` 提早返回(模擬核准，不建立紀錄、不發通知)；同時把它對 `../store` 的靜態 import 改成動態 import，修正一個會在測試直接載入此節點檔時炸掉的模組循環｜新增 `lib/workflow/nodes/waitApproval.test.ts`，以「先移除防護、確認測試真的會失敗（攔到真實 fetch 呼叫）、再復原」的方式實測驗證，而非只讀程式碼判斷｜`repeat-steps` 內嵌 `wait-approval` 已由既有 lint 規則禁止，故不需另外處理巢狀情境
- Fixed｜安全試跑(dry-run)時「Excel 篩選+highlight」節點完全沒有防護，會真的另存新檔並複製一份到使用者的真實桌面(`~/Desktop`)｜`lib/workflow/nodes/excelProcess.ts` 在完成讀取來源檔／找欄位／日期篩選(這段本來就該真的跑，用來證明邏輯正確)之後、真正寫檔之前加 `ctx.dryRun` 提早返回，回報比對到的筆數但不產生任何檔案｜**用真實 API 建一條隔離測試流程、餵合成的測試 Excel、實際呼叫跟畫面「安全試跑」按鈕相同的執行端點，修復前先重現一次(桌面真的多出檔案)，修好後同一條流程重跑一次，確認桌面沒有再多出檔案，而不是只看程式碼判斷｜同一輪也用同樣方式實測了 `unzip.ts`——它在 dry-run 期間一樣會真的解壓縮，但寫入的是流程自己的 `data/outputs/<runId>/` 內部暫存資料夾，不會流到使用者桌面，也不會出現在「檔案」頁的一般輸出清單(kind=intermediate)，經確認風險層級不同，本次未同步修改，留待需要時再處理
- Fixed｜節點卡住需要人補答時附上的執行證據沒有長度上限，掃描到的整頁內容可能讓對話訊息變成一大堆文字，違背「AI 說明必須簡短好懂」的產品原則｜`app/workflows/[id]/page.tsx` 組證據字串處加每行字數上限與行數上限，超過的部分提示到執行紀錄頁看完整內容｜讀碼確認上限套用位置正確；此路徑需要真的觸發 needs-human 失敗才能端到端重現，本次未另外用真實失敗案例截圖驗證｜若之後有其他呼叫端組出類似證據字串，仍需各自套用相同上限，尚未抽成共用工具函式
- Fixed｜以真實使用者身分測試「LINE 官方帳號接客訊息」情境時，AI 建圖後的自我檢查訊息把好幾個不同的上游欄位名(如 userId、replyToken)全部壓成同一句「前面步驟提供的資料」，使用者完全分不出是哪個欄位缺漏；同一則訊息也教使用者去設定頁「新增一個欄位『前面步驟提供的資料』」，把真正該建立的帳密欄位名稱(如 lineChannelToken)整個吃掉，使用者根本不知道要新增的欄位該叫什麼名字｜`lib/workflow/plainLanguage.ts` 的 `glossToken()` 不再把所有不認得的欄位名壓成同一句通用文字，一律保留原始名稱、用「」引號框起來；`lib/workflow/graphLint.ts` 的欄位不存在警告訊息也把上游可用欄位清單逐一加上引號，讓清單裡的每個欄位名都能被既有的引號保護規則整段保留｜這兩處修完後，用 Playwright 對著真實畫面重建同一個 LINE 接客情境，親眼確認新訊息裡欄位名稱清楚可辨——過程中意外發現 glossToken 自己新產生的引號會被下一輪 `hideTechnicalContracts` 的 camelCase 規則二次處理，變成「前面步驟提供的「前面步驟提供的「X」資料」資料」的雙重包裹，這是只讀程式碼不會發現、只有實際跑一次畫面才抓到的問題，修法是在 humanizer 產生白話文字之後、丟進 `hideTechnicalContracts` 之前再保護一層(用第三組跟既有兩層都不同的標記字元)，撐到整條 `.replace()` 鏈最後才一起還原｜三個層面都各自補了測試(`graphLint.test.ts`、`explainHints.test.ts` 兩則)，並用「先拔掉保護、確認測試真的抓到雙重包裹、再復原」驗證測試本身有效，而非只讀程式碼判斷
- Fixed｜以真實使用者身分描述「每天收到主旨含『每日銷售報表』的信，裡面有Excel附件，用AI比較今天跟昨天的銷售金額」這種完全自然的口語需求時，被誤判成「沒有說資料來源在哪」而擋下建圖，還回一句提到「安排 5 張簡報內容」的罐頭澄清句——使用者根本沒提過簡報，完全看不懂這句話在講什麼｜根因有兩個：① `lib/workflow/builder.ts` 的 `needsBusinessDataSourceClarification()` 判斷「有沒有講信件附件」時用的正規表示式要求「信件」「附件」兩個詞緊鄰，但「收到一封信…裡面有附件」這種自然斷句會被中間的字隔開而配不到，改成只要文字裡同時出現收信/信箱類詞和「附件」二字就算已指明來源，不要求緊鄰；② 擋下時回的澄清句寫死「我會安排 5 張簡報內容」，這句話是替某個特定的簡報情境寫的範例文字，被當成所有數字類需求的通用回覆在用，改成不預設下一步具體長怎樣｜新增 `builder.test.ts` 測試並用「先拔掉新規則、確認測試真的抓到誤判、再復原」驗證
- Fixed｜緊接著上一則修復，繼續以真實使用者身分測試同一個資料來源澄清流程時，照系統自己在澄清句裡建議的措辭「每次執行時讓我選檔」原文回覆，卻被同一句澄清句再問一次——系統聽不懂自己講的話｜`lib/workflow/requirementCheck.ts` 的 `isManualFileUploadRequested()` 硬性要求「檔案」兩字連在一起才算，但白話縮寫「選檔」只有「檔」一個字，改成「檔(?:案)?」讓「檔」單獨出現也算數｜新增 `requirementCheck.test.ts` 測試並用「先拔掉修法、確認測試真的抓到『系統聽不懂自己的建議』、再復原」驗證
- Fixed｜以真實使用者身分測試「Telegram 費用簽核」情境(AI判斷金額+類別→超門檻走人工簽核→核准/拒絕雙路徑)時，AI 教使用者去 Telegram 搜尋官方帳號「@BotFather」建立機器人取得 Token，這個 Telegram 官方帳號名稱剛好長得像 camelCase 內部變數，被誤套用「前面步驟提供的資料」框架、改寫成「@前面步驟提供的「BotFather」資料」——使用者完全看不懂要去哪裡找、也分不出這是不是系統壞了｜`lib/workflow/plainLanguage.ts` 的 `TOKEN_GLOSS` 加入 `BotFather` 一類「長得像程式變數，但其實是真實世界專有名詞」的原樣傳回項目，不套用資料欄位的說法框架｜新增 `explainHints.test.ts` 測試並用「先拔掉這筆對照表項目、確認測試真的抓到『BotFather 被誤譯』、再復原」驗證
- Fixed｜以真實使用者身分測試「三分店營業額比較+超過門檻發 Telegram 通知」情境時，最終流程完全沒有通知節點，需求核對清單連「通知管道」這一項都沒出現(不是打叉，是整項消失)｜根因是 `requirementCheck.ts` 的 `forbidsNotification`/`forbidsEmail` 否定詞清單裡有裸字「別」，使用者原句「發Telegram通知我要**特別**關注」的「特別」把「別」字誤判成「別通知」(不要通知)的否定語氣，整個通知需求被當成使用者自己禁止——跟後面在同一輪抓到的「以免/免得排程」誤判成否定排程是同一類 bug(`isScheduledExecutionRequested` 的裸字「免」)。同一輪也發現：`isScheduledExecutionRequested` 的「明確自動」動詞清單只認執行/跑/處理/更新/觸發/寄送/填寫，使用者說「選方案1**自動抓信**」時「抓」不在清單內，AI 明確承諾的排程整個沒建；`wantsAutoWebhook`(原內嵌邏輯，此輪抽成獨立函式)要求「外部工具/程式/系統/服務」等技術詞必須離「觸發」8字以內，使用者說「希望有一個外部**網址**...立刻**觸發**」中間隔了21字且用「網址」不是技術詞，AI 承諾會生成的 Webhook 網址沒生成｜`forbidsNotification`/`forbidsEmail` 否定詞清單移除裸字「別」，改用「別(?:再)?+動作詞」的緊鄰祈使句型；`isScheduledExecutionRequested` 動詞清單加入抓/收/找/搜尋/下載/讀取/擷取/同步，並修正「以免/免得」連接詞誤判；`wantsAutoWebhook` 重新設計成「提到外部網址/連結」+「提到觸發」兩個獨立訊號而非緊鄰比對｜每項都先寫失敗測試重現、確認失敗、修好、確認通過，並用原始真實對話重跑一次確認正式環境行為改變｜否定詞類 bug 屬於同一根因(裸字當否定信號)，可能還有其他未發現的類似情況，本輪掃描已針對已知的自然語言判斷函式逐一檢查但無法保證窮盡
- Fixed｜真實使用者的 Google Sheet 寫入 Apps Script 從「getSheetByName 對 null」錯誤(需重新部署)一路修到「找不到分頁」錯誤，過程中 AI 誤判「這是另一份試算表」，未經任何驗證就把 5 個節點原本能用的 `scriptUrl` 直接清空成空字串、要求使用者重新部署——猜測本身是錯的(其實是同一份試算表)，使用者反覆重新部署好幾次都救不回來，最終得由使用者外部反映才發現並修好，完全違背「問題都在對話裡讓 AI 解決」的產品目標｜`lib/workflow/builder.ts` 的 `phase:"edits"` 處理迴圈、`lib/workflow/nodeEditor.ts` 的單節點對話修改、`lib/workflow/graphRepair.ts` 的 `applyNodeConfigEdits`(對話修改與「讓 AI 修」修復迴圈共用同一份)三處都加上同一道防護：把一個「目前已經有值」的連結/端點類欄位(欄位名含 url/網址/端點)改成空字串時，除非使用者原話明確要求清空/重設，一律擋下來、餵回去要求先講清楚具體理由，不能憑一個沒驗證過的理論就把已經在運作的設定砍掉。同一輪也修正 `classifyFailure`：「找不到分頁」「找不到...欄」(Excel 找不到指定欄位)、Telegram Chat ID／LINE 傳送對象 ID 錯誤，這幾種「只有使用者知道答案、AI 沒辦法連進使用者的外部服務去確認正確值」的錯誤，原本會落到預設的 `ai-fixable`，讓修復迴圈對著它們一直空轉重試｜每項都用失敗測試重現(`builder.test.ts` 新增 edits-清空防護測試、`failureClassification.test.ts` 新增對應分類測試)並用「先拔掉修法、確認測試真的抓到問題、再復原」驗證；`nodeEditor.ts`/`graphRepair.ts` 的防護因為沒有使用者原話可查意圖，設計成無條件擋下(比 builder.ts 更保守)｜`graphRepair.ts` 的防護是「讓 AI 修」自動修復迴圈唯一會清空連結欄位的路徑，但這條迴圈目前沒有其他「清空非 URL 欄位」的類似風險尚待評估
- Fixed｜Google Apps Script 寫入節點的「🔎 檢查並套用」安全檢查按鈕，其 `capabilities` 探測分支在 `doPost` 最前面就直接回 `{ok:true}`，完全沒有先呼叫 `SpreadsheetApp.getActiveSpreadsheet()`——就算腳本專案沒綁定在正確的試算表上(`getActiveSpreadsheet()`回 null)，這個「安全檢查」仍然回報成功，使用者看到綠燈以為部署好了，一去真的寫資料才發現照樣壞掉，白白重新部署好幾次都修不好｜`lib/googleSheetScriptTemplate.ts` 的官方範本把 `ss` 是否為 null 的檢查移到 `capabilities` 分支之前，讓安全檢查也會誠實反映真正的綁定狀態；同一輪也把「找不到分頁」的錯誤內容加上「目前綁定的試算表叫什麼名字＋裡面實際有哪些分頁」，讓使用者一比對就知道是分頁被改名了還是腳本綁錯了試算表；另外新增了對 Google Drive「存取遭拒/需要授權」這個常見的貼新程式碼後第一次執行關卡的辨識，翻成「開 Apps Script 編輯器→選函式→按執行→同意授權」的具體步驟，不需要重新部署｜`lib/googleSheetScriptTemplate.test.ts`/`lib/workflow/nodes/googleSheet.test.ts` 新增對應測試，並用「先拔掉修法、確認測試真的抓到假綠燈/沒有列出分頁清單/沒有翻譯授權錯誤、再復原」驗證｜這些修法只影響範本原始碼，使用者需要重新複製貼上部署一次才會生效，不會回溯修好已經部署的舊版腳本
- Fixed｜真實使用者的排程流程，執行引擎(`lib/workflow/engine.ts`)在任何節點失敗且沒有接 `error` 出線時會直接 `break` 整條 run 迴圈——這包含完全不相干、沒有依賴關係的其他平行分支，對「核准後同時觸發好幾個獨立完成動作」(例如核准後同時寫入試算表+發 Telegram 通知)這種架構是致命的：其中一個分支寫入失敗，另一個完全獨立的通知分支永遠卡在 pending，不會執行也不會回報｜改用既有的 `skipDeadDownstream` 機制取代 `break`，只跳過真正依賴失敗節點的下游，不相干的平行分支繼續正常執行；新增 `unhandledFailures` 清單追蹤所有「沒有接錯誤分支的失敗節點」，執行摘要會逐一點名每個獨立失敗的分支，不是只講第一個｜新增 `lib/workflow/engine.fanout.test.ts`，用真實引擎(非 mock)建一條「wait-approval→[寫入試算表, 桌面通知]」測試流程，先重現 bug(通知分支永遠 pending)，修好後同一條流程重跑確認兩個分支都真的執行；另外透過真實對話建圖+執行驗證正式環境行為改變｜這是本次整輪掃描裡影響範圍最大的一個 bug，任何「核准/分流後同時做好幾件事」的流程架構都受影響
- Fixed｜通知節點在 Telegram/LINE 官方帳號被使用者封鎖(或從未按過「開始」)時，Telegram/LINE 回 403，但錯誤處理直接把原始英文 API 回應丟給使用者，跟 Slack 那邊已經有的 403(action_prohibited)具體指引不一致｜`lib/workflow/nodes/notify.ts` 的 `sendTelegram`/`sendLine` 都補上「請到 Telegram/LINE 找到這個帳號，確認沒有封鎖它，並傳一句話開始對話」的具體步驟，跟 Slack 分支統一｜新增對應測試，先拔掉修法確認測試抓到原始英文回應、再復原驗證
- Fixed｜使用者說「不用等我核准，自動處理就好」明確表示不要簽核關卡，但 `requirementCheck.ts` 的簽核判斷規則沒有否定句處理，「核准」二字仍被抓到、誤判成需要 wait-approval 節點——跟裸字「別」是同一類「否定句沒被辨識」問題｜比照 `forbidsNotification` 加上 `forbidsApproval` 否定詞判斷，涵蓋不要/不需/不用/不必/免+簽核/核准/審核/批准/確認/過我這關｜新增測試並用先拔掉、確認測試抓到誤判、再復原驗證
- Fixed｜對話訊息在報錯、做更改時使用者反映「很亂，看不懂」，追查發現兩個獨立問題：①一次改好幾個節點的「已實際套用到節點」摘要，直接拿設定表單用的完整欄位標籤(含解釋用的括號子句，如「Apps Script 寫入網址（必須以 /exec 結尾；不是 Google 試算表網址）」)當每一行的欄位名，5 個節點就把同一句落落長的說明重複印 5 次；②AI 生成的 message 常把「目前狀態」「真正原因」「使用者接下來要做什麼」這幾件事黏成一段連續文字，沒有分段，使用者要逐字讀完整段才找得到重點｜①在 `lib/workflow/plainLanguage.ts` 新增 `shortFieldLabel()`，去掉標籤裡的括號說明只留欄位名稱，套用到 `app/api/workflows/[id]/build/route.ts` 組摘要的地方；②`lib/workflow/builder.ts` 的 `systemPrompt`(從零建圖)與 `existingGraphEditSystemPrompt`(既有流程修改)兩份各自獨立的提示都加上「不同性質的內容之間要空一行分開，不要黏成一段」的排版規則｜新增 `shortFieldLabel` 測試(含正常標籤不受影響的回歸測試)與兩份系統提示各自的內容比對測試｜提示詞規則能否讓模型確實照做仍取決於模型本身的服從度，這裡只保證「規則有被餵進提示詞」，無法百分之百保證每次生成都排版正確
- Fixed｜節點卡片摘要(`nodeSummary.ts`)的 if-condition/set-variable/template-text/google-sheet-append 這幾種節點直接回傳原始 config 字串，若欄位內容含 `{{欄位}}` 會原樣顯示在畫布卡片上，只是目前唯二呼叫端(`page.tsx`、`explain.ts`)剛好都自己包了 `plainLanguage()` 才沒有真的出過包——這是「下一個沒包好的呼叫端就會踩到」的隱患，跟 LINE 情境那次雙重包裹是同一個根因類別(某處產生的技術字詞沒有在源頭處理乾淨，要靠每個呼叫端自己記得包一層)｜讓 `nodeSummary()` 對這幾種節點的回傳值自己呼叫 `plainLanguage()`，不依賴呼叫端記得包，不管未來哪裡呼叫它都不會漏｜新增測試涵蓋四種節點類型的白話化，並新增一則回歸測試確認一般檔案路徑/URL/單位這些既有摘要不受影響
- Fixed｜對話框沒有任何捲動管理，新訊息/思考中提示/待確認卡片一出現，捲軸位置維持在使用者上次停留處(通常是最上面)，看起來像「訊息都跑到很上面」，其實是最新內容長在畫面外看不到｜前兩版嘗試(依 state 變化觸發 `scrollIntoView`、`useEffect`+`useRef` 掛 `MutationObserver`)都因為對話面板是條件式 render 的其中一支、掛載時機抓不準而沒有真的生效，第三版改用 callback ref：`app/workflows/[id]/page.tsx` 的可捲動容器改用 `chatScrollCallbackRef`，只要這個 DOM 節點被掛上就設定 `MutationObserver`，不管是哪一輪 render 造成的掛載都不會錯過｜用 Playwright 對真實畫面實測：開一個有 12 輪對話的流程確認畫面直接顯示最新一則、模擬新訊息動態出現確認畫面持續跟著捲到底，不只是讀程式碼判斷｜純前端捲動邏輯，不影響任何資料流
- Fixed｜以真實使用者身分測試「子流程重用」架構(兩條父流程共用同一段「格式化+桌面通知」子流程，帶不同資料)時發現三個問題：①`run-workflow` 節點的 `paramsJson` 欄位自己在介面上教使用者用 `{{欄位}}` 帶值進 JSON 參數，但底層用純文字取代而非 JSON 安全轉義，只要帶進去的值裡有換行(例如把一段報表內文從一個流程傳給另一個)，組出來的 JSON 直接壞掉、報一句看不懂的「Bad control character」；②子流程執行完，父流程只讀「子流程最後一個節點」自己新增的欄位，但資料庫只存每個節點自己新增的欄位、不是引擎執行期合併後的完整狀態，導致子流程裡任何非最後一步算出的欄位在傳回父流程時會無聲消失；③模型回傳格式錯誤、接近但不是合法 JSON 時，原本的救援路徑會把這段半殘的 JSON 也丟進白話化流程，產出比原始錯誤更難懂的內容(如「"config":整理好的資料」)｜①新增 `lib/workflow/nodeHelpers.ts` 的 `resolveJsonSafeTemplate()`，取代 `subWorkflow.ts` 原本用的一般文字取代；②新增 `collectSubRunOutput()`，改成合併子流程所有成功節點執行順序上的輸出，重建正確的累積狀態；③新增 `looksLikeBrokenStructuredOutput()` 偵測明顯是壞掉的結構化輸出，改回一句誠實的「請重試」而非硬套白話化｜用真實對話建立兩個共用同一子流程的父流程、各自帶不同資料執行，確認子流程真的被重用(子流程本身的執行次數遞增)、名稱與 id 兩種指定方式都可行、改共用子流程後兩個父流程下次執行立刻套用新邏輯｜同一輪也發現 `subWorkflow.ts` 有個跟 `waitApproval.ts` 同一類的靜態 import 循環，改成動態 import 修正
- Fixed｜以真實使用者身分測試「客服訊息分類→switch 分流到 4 種完全不同處理路徑」架構時，AI 教使用者去設定頁新增 Telegram Bot Token 的說明文字顯示成「新增欄位「前面步驟提供的「telegramBotToken」資料」」——完全是垃圾字｜根因是 `plainLanguage.ts` 的 `protectLiteralPieces()` 只保護「」『』全形引號括住的內容，沒保護英文反引號(`` ` ``)這種 LLM 標記技術詞常用的寫法，導致 `` `telegramBotToken` `` 先被 camelCase 抓漏規則改寫一次，再被「反引號轉全形引號」的規則把已經改壞的內容包上第二層引號｜`protectLiteralPieces()` 新增對單一反引號的保護(排除三個以上反引號的程式碼區塊，那個由另一條規則整段隱藏)，直接轉成「」全形引號顯示格式｜先寫測試重現雙重包裹、確認失敗、修好、確認通過；分流邏輯本身另外用 4 種真實客服訊息各測一次，確認分類詞跟 switch 分支設定完全對得上、全部正確路由到各自不同分支
- Fixed｜以真實使用者身分測試「批次多地區資料驗證關卡」與「跨系統資料核對(訂單vs收款)」這兩種架構時，一次抓到 4 個同一類「判斷邏輯只認固定關鍵字或單一固定欄位名，不認語意上等價的說法」的漏洞：①`isManualFileUploadRequested`/`wireManualFileUpload` 的「這個檔案有沒有真的被讀取」判斷只認 4 種內建讀檔節點(read-file/excel-process/pdf-read/unzip)，完全不認 custom-code——但系統提示自己教 AI「內建節點做不到就用 custom-code」，複雜的多重業務規則驗證正是內建節點做不到、必須用 custom-code 的典型情境，造成需求無論自我修正迴圈重跑幾輪都無法被判定「已滿足」；②手動上傳檔案的「met」判斷寫死要求欄位 key 字面等於 `filePath`，但對帳這種天生需要一次上傳多個檔案的情境，AI 自然會取名 `orderFilePath`/`paymentFilePath` 這種語意化名稱，永遠對不上；③使用者說「不要用 Google 試算表了，改成上傳CSV」這種中途撤回舊說法，`sheetUpdate` 判斷邏輯只看有沒有出現「試算表」關鍵字，撤回後需求已經不存在，卻還是硬性要求要有試算表節點；④同樣地「不用排程」也只看「排程」兩字有沒有出現｜①新增共用的 `hasCustomCodeFileReader()`，兩處判斷都認得意圖裡提到檔案/上傳相關字眼的 custom-code 節點；②`isManualFileUploadRequested` 的「met」條件改成接受任何檔案語意的欄位名，而非硬性等於 `filePath`；`wireManualFileUpload` 偵測到語意化欄位已存在時不再多塞一個用不到的 `filePath` 上傳框；③④比照 `forbidsNotification` 加上「不要用/不用/改成/換成」的撤回偵測，比對 `checkRequirements` 分頁 393-395 行處理，同一份判斷邏輯也套用到 `isScheduledExecutionRequested`｜每項都先寫失敗測試重現(尤其 ①實測連續 3 輪自我修正都被同一句「沒有讀檔步驟」擋下，修好後同一請求第一輪就過)，確認通過後用真實對帳資料(5列訂單CSV+4列付款Excel，涵蓋全部4種分類)實際執行驗證核對結果正確
- Fixed｜`builder.ts` 的系統提示完全沒有教 AI「使用者在同一次對話裡描述兩條獨立觸發、但中間有一大段做法完全一樣的流程時，該建議拆成 `run-workflow` 子流程共用」——這正是 `PROJECT_GOAL.md` 已核准的「現階段優先驗收情境」明確列出的「子流程組合」要求，但系統提示裡完全沒有對應的主動指引，只能看模型自己會不會想到｜新增【子流程重用】段落，教 AI 在「同一段做法被重複用到」時建議拆出獨立流程用 `run-workflow` 呼叫，同時明講「只描述一件事就不要硬拆」避免矯枉過正｜新增系統提示內容比對測試，確認關鍵指引文字有被餵進提示詞；因今日 gateway 不穩定，完整的「兩條相似流程→AI 主動建議拆子流程」端到端對話驗證受外部逾時影響未能完整跑完，已用可重現的確定性測試(提示詞內容比對)驗證修改本身正確生效
- n8n 匯入流程新增「安全試跑後才允許無人值守」閘門：排程、資料夾監聽、webhook、LINE webhook 會等待同一版圖的成功 dry-run；手動執行仍可在明確確認後進行。
- n8n 特殊／未搬移連線的解鎖錯誤改為顯示實際數量，不再把所有缺口誤稱為「沒有連線」。
- n8n 匯入流程的「尚未安全試跑」現在會持續顯示在工具列與自動觸發面板，重整後仍保留清楚的下一步，不再只靠一次性提示。
- 自動觸發面板新增共用「啟用前檢查卡」：一次列出草稿狀態、結構錯誤、缺少帳密、驗收證據過期、只讀保護與 n8n 遷移／安全試跑缺口，並為每項提供白話下一步。
- 自動觸發檢查新增版本化「啟用護照」：同一版圖與同一結果去重保存，流程版本或阻擋原因變化才留下新快照；觸發面板會顯示最近檢查來源與時間。
